## Summary

<!-- What changed and why. -->

## Release note

<!--
Fill this in only if a real user of the app would notice this change.
If not (internal refactor, test-only, tooling, docs, coordination), write
"N/A" on the Category line below and delete Title/Description.

Category: one of Added, Fixed, Changed
Title: a few words, no ending period
Description: one plain-English sentence a non-technical user would understand,
             ending with a period. No code names, file paths, or internal jargon.

Then put the note into docs/WHATS_NEW.md yourself, in this pull request. Once
the pull request exists and has a number, run this from its branch:

    node scripts/append-release-note.mjs --pr <this PR's number>

and commit docs/WHATS_NEW.md onto the same branch. The script reads the section
below, so the wording and placement come out right without you formatting it by
hand; it does nothing when the Category is N/A.

This used to happen automatically on merge. It cannot any more: the job pushed
straight to main, and the branch rule there requires a CI gate run that a direct
push can never produce. See #1795.
-->

Category:
Title:
Description:
