node_modules/
.pnpm-store/
dist/
**/dist/
coverage/
**/coverage/
.turbo/
**/.turbo/
.git/
playwright-report/
test-results/
pnpm-lock.yaml
.claude/worktrees/
.claude/workflows/
.claude/skills/

# Point-in-time security-audit records — preserved verbatim, not style-gated
docs/audit/
docs/audits/

# Coordinator handoff docs — authored by the coordinator agent; never format
docs/coordination/
docs/superpowers/handoffs/

# Subagent-driven-development scratch — git-ignored briefs/reports/ledger/diffs
.superpowers/sdd/

# Frozen design handoff artifacts (keep byte-identical to the exported design project)
docs/superpowers/design/
# Same rule, second location: verbatim mockup screens pulled from the "Jarvis — Park Press Design
# System" project in Claude Design. They are the target the code is measured against, so they have
# to stay byte-identical to the source — a format pass rewrote all seven of them once already.
docs/superpowers/specs/job-search-mockup/

# #1326: unit-test fixtures mkdtemp() a ".tmp-*" directory directly in the repo root
# (external-worker-runtime, external-module-invocation-budget, module-sdk-worker); a
# concurrent format:check run could otherwise flag a generated file inside one mid-test.
.tmp-*/
