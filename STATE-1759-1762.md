# Live UI fixes state (Agentation loop) -- branch ui-live-fixes

Workflow: Ben browses the dev instance (http://192.168.50.36:5173) and drops live
annotations via Agentation. Watch with mcp__agentation__agentation_watch_annotations
in a loop. For each annotation:
- Mechanical/copy/layout/bug fix -> fix directly, commit (explicit paths only,
  never git add -A -- this is a shared checkout, see the shared-checkout skill),
  reply explaining what changed, then resolve.
- Needs a product decision -> file a GitHub issue (repo motioneso/moss), reply
  linking the issue number, then ACKNOWLEDGE (not resolve) so it stays pending
  and Ben knows not to re-annotate it.

No PR opened yet for this branch. When one is opened: fill in the Release Note
section and run `node scripts/append-release-note.mjs --pr <number>`, commit
the resulting docs/WHATS_NEW.md change onto the same branch (CLAUDE.md rule).

## Watch loop quirk (confirmed again this session)

Every page reload re-submits all still-visible annotations under brand-new
session/annotation ids, sometimes with the exact same timestamp as the
original. Always cross-check comment text + timestamp against already-handled
annotations before redoing work -- dismiss the duplicate id with a note
pointing at the original id, don't refix.

## Done this session (all committed on ui-live-fixes, in order)

Earlier in session (see prior commits, already resolved before this pass):
theme rgba validation, vault chooser env var name, personal nav group label,
Food module input width, Today mobile meds card order, theme delete confirm +
toast, persona pane dial/preview cleanup, Job Search nav icon.

This pass:
- Oversight pane: removed em dash from description, deleted the low-priority
  footer note (settings-admin-panes.tsx). Commit 25525e734.
- Chat module settings: "Set up" under Voice was linking to the *personal*
  Assistant & AI pane, which has no transcription config -- voice is
  instance-wide and only configurable in the *admin* AI Providers pane
  (settings-ai-admin-pane.tsx / settings-voice-config-group.tsx). Fixed the
  link target to "aiproviders", and turned the "Assistant & AI" text in the
  note below into a real clickable link (settings-module-subviews.tsx).
  Commit 043fec2c5.
- Connected accounts: removed the scopes line and the "Fallback cache..."
  line from each account row, removed "You stay in control..." from the pane
  description, reworded Built-in modules group description to "Modules
  included with core installation." (settings-personal-data-panes.tsx).
  Commit 53ed67d81.
- Email settings: removed the "Sending a reply always asks first... can't be
  turned off" row -- factually wrong, contradicted by the Auto-send replies
  toggle directly above it (packages/email/src/settings/index.tsx).
  Commit f56b504d9.
- Command palette Navigate group: was built from a hardcoded "V1" allowlist
  of module ids, so modules added since (job search, news, food, etc) never
  showed up. Rewrote to use every active module's nav entries (same source
  the sidebar uses), Today always first, Settings always last. Also dropped
  the "Briefings" entry -- it pointed at "/briefings", a route that was
  never registered in the web app, i.e. it was dead (command-palette-model.ts).
  Commit 849384c72.

Filed as GitHub issues (product decisions needed, replied + acknowledged,
left pending -- do not re-fix, do not re-file):
- #1921 Persona preview bubble ignores free-text persona (mt7k8dqk)
- #1922 Theme editor: unify color input format, convert server-side (mt7jwmvf)
- #1923 Job Search: move source settings into a module settings page (mt7kfost)
- #1924 Connected accounts: no way to check sync status (mt7kkexx)
- #1925 Weather chip: more detail on hover -- humidity, dew point (mt7jykx4,
  new-feature half; the "already changing the weather" half was already
  tracked in #1919, linked in the reply)
- mt7k273d (settings gear icon consistency across modules) -- already
  tracked in existing issue #1343, just linked and acknowledged, no new
  issue filed.

Annotations resolved this pass: mt7keubd, mt7keucc, mt7keude, mt7keudf,
mt7keudd-xppjdm (all duplicates of already-fixed items from earlier in
session), mt7kiy6q, mt7kj3xi, mt7kgaxd, mt7kh12b, mt7khk8r, mt7kib5s,
mt7kko69, mt7kktll, mt7kma0q, mt7ke6gx.
Dismissed as exact duplicate resubmissions (same comment+timestamp as a
still-open original, tracked under the original id instead): mt7keudd-i69z0e
(dup of mt7k8dqk), mt7keud1-kepl1t (dup of mt7kd5hv), mt7keudc-8ns7oy (dup
of mt7ke6gx, itself now resolved).

## Done this pass (continuation session)

- **mt7kd5hv** -- Added a light/dark mode switcher to the command palette.
  New "set-color-mode" action kind in command-palette-model.ts, shown only
  when the active theme is built-in (same gating as the settings pane,
  since custom themes have a fixed palette). Wired a mutation in
  command-palette.tsx calling setColorMode, same pattern as the existing
  theme mutation. Commit 53936e706.
- Ben reported separately (not an Agentation annotation): command palette
  Navigate group showed wrong icons for many modules. Root cause: the
  palette and the sidebar each kept their own hand-written icon map, and
  the two had drifted apart, so several modules (job search, weather,
  email, chat, news, sports, food, workshop) showed the wrong icon or the
  fallback. Fixed by moving both to one shared map,
  apps/web/src/shell/nav-icons.ts. Commit 9352c28aa.
- **mt7k0i40** -- Quick-add text was lost when clicking Details. Looked
  correct by reading the code (the title was threaded through properly);
  only reproduced live. Real cause: a second effect in
  task-details-dialog.tsx re-built the form right after it opened and
  dropped the typed title on the floor. Fixed by passing the title into
  that effect too. Verified live that the text now survives. Commit
  7ed68ff63.
- **mt7jzztq** -- Tasks page toolbar right side (filter, then list/matrix
  toggle) left large empty gaps at medium widths. Cause: the layout used a
  single stretchy spacer to push the toggle to the right edge, which only
  worked when everything fit on one line -- once the bar wrapped, the
  spacer's stretch applied to whichever line it landed on instead. Split
  the bar into two independent left/right groups that each wrap on their
  own. Checked visually at several widths from 1280px down to 480px
  (mobile), including the two-row mobile layout below 560px. Commit
  489ce9b7f.

## Still open -- pick these up next

None outstanding from this pass. Re-check `agentation_get_all_pending` at
the start of the next session in case Ben has added more.

## Notes for continuation

- Repo owner for `gh` commands: motioneso/moss.
- All commits this session used explicit file paths per the shared-checkout
  skill (never git add -A); diffed each touched file first with
  `git diff <path>` to confirm only intended lines changed, verified with
  `git show --name-only HEAD` after each commit.
- Before resolving an annotation as "already fixed", confirm against
  `git log --oneline` first -- don't trust memory of what was done.
