# Issue #1872 — relay 4 continuation

Successor: same worktree `1872-fable-plan`, branch `1872-fable-plan`. Plan (do not change):
`docs/superpowers/plans/2026-08-23-service-worker-image-fetch-recovery.md`. Read only by section
if you need it — it is long and already fully absorbed by the relay docs. Prior docs (relay 2 and
relay 3) still have background detail if something below is unclear, but everything you need to
act is in this one.

## The database check is now fully done and clean. Nothing left to verify.

Relay 3 accidentally collided with a leftover background job from relay 2 (both were touching the
same gate database at once — relay 2's Herdr session had already closed by the time this was
caught, so there was no live conflict to resolve, just a corrupted result to redo). That is fully
resolved:
- The stale process was stopped by its exact process ID and confirmed exited.
- A fresh, uniquely named gate database was created, the full check-and-test chain was run once in
  the foreground, watched start to finish, nothing else running against it at the same time.
- Result: everything passed. All repo checks, typecheck, 5168 of 5171 unit tests (3 skipped, as
  expected), the full database migration, 29 of 29 UAT seed tests, and 2044 of 2046 integration
  tests (2 skipped, as expected) — including the exact tests that check different users cannot see
  each other's data, which is the strongest confirmation the earlier failures were the database
  collision and not a real bug.
- The gate database has already been dropped. **You do not need to touch any gate database again**
  for this issue. Do not create a new one and do not re-run `pnpm db:migrate`,
  `pnpm test:uat-seed`, or `pnpm test:integration` — they are done and green.
- `pnpm lint`, `pnpm typecheck` — clean. `pnpm format:check` fails on exactly one file, the
  approved plan document itself — expected, pre-existing, out of scope, do not fix it.
- The coordinator (agent name `coordinator`) has already been told this gate result directly.

## What's actually left — just the paperwork, in order

1. Push the branch (first push for this branch): `git push -u origin 1872-fable-plan`
2. Open the pull request. The body is ready at `/tmp/1872-pr-body.md` — already correct, already
   has the release note and the corrected wording about screenshots not being attachable through
   the command line. Use:
   `gh pr create --base main --head 1872-fable-plan --title "fix(sw): recover from rejected
   uncached GET fetches instead of failing respondWith" --body-file /tmp/1872-pr-body.md`
3. Post the live-path proof as a pull request comment. It's textual only (screenshots exist on
   disk but a command-line tool cannot attach an image to a pull request comment, so say that
   plainly in the comment rather than silently leaving evidence out). The full written evidence,
   plus the underlying files, are described in relay 2's doc under "Live-path proof" — read that
   section for the exact bullet list and file paths
   (`/tmp/1872-live-proof.json`, `/tmp/1872-console.log`, four screenshots in `/tmp`).
4. Once the pull request number exists: `node scripts/append-release-note.mjs --pr <number>` from
   this branch, then commit the resulting change to `docs/WHATS_NEW.md` — use the
   `shared-checkout` skill's explicit-path commit, never a broad `git add`.
5. Invoke `coordinated-wrap-up` to close out formally.
6. Report the pull request link and a short evidence summary to the coordinator (agent name
   `coordinator` — run `herdr agent list` first and confirm exactly one live match before
   messaging), signed with your own pane id.
7. Stop there. Do not merge, move the board, or close the issue — that is the coordinator's job.

## Cleanup

- The `vite preview` (port 4180) and the fixed live proxy (port 4181) processes may still be
  running from relay 2's earlier work — check with
  `ps aux | grep -E "vite preview|1872-live-proxy"` before trusting them, and use them for the
  pull request comment if they're still up. Kill both once the live-path evidence comment is
  posted; they are throwaway, not part of the deliverable.

## Guardrails carried over

- Do not touch the Fable plan file, including its own formatting.
- Keep CSP and chat-stream warnings out of scope.
- No `git add -A`, no broad `git add`; use the `shared-checkout` skill's explicit-path commits.
- Before touching git or the database, confirm with `herdr agent list` / `herdr pane list` that no
  other session is already working this same issue in this same worktree — that exact collision is
  what relay 3 hit and had to clean up. If you see another live session pointed at this worktree,
  stop and check with the coordinator before acting, rather than assuming it is stale.
- Plain English in all chat, status updates, and handoff text — no jargon, no invented shorthand.
  This applies to the PR body and comment too, and to anything you tell the coordinator. Pass this
  rule on to any agent you in turn spawn.
