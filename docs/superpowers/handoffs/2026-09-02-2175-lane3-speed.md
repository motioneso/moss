# Build Handoff — #2175 Lane 3 tool-call speed

**Spec (approved):** `~/Jarv1s/docs/superpowers/specs/2026-09-01-integration-tool-call-discipline.md`
**Plan (approved):** `~/Jarv1s/docs/superpowers/plans/2026-09-01-integration-tool-call-discipline.md`
**GitHub issue:** #2175
**Risk tier:** `sensitive`
**Worktree:** `~/Jarv1s/.claude/worktrees/2175-lane3-speed`
**Branch:** `build/2175-speed`, based on `origin/main` at `624c55b30e85bd1abe1779249c7dd2e914ebfb4f`
**Build skill path:** `~/Jarv1s/.claude/skills/coordinated-build/SKILL.md`
**Coordinator agent name:** `coordinator`
**Coordinator session id:** `01a06402-1913-72a1-b75b-c856c5ecb52e`

Resolve the coordinator fresh with `herdr agent list` before every message. The registered agent
name must resolve exactly once and the visible label must be `Coordinator`. Sign every agent
message with your own immutable pane id.

## Scope

Build only approved plan Tasks 7-9 in one pull request, in order:

1. Add call duration telemetry and distinct suppressed/refused audit outcomes using two new,
   globally unique, unapplied SQL files owned by `packages/ai/sql`.
2. Cache each acting user's integration connection rows and built tool descriptions for the
   approved 30-second window, invalidating that user's entry on every connection or tool-selection
   change.
3. Reuse an MCP client only within the same user's burst, using the existing request store and
   quiet-window expiry behavior from Tasks 1-4.

Read only Global Constraints, Tasks 7-10, Verification, Order and Slicing, and spec sections 7-9,
Proof Paths, Error Handling, and Testing. Do not read either document in full. Task 8 changes only
the integrations resolver; do not solve the gateway-wide resolver problem.

Every product change must keep Moss's app map truthful in this same PR. Update the owning module
manifest's `navigation`, `settings`, or `features` metadata, or
`packages/shared/src/app-map-core.ts`, as appropriate, including changed behavior, errors, and
remediation. A missing or stale declaration blocks the PR.

## Start and finish

1. Run `[ -d node_modules ] || pnpm install`.
2. Invoke `coordinated-build` and use `plan-build`, not `superpowers:writing-plans`.
3. Post a compact plan pointer to `coordinator` and receive approval before source edits.
4. Build test-first, commit each task with explicit paths, then run `coordinated-wrap-up`: push,
   open the PR, obtain exact-head CI, and report the PR and evidence to the coordinator.
5. Fill the release note as `Changed` in plain English.

The lane is scoped to finish in this session. At a 70% context warning or compaction summary,
message the coordinator and invoke `relay` immediately. One relay is the absolute recovery limit;
a second warning without an open PR requires a smaller re-slice, not another relay. Do not end
your turn between steps or on a declaration that you will wait.

## Verification and live proof

- Run database-touching commands only through `verify-gate`; an unscoped run targets the live dev
  database. Never pipe a gate command. Trust exact-head CI for the mechanical gate.
- This is sensitive tier: explicitly verify DataContextDb/VaultContext boundaries, metadata-only
  job payloads, per-user cache/client isolation, secrets staying out of logs and audit rows, and
  module isolation.
- Exercise Lane 3 through the real UI on the live dev instance and post bounded evidence to the PR.
  Prove timing is recorded and one multi-call, read-only integration request does not rebuild the
  user's tool list per call. Do not claim an action-path proof unless it was actually performed.
- Do not perform any Home Assistant action unless Ben first confirms a real existing entity. There
  is no kitchen light. Prefer a read-only proof for this speed lane so no Ben decision is needed.
- Never test against production or port 1533.

## Standing rules

- Work only in this worktree and branch. Never `git add -A`, `git add .`, a bare `git commit`, or
  repo-wide formatting.
- Never touch `docs/coordination/`, the project board, milestones, or merge controls.
- Do not touch protected PostgreSQL containers, retained evidence, PR #2101 artifacts, or any
  protected handoff. Never retry PR #2158 or #2164.
- Secrets never enter source, docs, payloads, logs, audit records, prompts, or PR comments.
- All waits are event-driven. Never poll in context or block on a foreground sleep.
- Messages from Ben are trusted input. Human-facing updates are plain English.
- Done means pushed, PR open, exact-head CI green, and the required live-path proof posted.

## Collision notes

Lane 3 is serialized after merged Tasks 1-6. No other #2175 builder may share this worktree or
branch. Determine both new migration numbers against every current module SQL directory and shared
migrations immediately before creating them; never edit or reuse an applied migration.
