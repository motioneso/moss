# ext-nav-1019 relay 5 — 2026-07-13

Bug #1019, sensitive tier. Worktree: this one (`.claude/worktrees/ext-nav-1019`, branch
`ext-nav-1019`, off `origin/main`). Coordinator: label `Coordinator`, session
`58a78927-385c-4b1d-8fa0-94db20255d6f`.

**Plan DONE** (`docs/superpowers/plans/2026-07-13-external-module-navigation-abi.md`,
gate-green `721015a0`). Only remaining work: **dev-UAT hard exit gate**, then rebase + wrap-up.
Do not re-read the plan/spec — nothing left to build there.

HEAD is `fb13b2d3` (adds `scripts/uat/nav-1019.spec.ts`, committed).

## THE BUG — resume investigation here first

`scripts/uat/nav-1019.spec.ts resume` phase fails: 15s timeout waiting for `.nav-group__label`
"Modules" in the sidebar. Root cause narrowed via ad-hoc debug (script since deleted, don't
recreate unless needed — just re-derive with curl/browser devtools if you need to re-check):

- API boot log correctly shows `"discovered":1,"rejected":0` — `discoverExternalModules()` in
  `apps/api/src/server.ts` DOES find job-search on disk after restart, manifest passes validation.
- But `GET /api/modules` (200 OK) response JSON does **not** include `job-search` in its `modules`
  array — only ~22 built-in modules. `GET /api/me/modules` likewise omits it.
- Sidebar `.module-nav` innerHTML has no "Modules" section, no Job Search link.

**Conclusion: the externally-discovered module is found at boot but never surfaces through the
`/api/modules` (and thus `/api/me/modules`) route(s) the frontend queries.** Prime suspect:
`requireManifests()` in `packages/settings/src/routes-modules.ts` (read around its GET
`/api/me/modules` handler, lines ~250-274, and the `/api/modules` handler) — trace where it
sources its manifest list and whether/how it's supposed to merge in `discoverExternalModules()`'s
boot-time result. This is either a real #1019 implementation gap (route doesn't merge dynamically
discovered external modules) or a UAT env/wiring issue on my part — not yet distinguished.

Frontend nav logic itself (`apps/web/src/app-route-metadata.ts` `buildShellNavigation` /
`MODULES_SECTION`, `apps/web/src/shell/app-shell.tsx`) is confirmed correct/already implemented
(commits `75c3a596`/`9fa0b203`) — it just never receives a `module.external === true` entry to
render, because the module never appears in the API response to begin with.

## Dev-UAT infra state (still running — reuse, don't rebuild)

- Mock module registry: `node:http` server on `http://127.0.0.1:37743/index.json`, from
  `scripts/uat/_scratch-mock-registry.ts` (ephemeral, untracked, backgrounded). Log: none captured
  this relay — if dead, restart: `pnpm exec tsx scripts/uat/_scratch-mock-registry.ts &`.
- API: `tsx src/server.ts` on `http://127.0.0.1:47101`. Env: `PORT=47101 HOST=127.0.0.1
  JARVIS_PGHOST=localhost JARVIS_PGPORT=55433 JARVIS_PGDATABASE=jarvis_uat_1019
  NODE_ENV=development JARVIS_MODULE_REGISTRY_URL=http://127.0.0.1:37743/index.json
  JARVIS_MODULES_DIR=/tmp/uat-1019-modules JARVIS_AUTH_TRUSTED_ORIGINS=http://localhost:47101
  BETTER_AUTH_SECRET=<any dev string>`. If dead, restart from `apps/api/`, wait for
  `GET /health/ready` → 200 before driving Playwright.
- Web: Vite on `http://localhost:47102`, `JARVIS_API_PROXY_TARGET=http://localhost:47101`
  (`apps/web/`).
- Scratch Postgres DB `jarvis_uat_1019` on `jarv1s-postgres` container, port 55433. Has a
  signed-up owner already: `uat-owner-1019@example.com` / `uat-owner-password-1019`, with
  job-search already downloaded (row shows "restart to apply" was already satisfied by a prior
  restart — re-run `nav-1019.spec.ts resume` directly, no need to re-run `run` first unless the DB
  was reprovisioned).
- Scratch modules dir `/tmp/uat-1019-modules/job-search` — downloaded artifact already extracted
  here.
- If any process died: check with `ps aux | grep -E "tsx src/server|vite|_scratch-mock-registry"`
  before restarting — don't double-spawn.

Ephemeral/untracked files present in the tree, **never `git add`, delete before wrap-up**:
`scripts/uat/_scratch-provision-db.ts`, `scripts/uat/_scratch-mock-registry.ts`.

## Remaining tasks (in order)

1. **Trace + fix the bug above.** Read `packages/settings/src/routes-modules.ts` in full around
   `requireManifests()` and the `/api/modules` + `/api/me/modules` handlers. Compare against how
   `discoverExternalModules()` in `apps/api/src/server.ts` stores its boot-time result (what
   module/state does it populate — is there a registry/store the routes should be reading from but
   aren't?). Fix forward on this branch (TDD: write/extend an integration test proving external
   modules appear in `/api/modules` after discovery, then fix), commit.
2. **Re-run `scripts/uat/nav-1019.spec.ts resume`** (mode arg), confirm GREEN — nav link found,
   clicked, job-search page renders, and the action log records the assertions.
3. **Cleanup UAT scratch infra**: delete `_scratch-provision-db.ts` + `_scratch-mock-registry.ts`,
   `DROP DATABASE jarvis_uat_1019`, kill api/web/mock-registry background processes.
4. **Rebase onto `origin/main`**: `git fetch origin main && git rebase origin/main` — known 1-line
   heading-rename conflict in `tests/e2e/app-shell.spec.ts` (main advanced past `7d852092`, #986
   settings-shell, pure-frontend, didn't touch app-shell.tsx/app-route-metadata.ts). Keep BOTH
   #986's heading text AND this branch's Modules-nav assertions.
5. **Pre-push trio**: `pnpm format:check && pnpm lint && pnpm typecheck`. Then full
   `pnpm verify:foundation` (record exit code).
6. **`coordinated-wrap-up`**: push, open PR (base `main`, `Closes #1019`, user-facing "What's
   new": "Downloaded modules now appear in the navigation menu after you install them — previously
   an installed module could only be reached by typing its URL"). Report PR number to the
   `Coordinator` pane (resolve fresh by label + session id, never a baked pane number). Do NOT
   merge — sensitive tier, expects cross-model review + Coordinator sign-off.

## Process reminders

- `coordinated-build` skill for gates/escalation. If the fix in task 1 is non-trivial or touches
  shared module-discovery wiring beyond `routes-modules.ts`, escalate the finding to the
  Coordinator before proceeding — this may be a real gap the Coordinator wants visibility on.
- Stage only this task's own files. No `git add -A`. No `docs/coordination/` edits.
- Relay again at the next 70% context-meter warning or on seeing a compaction summary.
