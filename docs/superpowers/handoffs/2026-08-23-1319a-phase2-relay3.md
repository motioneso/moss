# Relay 3 — #1319-A phase 2 build (catalog-verify lane)

**Status:** Task A done (committed earlier at `f786077a3`). Task B mostly written but
**not yet tested or typechecked** — committed as WIP at `f118ece3b` so it survives the
relay (previous file-revert incident, see relay-2 doc). Resume via `coordinated-build`
step 2 (Build), finishing Task B.

**Plan:** `docs/superpowers/plans/2026-08-23-1319a-phase2-catalog-verify.md`
**Master plan (Task 4 has the full spec):**
`docs/superpowers/plans/2026-08-18-1319-signed-module-catalog.md` (search "Task 4")
**Prior relay docs (read only if you need more background — this doc should be enough):**
`docs/superpowers/handoffs/2026-08-23-1319a-phase2-relay2.md`

**Branch/worktree:** `build/1319a-catalog-verify`, this worktree. `node_modules` already
installed — skip `pnpm install`.

**Coordinator:** registered name `coordinator`. Re-resolve fresh via `herdr agent list`
before messaging — never reuse a pane id from this doc.

## What's committed (`f118ece3b`, WIP — build on it, don't revert it)

- `packages/settings/src/routes-external-module-types.ts`: new `RegistryEntriesSnapshot`
  interface (`entries`, `catalogVerification`, `catalogDigestSha256`);
  `ModuleDistributionDependencies.fetchRegistryEntries` returns
  `Promise<RegistryEntriesSnapshot>`.
- `packages/settings/src/routes.ts`: re-exports `RegistryEntriesSnapshot`.
- `apps/api/src/module-distribution-port.ts`: cache now holds one `RegistryEntriesSnapshot`
  written atomically from one `fetchRegistryIndex` call; `"unavailable"` never cached
  (early-return before touching `registryCache`).
- `packages/settings/src/routes-module-registry.ts`: GET route (both the `enabled:false`
  early return and the main path) and all three post-mutation refetch call sites
  (download/remove/purge-cancel) consume the snapshot.
  `registryUnavailable === (catalogVerification === "unavailable")` on the `enabled:true`
  path; `enabled:false` sends `catalogVerification: "unavailable"`,
  `catalogDigestSha256: null`, `registryUnavailable: false`.
- `packages/shared/src/platform-api-modules.ts`: `GetModuleRegistryResponse` +=
  `catalogVerification`, `catalogDigestSha256`; `getModuleRegistryRouteSchema`'s 200
  response schema has both in `properties` AND `required`.
- Fixtures updated: `tests/e2e/settings-modules.spec.ts`,
  `tests/unit/settings-instance-modules-pane-render.test.tsx` (all 3
  `satisfies GetModuleRegistryResponse` fixtures — added `catalogVerification: "verified"`,
  `catalogDigestSha256: "a".repeat(64)`).
- `tests/unit/instance-modules-dedup.test.tsx` — checked, does NOT need updating. It builds
  `ModuleRegistryRowDto` rows directly, never a `GetModuleRegistryResponse` envelope. The
  relay-2 doc's "update anyway" note was wrong; skip it.

## What's NOT done — pick up here

1. **`tests/integration/module-distribution.e2e.test.ts` D6 harness signing — incomplete.**
   Already added (committed): `generateKeyPairSync` import, `signCatalogBytes` import from
   `@moss/module-registry/node`, module-scope `catalogTestKeypair`, `CATALOG_TEST_KEY_ID =
   "test"`, and a `registryIndexRequestCount` counter variable (declared, not yet
   incremented anywhere).
   Still needed, in the `registry = createServer((req, res) => { ... })` handler
   (currently around line ~160 post-edit — re-grep, don't trust this number) inside the
   `if (req.url === "/index.json")` branch:
   - increment `registryIndexRequestCount` at the top of that branch.
   - after computing `index`, serialize it to bytes ONCE
     (`const indexBytes = Buffer.from(JSON.stringify(index), "utf8")`), send those exact
     bytes as the response body (`res.end(indexBytes)`), and reuse `indexBytes` for signing
     — the signature must cover the exact served bytes.
   - add a new `if (req.url === "/index.json.sig")` branch: build
     `signCatalogBytes(indexBytesForSig, catalogTestKeypair.privateKey, CATALOG_TEST_KEY_ID)`
     and serve it as JSON. Since the index route is built per-request (mutable
     `latestVersion`), cache the freshest index bytes into a module-scope variable when
     `/index.json` is served, then sign THAT when `/index.json.sig` is requested
     immediately after — the pipeline always fetches `.sig` right after `index.json`, so a
     single "last served bytes" variable is sufficient (no need for real concurrency
     safety in this test-only mock).
   - in `beforeAll`, alongside the existing
     `process.env.JARVIS_MODULE_REGISTRY_URL = ...` line, add
     `process.env.MOSS_MODULE_CATALOG_TEST_PUBLIC_KEY = catalogTestKeypair.publicKey;`
   - in `afterAll`, alongside the existing `delete process.env.JARVIS_MODULE_REGISTRY_URL`,
     add `delete process.env.MOSS_MODULE_CATALOG_TEST_PUBLIC_KEY;`
   - Reasoning for why this makes the pipeline verify: `resolveCatalogTrustedKeys` (already
     shipped, `packages/module-registry/src/distribution/catalog-signing.ts`) adds a
     `{ keyId: "test", publicKeyPem: ... }` entry to the trusted keyring whenever
     `JARVIS_MODULE_REGISTRY_URL` override is active AND
     `MOSS_MODULE_CATALOG_TEST_PUBLIC_KEY` is set — exactly this suite's existing setup,
     plus the one new env var.
   - Existing assertion at (re-grep) `expect(body.registryUnavailable).toBe(false);` in the
     `"lists the registry module as not-installed with full capabilities"` test: also add
     `expect(body.catalogVerification).toBe("verified");` and
     `expect(typeof body.catalogDigestSha256).toBe("string");` right after it.

2. **New route-level tests — add to `tests/integration/module-distribution.e2e.test.ts`**
   (confirmed correct home this session and last — `tests/integration/module-registry.test.ts`
   does NOT test the HTTP route, only `assertModuleRegistryConsistency` /
   `getExternalModuleDeletionTables`; don't put tests there):
   - `enabled:false` envelope: needs a server/dependencies configuration where
     `externalModules.enabled` is false or `moduleDistribution` is undefined — check how
     `createApiServer`/`apiServerConfig` toggles that elsewhere in this test file or in
     `tests/integration/external-modules-routes.test.ts` before inventing a new server
     config; assert `catalogVerification: "unavailable"`, `catalogDigestSha256: null`,
     `registryUnavailable: false`.
   - schema-strip check: assert the HTTP JSON body actually contains
     `catalogVerification`/`catalogDigestSha256` (i.e. they survived
     fast-json-stringify) — the existing "lists the registry module" test's added
     assertions from step 1 cover this once they're in.
   - non-admin-before-fetch ordering: extend the existing
     `"denies non-admin access to every registry route"` test — record
     `registryIndexRequestCount` before the loop and assert it is unchanged after (the
     mock registry must never see a hit from a non-admin request). This proves
     `assertAdminUser` runs before `fetchRegistryEntries` without needing a spy.
   - cache behavior (atomic snapshot; unavailable never cached) — the master-plan spec asks
     for this, but it needs sub-TTL-window control that this DB-backed e2e file can't give
     cheaply (10-minute real TTL). **Recommend instead:** a small new unit test file
     `tests/unit/module-distribution-port.test.ts` exercising
     `createModuleDistributionPort` directly with a fake `fetchFn` (same `fakeFetch`
     pattern as `tests/unit/module-distribution-pipeline.test.ts`), a fake `server.log`,
     and a minimal `apiServerConfig`/`options` — assert: (a) two successful fetches within
     `refresh:false` return the identical snapshot object (cache hit, fetchFn called once);
     (b) `refresh:true` bypasses the cache; (c) a fetch that returns a non-ok index does
     NOT overwrite a previously-cached good snapshot on the next `refresh:false` call
     (this requires calling with `refresh:true` to force the failing fetch, then
     `refresh:false` again to prove the old cache is still being served — reason through
     the exact sequence before writing it, the TTL clock is real `Date.now()` so keep the
     test fast and don't sleep). If this genuinely doesn't fit as a unit test either, that
     is a real plan gap — flag it to the coordinator rather than skip the coverage.

3. **Verify commands (adjust to match whichever files actually end up holding the new
   tests):**
   `pnpm typecheck` → exit 0 (run this FIRST, cheap, will catch anything the WIP commit
   missed).
   `pnpm test:unit tests/unit/module-distribution-port.test.ts` → exit 0 (if step 2's
   recommendation is taken).
   `pnpm test:integration tests/integration/module-distribution.e2e.test.ts` → exit 0 —
   this is also the Phase-2 e2e finish line (proves the verified listing end-to-end); no
   separate second run needed once the D6 signing lands.

4. Commit Task B the moment all of the above is green — don't batch further.

## Next concrete steps after Task B is green

1. Pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`) + `git fetch origin
   main && git rebase origin/main`.
2. `coordinated-wrap-up`: full gate via the `verify-gate` skill (isolated DB, never
   unscoped), push, open a non-draft PR, report exact-head evidence to the coordinator.
   Backend/API-only change (no UI in this phase) — confirm with the coordinator whether
   live-path UAT proof is still expected for this lane or deferred to Phase 3 before
   claiming done.
3. Do not merge, move the board, or touch coordination files — report to the coordinator
   and stop.

## Preservation constraints (still binding, from the coordinator's original approval)

- fixed verification enums exactly as specified in master-plan Task 3 (already shipped)
- raw-byte digest/signature contract (already shipped)
- atomic snapshot invariant — one `fetchRegistryIndex` call produces entries + verification
  + digest together; cache never mixes fields from different fetches; `"unavailable"` never
  cached (this session's code change; still needs the test coverage above)
- non-admin-before-fetch ordering — `assertAdminUser` runs before `fetchRegistryEntries` is
  ever called (test this explicitly — see step 2 above)
- hard boundary — do NOT touch pipeline enforcement/409/override (Phase 3), ensure-at-boot
  (Phase 3 Task 6), web UI (Phase 3 Task 7), UAT (Phase 4), or any migration. Phase 1
  (catalog-signing.ts) is merged to main (PR #1684) — read-only for this lane.

## Reminders

Security tier — build defensively, no crypto/library error detail in product-facing
messages or logs. Never `git add -A`/bare-commit in this shared-checkout worktree — use the
`shared-checkout` skill's explicit-path procedure. This session's WIP commit
(`f118ece3b`) is deliberately not-green; verify everything before treating any of it as
done. Resolve `coordinator` fresh via `herdr agent list` before messaging (confirm exactly
one live match) — never reuse a pane number from this doc. Sign every message to the
coordinator with your own pane id. Write in plain English to the coordinator and in any
spawn prompts — no invented jargon, name only what Ben must act on.
