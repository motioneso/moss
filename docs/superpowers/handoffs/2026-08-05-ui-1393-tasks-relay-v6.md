# #1393 Tasks+notifications UI consolidation — relay v6

Continuation of relay v5. **Read v4 IN FULL**
(`docs/superpowers/handoffs/2026-08-05-ui-1393-tasks-relay-v4.md`) — it has the exhaustive
per-selector property list for Step 2 (lines 58-211) and the rest of Steps 1-6. This doc only
says what's done and what's next; do not re-derive the triage.

Worktree: `/home/ben/Jarv1s/.claude/worktrees/ui-1393-tasks`, branch `feat/1393-ui-tasks`.
Sole writer.

## Done

- **Step 1: fully committed**, all three app files. `6a84921e` = kit-tasks.css. `9f58ef9a` =
  kit-tasks-modal.css + tasks.css (255 deletions, verified clean via full `git diff` before
  committing — pure deletion of the v4-spec dead selectors, nothing foreign, nothing else
  touched). Working tree is clean except this doc and one untracked file (next item).
- **Step 2, partial: `packages/ui/src/styles/components-tasks.css` created** (untracked, not
  yet committed) — full content already written, 12369 bytes. Header comment credits
  kit-tasks.css/kit-tasks-modal.css/tasks.css as sources and #1393 as the issue. Three
  source-order-preserving sections (`/* ---- from kit-tasks.css ---- */` etc.) containing every
  banned-property declaration for every live selector, unrenamed, cross-checked selector-by-
  selector against actual file content (not just the v4 annotations — two doc/source mismatches
  were caught and resolved by ground truth, see below). **Read this file before trusting it** —
  it was written in the prior session and not yet independently re-verified in this one.

  Two corrections vs the literal v4 doc text (apply these when editing the app files, not what
  v4's per-selector annotation literally says):
  - `.tk-modal__eyebrow` (in kit-tasks-modal.css): v4 doc lists only font-family/color as banned,
    but source also has `font-size: 11px` (banned, exact-match list). New file has all three
    (`font-family, font-size, color`); when editing kit-tasks-modal.css, the app file must keep
    only `letter-spacing`/`text-transform`.
  - `.tk-foot .ic` (in kit-tasks.css): v4 doc says "whole rule" moves, but source has
    `display: inline-flex` (NOT banned) alongside `color` (banned). New file gets only
    `color: var(--text-faint);`; when editing kit-tasks.css, KEEP the rule with
    `display: inline-flex;` — do not delete it entirely.

## Next: finish Step 2 (not started)

1. Re-read `packages/ui/src/styles/components-tasks.css` fresh and spot-check a handful of
   entries against the v4 selector table and current app-file content before trusting it.
2. Edit all three app files (`apps/web/src/styles/kit-tasks.css`,
   `apps/web/src/styles/kit-tasks-modal.css`, `apps/web/src/tasks/tasks.css`) — strip out
   exactly the properties now living in components-tasks.css per the v4 table (lines 58-211),
   preserving the two corrections above. If a rule's properties are ALL banned, delete the whole
   rule; otherwise keep the rule with only the non-banned properties remaining.
3. Add `@import "./styles/components-tasks.css";` to `packages/ui/src/styles.css`, after the
   `components-empty.css` line (currently the 11th and last import).
4. Commit Step 2 (new CSS file + 3 app-file edits + import line) as its own commit, explicit
   paths only.

## Then: Steps 3-6 (not started) — follow v4 verbatim

3. Grep all three app files for the banned-property regex — zero matches required.
4. Diff `apps/web/src/styles.css` against BASE `5ff2423d`, delete dead selectors found there.
   Keep `.empty-state`, `.notification-link`, `.notification-badge`, `page-stack`/`page-heading`/
   `eyebrow`. **Kill-gate, not judgment call:** do NOT delete `.notification-row.unread` until the
   `10-notifications` capture proves `.jds-task--unread` renders correctly — if it doesn't, STOP
   and report to coordinator.
5. Register the 3 new/migrated CSS files in `scripts/check-design-tokens.ts`'s
   `MIGRATED_SECTION_CSS_FILES` (11→14, excluding root styles.css). Register 7 TSX paths (6 in
   `apps/web/src/tasks/` + `apps/web/src/notifications/notifications-page.tsx`) in
   `scripts/check-migrated-sections.ts`'s `MIGRATED_SECTION_PATHS` (49→56).
6. Gates unpiped, in order: `check:design-tokens`, `check:migrated-sections`, `check:ui-classes`,
   `build:ui-catalogue` then `check:ui-catalogue`, `verify:foundation` (fresh exported gate DB,
   never live dev DB — see CLAUDE.md gate-db-isolation rule). Run focused browser assertions; confirm changes
   confined to the 4 approved amendments from sign-off `5188097889` (kill-gate on anything else,
   including any lost unread/read styling). Live-path proof on a dev instance: tasks list+matrix,
   details dialog, edit date, tag menu, change status; notifications filter/mark-read/both empty
   states — record on the PR. Post the required note on issue #1405 (notifications adopted raw
   `jds-task`, family gained authored `--unread` modifier). Then `git fetch origin main && git
   merge origin/main`, pre-push trio (`format:check && lint && typecheck`), push, open PR against
   `main` with release-note summary + live-path proof + #1405 note. **Do not merge** — coordinator
   reviews/merges. Report PR number + measured guard numbers (expect 14 and 56) to coordinator
   pane "DESIGN ELEMENTS" (re-resolve by label + session id via `herdr pane list`, never a baked
   pane number).

## Git safety (shared checkout, per CLAUDE.md)

Never `git add -A`/`.`, never bare `git commit` — explicit paths only. Before committing a shared
file, `git diff <path>` and read it; after, `git show --name-only HEAD` to confirm the file list
is what you meant. Never `git checkout --` in this worktree.

## Chain straight through

Plan is already approved (issue #1393 comments `5187960649`, `5188097889`). Execute the rest of
Step 2 and Steps 3-6 without stopping for approval. Relay again immediately on the meter's 70%
warning or a compaction summary — do not push past it.
