# Handoff — build lane: surface the briefing prose on Today (#1372, parent #1327)

**Coordinator:** label `Coordinator`, Claude session `43e5f5e2-0deb-4ab5-9237-436e8795b611`.
Re-resolve the pane fresh via `herdr pane list` before messaging — never trust a written pane number.

**Worktree:** `~/Jarv1s/.claude/worktrees/build-1327-prose` (you are in it), branch
`build/1327-prose`, based on `origin/main` at `d8bf5e3b`.
**Issue:** #1372. **Spec:** `docs/superpowers/specs/2026-07-29-1327-briefing-action-rows.md`.
**Risk tier:** `routine` — UI only, no schema, no contract change, no new API field.

## Step 1

`pnpm install`. Then read **§8 "Briefing prose surfaces"** and **§9 Task 5** of the spec. Read those
sections only — do not read the spec in full; it covers six other tasks that are not yours and
full-reads bloat a fresh context.

## Your scope — exactly Task 5, nothing else

The briefing prose is composed, persisted and served today, and is almost never rendered. Close that
render gap. **Do not** touch briefing composition, storage, the API contract, extraction,
suppression, or the action rows — those are separate lanes already in flight on other branches.

Files you own: `apps/web/src/today/today-page.tsx`, `apps/web/src/today/evening-mode.tsx`, and
existing tokenized Today CSS. Nothing else.

The spec's §8 is authoritative on placement, the truncation ruling, and the empty/loading/stale
states. Two things it flags that are real work, not tweaks:

- `today-page.tsx` currently queries **evening** runs only (`eveningRunsQuery`). You must add the
  morning definition + run resolution using the existing briefing API — no new endpoint.
- Morning and primary-evening prose are **never truncated**. `compactSummary()`'s 220-character cut
  survives only on the small day-mode evening tile.

## Constraints that are not negotiable

- **Design system:** extend existing `jds-brief__*`, `agenda-clear` and authored loading/empty
  patterns. No new raw colours outside `apps/web/src/styles/tokens.css`. No mono, no serif.
- **Never `git add -A` / `git add .`** — stage explicit paths. **Never** run repo-wide
  `pnpm format`; use `npx prettier --write <single file>`.
- **Do not touch `docs/coordination/`** — coordinator-only.
- **`.tsx` test files are not typechecked** (#1335). Your two new test files are `.tsx`, so fixture
  drift will not be caught by `pnpm typecheck`. Assert against real rendered output, not against a
  hand-built fixture shape you assume matches `BriefingRunDto`.
- Commit per task. Reading is not progress — build.

## Definition of done

1. `pnpm verify:foundation` green locally. **Export an isolated `JARVIS_PGDATABASE`** (freshly
   DROP/CREATEd) before any test or DB command — never inline it, never share a gate DB.
2. The two tests named in §9 Task 5 exist and pass.
3. **Live proof on a real dev instance, recorded on the PR.** This is user-facing, so a live assertion
   or a described walkthrough of the morning prose card and the evening recap is the gate. Without
   it the status is "code-complete, unverified", not done.
4. PR opened against `main` (`Closes #1372`), with a short user-facing summary in release-note
   language. Follow the `coordinated-wrap-up` skill, then report to the `Coordinator` pane.

## Escalation

Tag `[DESIGN-FORK]` / `[SECURITY]` / `[CRIT]` and stop rather than guessing. Message via
`herdr pane run <coordinator-pane> "<msg>"` after resolving the pane by label.
