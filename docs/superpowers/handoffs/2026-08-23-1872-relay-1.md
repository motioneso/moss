# Issue #1872 — relay 1 continuation

Successor: pick up in worktree `1872-fable-plan`, branch `1872-fable-plan`. Plan:
`docs/superpowers/plans/2026-08-23-service-worker-image-fetch-recovery.md` (Fable-authored,
approved — do not change it, including its own prettier formatting warning, see below).
Handoff that started this lane: `docs/superpowers/handoffs/2026-08-23-1872-fable-plan-build.md`.

## Done and committed (Tasks 1-2-3)

- `7de285b04` — the fix itself: `apps/web/public/service-worker.js`, new
  `fetchWithRecovery()` + `IMAGE_RETRY_DELAYS_MS`, plus
  `tests/unit/service-worker-fetch.test.ts` (5 cases, all green). Verified red against the
  pre-fix file (3 of 5 cases failed as predicted) before committing.
- `2d1ce52a0` — `playwright.sw.config.ts` (new, port 4174) +
  `tests/sw/service-worker-image-recovery.spec.ts`: loads the production build through a
  registered Service Worker, points an `<img>` at a helper server that kills the socket on the
  first request and serves a real PNG after, asserts the image loads with no page reload and no
  `respondWith`/`FetchEvent network error` console text. Verified red against the pre-fix SW
  (temporarily swapped in via `git show HEAD~1:...`, restored after), green against the fix.
- `5e0be815e` — lint/format cleanup (unused catch binding, prettier on the test file).

Run individually to re-confirm if needed:
```
pnpm test:unit tests/unit/service-worker-fetch.test.ts   # EXIT=0
pnpm exec playwright test --config playwright.sw.config.ts   # EXIT=0
```

## Gate status

- `pnpm format:check` / `pnpm lint` / `pnpm typecheck` (the pre-push trio): lint and typecheck
  clean. format:check fails on exactly one file —
  `docs/superpowers/plans/2026-08-23-service-worker-image-fetch-recovery.md` — which is Fable's
  own plan doc, committed before this lane started (`9373e271a`), not touched by this branch. The
  plan guardrail says don't change the plan; treat this as a pre-existing, out-of-scope failure,
  not something to fix here. Say so plainly in the PR rather than silently reformatting Fable's doc.
- `pnpm test:e2e`: 98 passed, 1 failed — `tests/e2e/briefing-action-rows.spec.ts` — but it passes
  cleanly when run alone (`pnpm exec playwright test tests/e2e/briefing-action-rows.spec.ts`),
  confirmed a pre-existing flake unrelated to this change (that file has no service-worker/image
  content and wasn't touched by this branch).
- `pnpm verify:foundation`: only reachable step-by-step because of the same format:check
  blocker (the full chain uses `&&`). Ran the remaining steps individually against a scoped gate
  database — **check the result before doing anything else**:
  - Log: `/tmp/1872-vf-rest.log` (a `### FINAL rc=N` line at the end tells you pass/fail).
  - Gate DB: `jarvis_gate_1872` — was exported as `JARVIS_PGDATABASE` for that run. **Drop it when
    you're done** (`docker exec jarv1s-postgres psql -U postgres -c "DROP DATABASE
    jarvis_gate_1872;"`) — don't reuse it for anything else without dropping and recreating it
    first (per the verify-gate skill).
  - If `/tmp/1872-vf-rest.log` doesn't show `FINAL rc=0`, read it, fix, and re-run following the
    verify-gate skill's scoping rules (never reuse a stale gate DB, never pipe the command).

## Live-path proof — set up, not yet done

A deployed-style instance is already running so you can do the live-path proof without rebuilding:

- Web app built (`pnpm --filter @moss/web build`) and served via `vite preview` on
  `127.0.0.1:4180`.
- A small proxy (`node /tmp/1872-live-proxy.mjs`, source still on disk there) is listening on
  `0.0.0.0:4181` — routes `/api/*` and `/health` to the real dev API on `:3000` (already running
  from source, unrelated to this branch), everything else to the preview server. Reachable at
  `http://192.168.50.36:4181` from the LAN.
- Confirmed reachable: `/` returns 200, `/service-worker.js` returns 200.
- **Not yet done**: the actual Task 4 walk-through — open that URL in a real browser, sign in,
  confirm DevTools → Application → Service Workers shows the new SW controlling the page, show one
  article photo (Today/News) and one sports logo (Sports) rendering with no
  `respondWith`/network-error console text during normal navigation, then DevTools offline mode +
  navigate to confirm the offline app-shell page still appears. Record it and post it as a
  `gh pr comment` on the PR (Task 4 in the plan). If jarvis.motioneso.com's TLS cert (expired,
  confirmed separately, unrelated to this issue) gets fixed before you get here, that's the
  preferred deployed target per the plan — but the local proxy setup above is an equally valid
  "equivalently built+served dev instance" per the plan's own fallback language, so don't block on
  the cert.
- If either the preview (4180) or proxy (4181) process died, restart them:
  ```
  cd /home/ben/Jarv1s/.claude/worktrees/1872-fable-plan
  nohup pnpm --filter @moss/web exec vite preview --host 0.0.0.0 --port 4180 > /tmp/1872-preview.log 2>&1 &
  disown
  nohup node /tmp/1872-live-proxy.mjs > /tmp/1872-live-proxy.log 2>&1 &
  disown
  ```
  (recreate `/tmp/1872-live-proxy.mjs` from this doc's description if it's gone — it's a ~25-line
  plain `node:http` reverse proxy, `/api/` and `/health` → `127.0.0.1:3000`, everything else →
  `127.0.0.1:4180`.)

## Still to do (in order)

1. Confirm/fix `verify:foundation` remainder (see Gate status above), drop the gate DB after.
2. Do the Task 4 live-path proof on `http://192.168.50.36:4181`, capture evidence.
3. Task 5 release note: Category **Fixed**, title like "Photos and logos recover on their own",
   one plain-English sentence. Then `node scripts/append-release-note.mjs --pr <number>` from the
   branch once the PR number exists, commit `docs/WHATS_NEW.md` onto this branch.
4. `coordinated-wrap-up`: clean tree, push (pre-push trio again first), open PR, post the Task 4
   evidence as a `gh pr comment`, report PR + evidence to `coordinator` (agent name `coordinator`,
   confirm exactly one live match in `herdr agent list` before messaging), signed with your pane id.
5. Stop — merge/board/close are the coordinator's, not yours.

## Guardrails carried over

- Do not touch the Fable plan file, including its own formatting.
- Keep CSP and chat-stream warnings out of scope.
- Do not touch `docs/coordination/`, no broad `git add`, no repo-wide formatting.
- Use `shared-checkout` skill before any commit; explicit paths only, no `git add -A`.
- Plain English in all chat/status/handoff text — no jargon, no invented shorthand (box-wide
  CLAUDE.md rule, reaffirmed 2026-08-18).
