# Relay 1 continuation — #2175 Lane 3 (speed), Tasks 7-9

No code written yet. This is research grounding only. Original handoff (still authoritative for
scope, standing rules, and process): `docs/superpowers/handoffs/2026-09-02-2175-lane3-speed.md`.
Read that file in full before anything else. Coordinator agent name: `coordinator` (resolve fresh
with `herdr agent list`, confirm exactly one match, before messaging).

Do not re-read the full spec or plan. Everything needed from them is grounded below. If a detail
is missing, read only the specific named section (spec sections 7-9, Proof Paths, Error Handling,
Testing; plan Global Constraints, Tasks 7-10, Verification, Order and Slicing — same as the
original handoff named).

## Confirmed against the branch (do not re-verify)

- Audit table is `app.moss_action_audit_log` (renamed from `jarvis_action_audit_log` by
  `packages/ai/sql/0183_moss_rename_ai.sql`). Columns today: id, owner_user_id, tool_module_id,
  tool_name, action_family_id, action_kind, approval_mode, outcome, error_class, request_id,
  chat_session_id, source_surface, input_summary, occurred_at. **No duration column yet.**
- Outcome check constraint currently allows: success, failed, denied, cancelled, invalid,
  conflict (widened once already by `packages/ai/sql/0177_audit_outcome_widen.sql`).
- Highest SQL version number across every module `sql/` dir and `infra/postgres/migrations` is
  **0209** (`packages/integrations/sql/0209_integration_group_derivation_grandfather.sql`, from
  the already-merged Task 5). **Next two free, globally unique numbers: 0210 and 0211.** Put both
  new files in `packages/ai/sql/` per the plan (AI package owns this table).
- The write path is `AiRepository.insertActionAuditLog` (`packages/ai/src/repository.ts:1982`),
  called from `AiGateway.recordAuditRaw` (`packages/ai/src/gateway/gateway.ts`, ~line 906). Both
  need a `durationMs` field threaded through (`InsertAuditLogInput` interface is at
  `packages/ai/src/repository.ts:198`). `MossActionAuditLogTable` type is at
  `packages/db/src/types.ts:524` — add `duration_ms: number | null`.
- `recordAuditRaw`/`recordAudit` are called as fire-and-forget (`void this.recordAudit(...)`) from
  several call sites in `gateway.ts` (lines ~232, 262, 277, 309, 786, 815) — timing must be
  measured around the actual tool `execute()` call and passed into whichever recordAudit call
  reports the outcome. Read the surrounding function bodies at those line numbers before editing;
  I have not read full call-site bodies yet, only confirmed they exist.
- Tasks 1-4 (duplicate suppression, ceiling) already shipped and live in
  `packages/integrations/src/call-memory.ts` (`CallMemory` / `RequestBudget`, both exported as
  module-level singletons `callMemory` / `requestBudget`) — NOT in gateway.ts. Read this file in
  full if you need the exact shape; it is short (216 lines). Key pattern: buckets keyed by
  `${actorUserId} ${chatSessionId}`, 30s window (`windowMs`), lazy expiry only (no active sweep
  timer today).
- The per-call rebuild problem (Task 8 target) is
  `createIntegrationsActiveModulesResolver` in `packages/integrations/src/tool-manifests.ts`
  (226 lines total, already read in full). Every invocation of the returned resolver function
  calls `repository.listConnections(scopedDb)` fresh and rebuilds every tool manifest via
  `buildToolManifest`. This resolver is invoked by `AiGateway.executableTools` in gateway.ts
  (~line 859), itself called on every tool-listing AND every individual tool-call path — confirmed
  by grep, not yet read in full.
- Task 9 target is `callMcpTool` in `packages/integrations/src/mcp-client.ts` (93 lines, already
  read in full). It calls `connect()` (which does StreamableHTTP then falls back to SSE in a
  catch) and `client.close()` in a `finally`, on every single call — no reuse today. The only
  caller is `tool-manifests.ts:161` (`buildToolManifest`'s `execute` closure), which has the
  `conn.id`, `ctx.actorUserId`, `ctx.chatSessionId` available to build a cache key matching
  call-memory's `${actorUserId} ${chatSessionId}` + connectionId scoping.
- Connection/tool-selection mutation points (Task 8's cache-invalidation triggers), all in
  `packages/integrations/src/routes.ts` (362 lines, already read in full): `POST /api/integrations`
  (create), `PATCH /api/integrations/:id` (edit — covers enabledGroups/enabledTools/mutedTools/
  unsuppressedTools/credential/etc.), `POST /api/integrations/:id/refresh` (rediscovery),
  `DELETE /api/integrations/:id`. Each already has `accessContext.actorUserId` in scope at the
  point the mutation succeeds — that's where to call a cache-invalidation hook.
- Existing unit test files to follow the pattern of: `tests/unit/integrations-call-memory.test.ts`,
  `tests/unit/integrations-tool-manifests.test.ts`. Existing integration test:
  `tests/integration/integrations-mcp-client.test.ts` (fake MCP server pattern already exists —
  reuse it for Task 9's test).
- `packages/integrations/src/limits.ts` holds the shared constants (`TOOL_CALL_TIMEOUT_MS = 30_000`,
  `INTEGRATION_CALL_CEILING = 12`, `INTEGRATION_REQUEST_CHAR_BUDGET = 24_000`,
  `INTEGRATION_RESPONSE_CHAR_CAP = 8_000`). Add the tool-list cache's 30s window here too, matching
  the plan's "same number as Task 3's store" instruction.

## Not yet done — pick up here

1. Finish grounding (quick): read the `gateway.ts` call sites around lines 232/262/277/309/786/815
   and the `executableTools`/tool-call dispatch path (~750-900) to see exactly where a duration
   timer should wrap the call and which `recordAudit` call reports the final outcome (including
   the suppressed/refused paths from `tool-manifests.ts`'s `execute` — those return through the
   normal envelope, not a distinct gateway code path, so check whether the gateway can even tell a
   suppressed call apart from a normal success today, or whether that distinction has to come from
   the envelope's `status`/`summary`, or a new signal). This is the one real open design question
   for Task 7 Step 3 (distinct suppressed/refused outcome) — resolve it during planning, not code.
2. Run `plan-build` (not `superpowers:writing-plans`) to produce
   `docs/superpowers/plans/2026-09-02-2175-lane3-speed.md` covering Tasks 7, 8, 9 using the
   grounding above. Decisions only (exact paths/signatures/DDL/test cases), no pasted
   implementation bodies.
3. Message `coordinator` with the plan pointer, wait for approval, then build test-first, one
   commit per task, explicit paths (never `git add -A`).
4. Pre-push trio + rebase, then `coordinated-wrap-up`: push, open one PR for Tasks 7-9, live-path
   proof (Task 10 steps 3 — tool list not rebuilt per call — is the one this lane must prove; do
   not attempt Task 10 steps 1/2/4/5, those belong to other lanes' proof, though step 3 needs a
   real multi-call chat request against the live Home Assistant connection).
5. Fill the PR release note as Changed, plain English, one sentence.
6. Check whether Tasks 7-9 need any app-map update (`packages/shared/src/app-map-core.ts` or the
   integrations manifest) — my working assessment is no, since nothing here is a new
   screen/setting/nav path, only internal telemetry and caching, but confirm before treating it as
   settled.

## Standing rules (carry verbatim into any further sub-agent prompt)

- Plain English to humans in chat/status/handoffs (box-wide CLAUDE.md); code/commits/comments stay
  technical.
- Never `git add -A`/`git add .`, never bare `git commit` — explicit paths only, via
  `shared-checkout` skill for any commit in this shared checkout.
- DB-touching commands only via `verify-gate` skill, never unscoped, never piped.
- Never test against production or port 1533.
- Live-path gate: PR needs real UI proof on the live dev instance, posted as a PR comment, not
  claimed without being performed.
- One relay is the absolute recovery limit for this lane. If the successor also hits the 70%
  warning without an open PR, stop and report back to the coordinator for a re-slice — do not
  relay again.
- Never edit an applied migration; new SQL files only, globally unique 4-digit version.
- Secrets never enter source, docs, payloads, logs, audit records, prompts, or PR comments.
- Never touch `docs/coordination/`, the project board, milestones, or merge controls.
- Sign every message to the coordinator with your own pane id.
