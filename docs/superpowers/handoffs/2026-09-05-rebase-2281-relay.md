# Rebase PR 2281 onto main after 2255 — relay (2026-09-05)

Plain English for every reader. Relay depth is 1: do not relay again. If the meter fires, push
what is green, post what you have, and tell the coordinator.

## Where things stand

- Branch `fix/sports-picker-groups`, worktree `~/Jarv1s/.claude/worktrees/sports-picker-groups`.
  PR 2281. Brief: `~/.coord-briefs/boot-rebase-2281.txt` (steps 5 to 7 remain).
- Rebased onto origin/main (2255 merged as e3f06586e). Five original commits replayed, plus one
  new commit 660701f84 that moves the three grouping tests into
  `tests/unit/settings-sports-search-groups.test.tsx` (the pane test file hit 1042 lines and the
  1000-line file-size check failed the first gate run).
- Conflict resolution: kept the grouped rendering from 2281; its team-tile helper now decides
  "active" through followFor with the team's source id and passes that id to onToggle (2255's
  semantics). Browser test mock search matches name or short name over ALL_TEAMS.
- Prettier already clean on all touched files (checked, exit 0).

## Verification so far

- Sports browser tests (3 specs, 13 tests): exit 0. Log `/tmp/rebase-2281-e2e.log`.
- Unit tests for the two sports pane test files: 39 passed, exit 0. Lint on them: exit 0.
- First full gate run: exit 1 on check:file-size only (fixed by the split above).
- Second full gate run in progress: log `/tmp/jarv1s-gate/sports_picker_groups-20260905-004047.log`.
  Static checks passed; it was in test:unit at relay time. Read the verdict with
  `scripts/run-gate.sh status` (verify-gate skill; never pipe it). Expect exit 0 unless the
  module-sdk-worker local-only failure appears (known, green in CI; do not bisect).

## Live proof (done, instance running)

- Api on 3281 and web on 5281 started from this worktree with `/tmp/rebase-2281-start.sh`.
  Pids in `/tmp/rebase-2281-pids.txt`. Logs `/tmp/rebase-2281-api.log`, `/tmp/rebase-2281-web.log`.
  The shared api on 3000 runs a checkout without 2255, so both were started here.
- URL for Ben: http://192.168.50.36:5281 then Settings > Modules > Sports (address
  `/settings?section=modules&module=sports`). Login ben@ben.com / jarvistest123!.
- Proof script `/tmp/rebase-2281-live.cjs`, results `/tmp/rebase-2281-live.json`, screenshots in
  `/tmp/rebase-2281-shots/` (01-pac-crop.png, 02-united-crop.png, 03-followed.png, 04-unfollowed.png).
  "pac" gave four league groups (NFL: Packers; NBA: Pacers; NCAA Football (FBS): 6 tiles; NCAA
  Baseball: 5 tiles), NC State shown under each sport, no ungrouped tiles. "united" gave three
  groups (Premier League 3, EFL Championship 2, EFL League One 3). Following Sheffield Utd went to
  "Following" through the real api, then unfollowed cleanly. The follow request body carries
  competitionKey and teamKey only; that matches main's landed code, not a regression.

## What is left (steps 5 to 7 of the brief)

1. Wait for the gate verdict (status command above). If red for anything other than the known
   worker suite, fix, commit path-scoped, re-run the gate.
2. Push: `git push --force-with-lease origin fix/sports-picker-groups` (already pushed once at
   relay time; push again if you commit).
3. PR comment on 2281 titled "Rebased onto main after 2255": what changed in the rebase (above)
   and gate results with exit codes (browser 0, unit 0, lint 0, full gate: fill in).
4. Second PR comment with the live proof and the URL above; say the instance stays up for Ben.
5. Report: `herdr agent prompt coordinator "<one line: PR 2281 head sha, gate exit codes, live URL>"`
   signed with your pane id. Then ask the coordinator to reap the old pane w1:pKE.
