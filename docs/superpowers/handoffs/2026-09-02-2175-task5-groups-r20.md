# Build Handoff — #2175 Lane 2 Task 5

**Approved spec/plan:** integration tool-call discipline, Task 5 only  
**Issue:** #2175  
**Tier:** `sensitive`  
**Worktree/branch:** `~/Jarv1s/.claude/worktrees/2175-task5-groups-r20`,
`build/2175-task5-groups-r20` from green main `35814acd0`  
**Coordinator:** `coordinator`, session `01a062e9-f0cb-75d2-96a7-fa8c431f1163`

Fable's binding authorization is issue #2175 comment `5513240797`. Build Task 5 only: capture the
real 75 names as a safe checked-in fixture; implement the exact derived-group procedure; keep
service tool names unchanged; activate opt-in for large ungrouped connections; and use the
smallest existing persistence seam for the explicit-enabled-list grandfathering step so current
tools stay on but later discoveries do not auto-enable. Prove the upgrade path and sensitive-tier
invariants. Inspect the end-to-end discovery/curation persistence flow before proposing a compact
plan; wait for coordinator approval before edits.

Invoke `coordinated-build` and finish with `coordinated-wrap-up`: focused red/green, isolated full
gate via `verify-gate`, pushed PR, independent live/read-only proof where applicable. Never touch
Task 6 UI or Lane 3, `docs/coordination/`, prod/port 1533, settings/entities/physical actions, or
protected evidence. Work only here, commit explicit paths, no repo-wide format, no secrets. One
session for Task 5; at 70% invoke the single allowed relay immediately. Do not merge or change the
board, and do not end between steps.
