# Relay: fix 2290 + 2291 (News favicons)

Plain English rule for every human-facing message: no jargon, no coined shorthand, ASCII
punctuation only, at most one backtick per sentence. Pass this on verbatim to any agent you spawn.

Brief: `~/.coord-briefs/boot-fix-2290-2291-favicons.txt` (read it in full; it is the contract).
Worktree: `~/Jarv1s/.claude/worktrees/favicons-2290`, branch `fix/2290-2291-news-favicons`.
Tier: routine. Astra reviews after the PR opens. Relay depth: this is relay 1 (the last allowed).

## Done (commit dba82e507, on top of main e3f06586e)

- 2291: `packages/news/src/favicon-route.ts` now passes the requested domain plus the catalog
  publisher's `imageHosts` as the redirect allow-list (`faviconFetchHosts`). New lookup
  `sourceEntryForHomepageHost` in `packages/news/src/source/catalog.ts`. Custom sources stay
  same-host. The fetch port already refuses any hop outside the list, so nothing else changed.
- 2290: Today list icon sits on an 18px tile: wrapper span `nw-twlist__favicon-tile` in
  `packages/news/src/web/today-widget.tsx`; layout in `packages/news/src/web/styles/news-2.css`;
  colour (`--white` background, `--shadow-xs` ring, `--radius-xs`) in
  `packages/ui/src/styles/components-news.css`. Only the Today card renders favicons; the News
  desk itself shows publisher names as text, so there was nothing to change there.
- App map: the News navigation description in `packages/news/src/manifest.ts` now says the icon
  sits on a light tile so it reads in both themes.
- Tests: three new cases in `tests/unit/news-favicon-route.test.ts`.
- Verified green (unpiped, logs in /tmp/fix-2290-*.log): `pnpm typecheck`, `pnpm lint`,
  `pnpm format:check`, `pnpm check:design-tokens`, `pnpm check:ui-classes`, and vitest on
  news-favicon-route, news-today-widget, news-manifest, news-page (28 tests pass).
  The full DB gate was NOT run; the brief only requires the static checks plus touched unit tests.

## Left to do, in order

1. Push the branch and open the PR against main with `gh pr create`. Body: Summary, "Closes #2290"
   and "Closes #2291", Release note section: Category Fixed, Title "News publisher icons",
   Description one plain sentence (icons now show for NPR and read clearly in both light and
   dark mode). No `gh pr edit` for the body (it fails here); use the REST PATCH if a body edit is
   needed.
2. Live proof from THIS worktree, own instance, api 3290 and web 5290 (never 3000, 5173, 5198,
   1533). Start by hand (no port helper exists):
   - api: `PORT=3290 JARVIS_AUTH_TRUSTED_ORIGINS="http://localhost:3290,http://192.168.50.36:5290,http://localhost:5290,http://localhost:3000" BETTER_AUTH_SECRET=<any stable dev value> pnpm dev:api`
     (check apps/api for an env file pattern first; the api reads DATABASE settings from the
     same place the main dev instance does, default is the live dev database, which is expected).
   - web: `JARVIS_API_PROXY_TARGET=http://localhost:3290 pnpm --filter @moss/web dev -- --port 5290`
   - record every pid in `/tmp/fix-2290-pids.txt`; stop by those pids only, never by name.
   - Sign in ben@ben.com / jarvistest123! at http://192.168.50.36:5290. Check: NPR icon shows on
     the Today news card; icons read in light and dark theme (toggle theme in settings);
     `GET /api/news/favicon/www.npr.org` answers 200 (curl with the browser session cookie, or
     confirm through the page's network requests). Screenshots go to disk, view cropped only.
   - Post a PR comment headed "Live proof (2290, 2291)" with URL, what was checked, what was seen.
   - Stop the instance by pid.
3. Report: `herdr agent prompt coordinator "2290/2291 PR <number> open at <sha>, live proof <posted or unproven, one plain sentence> [pane $HERDR_PANE_ID]"` then confirm it landed with a bounded read.

## Traps already known

- A stale server can hold a port and fake a healthy instance; check the pid file before trusting :3290.
- Login from a non-standard port fails 403 unless the trusted origins env var above is set.
- Never pipe a gate command; never read a log unbounded.

## Relay 1 result (2026-09-05, PR #2294)

- The app-map description was over 240 chars and the api would not start; shortened (845a6a670).
- The redirect allow-list alone did not give NPR an icon: the news image fetch runs a robots gate
  on every hop and media.npr.org disallows everything. Added a robots-free `favicon` port for News
  (host-pinned, HTTPS, size-capped, rate-limited), same posture as the sports icon route (#2211).
- Live on api 3290 / web 5290: favicon route answers 200 for www.npr.org (PNG, 1823 bytes),
  www.theguardian.com and www.nytimes.com; Settings > Appearance toggles dark/light and the tile
  measures white bg, ring, 4px radius in both. NOT yet captured: a screenshot with the NPR icon
  visibly drawn on the Today card (NPR was the lead row, which carries no icon; list rows do).
  Proof script: /tmp/fix-2290-proof.mjs. Re-slice from here.
