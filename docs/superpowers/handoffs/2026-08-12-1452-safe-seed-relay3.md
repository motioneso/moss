# Relay 3 — fix-1452-safe-seed

**Issue:** #1452 (part of #1440). **Risk tier:** `routine`. Plan written and committed, zero build
code yet. Relaying at the context-meter 70% warning.

**Worktree:** `/home/ben/Jarv1s/.claude/worktrees/coord-overnight-20260810/.claude/worktrees/fix-1452-safe-seed`
**Branch:** `fix-1452-safe-seed`. `node_modules` already installed — do NOT re-run `pnpm install`.

**Coordinator:** label `Coordinator`, session `0bb9f516-c026-454f-bc97-dc9faf43bd20` — re-resolve
via fresh `herdr pane list` before messaging (never a cached pane id).

## Done

1. Full seams research complete (relay-1/2) — do not re-read the issue or re-derive these.
2. **Plan committed:** `docs/superpowers/plans/2026-08-12-fix-1452-safe-seed.md` (commit `e87fda722`).
   Covers: 15-item seams check w/ file:line citations, Task 1 (new UAT spec
   `tests/uat/specs/1452-briefing-live-content.uat.spec.ts` — throwaway signup via real auth UI →
   direct authenticated-fetch definition-create (`selectedToolNames: ["vault"]`, a virtual source
   that bypasses the module-manifest check, so zero installed modules is fine) → run-trigger POST →
   poll `GET .../runs` (60s ceiling, no "pending" state — row appears only on completion) → reload →
   assert rendered card + zero `/Jarvis/i` occurrences → screenshot), Task 2 (two `blocking` rows in
   `.claude/skills/coordinate/uat-trigger-map.tsv`), kill gate, verification commands. **No open
   design questions** — read the plan's "Open questions" section, not the full plan, for why.
3. Plan-ready message sent to coordinator (pane `w1:p7P` at send time, session id above) requesting
   approval or a fork-flag. **No reply yet as of this relay** — coordinator showed `agent_status:
   working` on the last `herdr pane list` check, not necessarily on my message.

## Not done — next steps in order

1. **Re-resolve the coordinator pane fresh** (`herdr pane list`, match by label `Coordinator` +
   session id above) and check for a reply to the plan-ready message. If no reply and pane is idle,
   it's safe to proceed per the plan-ready message's own wording ("or wait for your reply if you
   flag anything first") — a silent approval is fine here per `coordinated-build` step 1, since the
   message explicitly offered to proceed absent objection. If the coordinator flagged something,
   resolve that first.
2. **Build Task 1** via TDD: `tests/uat/specs/1452-briefing-live-content.uat.spec.ts`, exactly per
   the committed plan's Task 1 section (test outline is fully specified there — 10 numbered steps).
   Read the plan **by section** (Task 1 only) when writing it, not front-to-back again.
3. Commit Task 1 alone (explicit path, `shared-checkout` discipline — no `git add -A`).
4. **Build Task 2**: add the two TSV rows to `.claude/skills/coordinate/uat-trigger-map.tsv` exactly
   as specified in the plan's Task 2 section. Commit alone.
5. Run `pnpm typecheck` standalone on the new spec file before any gate attempt (vitest's transform
   doesn't catch `noUncheckedIndexedAccess`/cast errors that typecheck does).
6. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`, then
   `git fetch origin main && git rebase origin/main`.
7. `coordinated-wrap-up`: own isolated-DB gate run (via `verify-gate` skill — never improvise),
   push, open PR, post live-path proof (`gh pr comment` with rendered-card screenshot + confirmation
   the shared dev DB was left as found — the UAT harness's own ephemeral-stack teardown is that
   proof, cite seam #1 in the plan), report to coordinator. Never merge, never touch
   board/milestones/`docs/coordination/`.

Never end your turn between steps. Relay again only on the next 70%-warning or compaction summary —
build and commit first if that fires before you've made progress.
