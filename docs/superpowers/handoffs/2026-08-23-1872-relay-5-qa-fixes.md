# Issue #1872 — relay 5: fix QA's red verdict on PR 1874

Same worktree `1872-fable-plan`, branch `1872-fable-plan`. You are sole owner — no other
session should be touching this worktree; confirm with `herdr agent list` before any git action
per the shared-checkout guardrail (same rule as every prior relay doc).

PR: https://github.com/motioneso/moss/pull/1874 (already open, already pushed once).
QA posted a RED verdict on it (3 blocking: B1, B2, B3; plus S1-S7 coverage gaps). The coordinator
(pane resolved fresh via `herdr agent list`, look for the codex agent named "coordinator") told
this relay to fix it, re-run the gate, push, update PR evidence, and report for fresh QA.

## Already done, verified locally — do not redo

All of this is sitting as **uncommitted changes** in the working tree right now:

- **B1**: `docs/superpowers/plans/2026-08-23-service-worker-image-fetch-recovery.md` reformatted
  with `npx prettier --write` — checked the diff, it is whitespace-only (`*emphasis*` to
  `_emphasis_`), no wording changed. This is allowed: QA's B1 explicitly says run Prettier over
  this file and commit it, and it's this PR's own file (added by this branch's commit
  `7383a939b`), not a pre-existing failure.
- **B2**: added `"test:sw": "playwright test --config playwright.sw.config.ts"` to
  `package.json` (next to `test:e2e`), and a new CI step "Run Service Worker regression test" in
  `.github/workflows/ci.yml` right after "Run Playwright smoke tests" (5 minute timeout, same
  pattern as the existing steps). Verified locally: `pnpm test:sw` passes, 1/1.
- **S1, S3, S4, S5**: added 5 new tests to `tests/unit/service-worker-fetch.test.ts`:
  exact-three-attempts for an image that always fails, exact-one-attempt for a non-image, the
  failure response is genuinely `Response.error()` (`type: "error"`, `ok: false`, `status: 0`,
  not just "any Response"), `/api/` paths are never intercepted and never touch `caches.match`,
  non-GET requests are never intercepted. Added a `tryDispatchFetch` helper (doesn't throw when
  the worker declines to handle a request — the old `dispatchFetch` always threw in that case,
  which is exactly why S4 was untestable before).
- **S6**: `apps/web/public/service-worker.js` — the generic GET branch's `caches.match(request)`
  had no `.catch()`, so a rejected cache lookup (storage full/corrupted, private-browsing) would
  still reject `respondWith()`'s promise, the same defect the rest of this PR fixes, one step
  earlier. Added `.catch(() => fetchWithRecovery(request))` after the `.then()`. This is the same
  statement Task 1 of the approved plan already authorizes touching ("lines 48-50 only" in the
  plan's line numbers, same GET branch), and it's required by the plan's own stated design goal
  ("respondWith()'s promise never rejects for any GET, same-origin or cross-origin") — not a scope
  expansion. Added a matching test (cache lookup rejects, falls through to network, resolves
  the successful response, exactly one fetch call).
  **Result: `tests/unit/service-worker-fetch.test.ts` now has 11 tests, verified all 11 passing**
  (`pnpm test:unit tests/unit/service-worker-fetch.test.ts > /tmp/1872-unit2.log 2>&1; echo
  EXIT=$?` gave EXIT=0, "11 passed (11)").
- S2 and S7 were **not** touched — S2 is a severity note about S1's bound, not a separate
  requirement (S1's new exact-count test covers it). S7 (offline page missing from cache) is
  pre-existing and out of this PR's scope per QA's own wording ("Pre-existing and untouched").
- **B3**: captured a durable live-path proof with a genuinely induced failure (not happy-path).
  Script, evidence, and result are described below — **not yet posted to the PR**.

## What's left, in order

1. **Fix the PR body's false claims** (this is part of B1 — QA flagged the waiver note as wrong,
   not just the gate). Current body has two problems, both in the copy already on the PR
   (`gh pr view 1874 --json body -q .body`):
   - The "Notes" section says the plan file's formatting failure is "pre-existing,
     out-of-scope... not something this PR should fix" — that's false (see B1 above) and needs to
     be replaced with something like: "The plan document had a formatting issue from Prettier's
     em-phasis-marker rule; it's fixed in this update (whitespace only, no wording changed)."
   - The Test plan checkbox for `pnpm verify:foundation` says "see gate note below" pointing at
     that false claim — once the gate is actually rerun clean (next step), update this line with
     the real exit code and gate database name, same format the earlier relay docs used.
   - Also add a line noting the new unit tests (11 total now) and the new `pnpm test:sw` CI wiring.
2. **Full pre-push checks, unpiped**:
   `pnpm format:check && pnpm lint && pnpm typecheck` — all three should be clean now that the
   plan file is reformatted (format:check was the only known failure).
3. **Full gate** — use the `verify-gate` skill before running `pnpm verify:foundation`, exactly as
   every prior relay doc for this issue says. Fresh isolated gate database, never the live dev
   database. Prefer `scripts/run-gate.sh` per the `coordinated-wrap-up` skill's guidance (start /
   wait / status, never `pgrep`, never a piped exit code).
4. **Re-run `pnpm test:e2e`** to reconfirm the full suite is still green after the
   `service-worker.js` change (the cache-lookup `.catch()` is a small addition; the e2e config
   never registers the real SW — it's `vite dev`, no PROD build — so this is a low-risk check, not
   expected to interact, but it was in the original verification list so keep it in).
5. **Rebase on `origin/main`** (`git fetch origin main && git rebase origin/main`) before pushing
   — several other PRs have landed on main since this branch's last push.
6. **Commit by explicit path** (use the `shared-checkout` skill; confirm via `herdr agent list`
   first that no other session is in this worktree). Files to commit:
   `apps/web/public/service-worker.js`, `tests/unit/service-worker-fetch.test.ts`,
   `docs/superpowers/plans/2026-08-23-service-worker-image-fetch-recovery.md`, `package.json`,
   `.github/workflows/ci.yml`. Do not touch anything else — `git status --porcelain` should show
   exactly these five files before you commit.
7. **Push**, then **update the PR body** with `gh pr edit 1874 --body-file <file>` (corrected
   Notes section and Test plan section from step 1, plus the real gate exit codes from step 3).
8. **Post the B3 live-path proof as a new PR comment** — durable text, not just `/tmp` file
   references (QA's exact complaint: "all the raw evidence lives in `/tmp` on the author's
   machine, so none of it can be checked from the PR"). Use the captured evidence below, quote
   the JSON inline in the comment body so it's readable directly on GitHub.
9. **Report to the coordinator for fresh QA** — resolve the coordinator's pane fresh via
   `herdr agent list` (do not reuse a pane id from this or any other doc, they reflow), sign with
   your own pane id. Plain English, no jargon, per CLAUDE.md and the global instruction that's
   been carried through every relay doc on this issue — this applies to the PR body and comment
   too, not just chat.
10. Kill any throwaway processes you start for this (`ps aux | grep -E "vite preview|1872-live"`)
    before reporting done. None should be running when you finish — this relay already killed the
    ones it used.

## B3 evidence already captured — use this for the PR comment

Live instance = a built app (`pnpm --filter @moss/web build` then `vite preview --host 127.0.0.1
--port 4180`) served through a small proxy at `http://127.0.0.1:4181` that forwards `/api/*` and
`/health` to the real dev API on `:3000` (rewriting Origin/Referer so Better Auth trusts it) and
everything else to the preview server. Proxy source is `/tmp/1872-live-proxy.mjs` (still on disk,
not part of the deliverable — throwaway). Both processes were started and then stopped again by
this relay; restart them the same way if you need to re-verify before posting.

The proof script (`/tmp/1872-b3-live-proof.mjs`, needs `@playwright/test` resolved — run it copied
into the repo root temporarily, e.g. `sed 's/from "playwright"/from "@playwright\/test"/'
/tmp/1872-b3-live-proof.mjs > ./throwaway.mjs && node ./throwaway.mjs`, then delete the copy) does
this against the live proxy:

1. Loads `/`, confirms the registered service worker controls the page
   (`scriptURL: http://127.0.0.1:4181/service-worker.js`).
2. Injects an `<img>` pointing at a local `node:http` server that **destroys the TCP socket** on
   the first request to that exact path (a real, low-level induced failure — not a mock) and
   serves a real 1x1 PNG on the request after.
3. Waits for the image element's `load`/`error` event, no page reload anywhere in the script.
4. Captures every console message the whole time and screenshots before/after.

Result (`/tmp/1872-b3-result.json`, full contents — paste this into the PR comment):

```json
{
  "steps": [
    "attempted sign-in as ben@ben.com through the real dev API (proxy at 127.0.0.1:4181)",
    "service worker controller present: true",
    "controller scriptURL: http://127.0.0.1:4181/service-worker.js"
  ],
  "consoleMessages": [
    "Failed to load resource: the server responded with a status of 401 (Unauthorized)"
  ],
  "badConsole": [],
  "imageOutcome": { "loaded": true, "width": 1, "height": 1 },
  "serverEvents": [
    { "path": "/induced-failure.png", "outcome": "socket destroyed (induced failure, first request)" },
    { "path": "/induced-failure.png", "outcome": "200 real PNG served" }
  ],
  "urlPageStayedOn": "http://127.0.0.1:4181/"
}
```

Read this plainly for the comment: the image's first request was killed at the network level on
purpose; the same image element then loaded successfully with no page reload; the console carried
no trace of the failure text this issue is about (`badConsole` is empty); the one console line
that did appear is the same expected sign-in check every earlier proof on this PR also saw.
Screenshots are at `/tmp/1872-b3-before.png` and `/tmp/1872-b3-after.png` (both exist, 1280x720,
cannot be attached to a PR comment from the command line — say so plainly in the comment, same
wording pattern as the first live-path comment already on the PR).

## Guardrails carried over from every prior relay doc on this issue

- Do not touch the Fable plan file's wording — only the Prettier formatting pass already done and
  verified as whitespace-only.
- Keep CSP and chat-stream warnings out of scope.
- No `git add -A`, no broad `git add`; explicit paths only, via the `shared-checkout` skill.
- Plain English in all chat, status updates, PR text, and comments — no jargon, no invented
  shorthand. Pass this rule on to any agent you in turn spawn.
- Do not merge, move the board, or close the issue — report to the coordinator and stop.
