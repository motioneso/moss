# #1110 app-map relay — checkpoint 20

Worktree: `~/Jarv1s/.claude/worktrees/build-1110-app-map`, branch `build/1110-app-map`, PR #1122,
head `874759ec`. Coordinator: re-resolve label fresh via `herdr pane list` (was
`Coord-1109-1110-g7` as of this checkpoint). **Never merge/board/close** — report only.

## Status: browser-safety fix is DONE/pushed/merged-ready (checkpoint 19). Active work is a
NEW task: local CI-conditioned repro to discriminate branch-defect vs CI-environment-fragility
for the VF timeout, per Fable's ruling (delegated by Ben) on issue #1123.

**Do NOT re-litigate**: the `errors.ts` leaf-split root cause/fix from checkpoint 19 is correct
and already pushed (commit `a29cd8aa`, on `874759ec`). That is not in question anymore.

## Full authorization — read this before doing anything

Fable's full ruling is posted at `gh api repos/motioneso/Jarv1s/issues/comments/5000410449`
(issue #1123). Key points already extracted (don't re-fetch unless verifying):

- **No more `gh run rerun`. No waiver. No CI-Postgres re-provisioning yet.** Only one action
  authorized: the local repro below.
- Counter-evidence Fable flagged against pure runner-noise: the signature recurred **identically
  3x** — same 3 files (`multi-user-isolation`, `account-self-deletion`, `auth-bootstrap-recovery`),
  uniform **~10.9s per-test timeout**. A stochastic noisy-neighbor wouldn't produce a constant.
  Also: Postgres in VF is a per-job service container (no cross-job DB contention), and `fix/1112`
  ran the identical VF job green same morning.
- **Task**: under CI-like constraints (~2 vCPU via taskset/cgroup, fresh pgvector Postgres
  container at CI-default settings incl. `max_connections` — i.e. NO custom `command:` overrides,
  confirmed `infra/docker-compose.yml`'s postgres service has none, just image defaults), run
  `test:integration` on branch head `874759ec` **and** on `origin/main` HEAD, same constraints.
  Start with the failing trio. Instrument `pg_stat_activity` snapshots + pool counters during any
  hang. **Name the exact config constant the ~10.9s corresponds to** — ruled out already: vitest
  `testTimeout` (30s, see `vitest.config.ts`), and `pg.Pool` `connectionTimeoutMillis` default
  (5000ms, see `packages/db/src/database.ts:20-22`, overridable via `JARVIS_DB_CONNECT_TIMEOUT_MS`).
- Verdict → post as a comment on issue #1123:
  - Reproduces on branch only → real defect, fix on this branch, re-verify locally under same
    constraint, push ONCE, ping coordinator.
  - Reproduces on main too → suite/infra fragility, NOT a #1122 defect. File separate CI-infra
    task with evidence; do **not** touch PR #1122 code for this.
- **Separately** (issue #1110 comment 5000410700): #1110's exit criterion deferred to #1121,
  APPROVED — but merge additionally needs a manual live-path walk (3 spec §8 scenarios, with
  bounded action logs and DOM/network assertions in a `gh pr comment`) on a dev instance running the PR head, **after** CI
  clears. **Do not start this yet** — repro comes first.

## Research done so far (this session, not yet acted on)

- `packages/db/src/database.ts`: `createDatabase()` → `pg.Pool({ max: options.maxConnections ??
  4, connectionTimeoutMillis: options.connectionTimeoutMillis ??
  Number(process.env.JARVIS_DB_CONNECT_TIMEOUT_MS ?? 5000) })`. Every pool defaults to **4 max
  connections, 5000ms connect timeout** unless a caller overrides.
- Grepped repo-wide for a literal `10900`/`10_900`/statement_timeout/lock_timeout/
  idle_in_transaction_session_timeout — **none found anywhere** (SQL migrations, TS, better-auth
  config, pg-boss config). The ~10.9s is NOT a hardcoded constant in this repo — it must be
  **emergent** (e.g. sum of retries, or a Postgres/OS-level default under the CI resource
  profile) and has to be found empirically via the instrumented repro, not by more grepping.
  Checked and ruled out as explaining it: `auth-bootstrap-recovery.test.ts`'s
  `waitForUserCountByEmailPrefix` helper uses a 20_000ms deadline (not 10.9s), and only gates 2
  of that file's tests, not the systemic pattern across all 3 files.
- `infra/docker-compose.yml` postgres service: `pgvector/pgvector:pg17` image, **no custom
  `command:` args** — i.e. CI already runs plain image defaults, nothing to match/copy beyond
  "don't add tuning flags."
- Host resources: 16 vCPU, 62GB RAM (2.2GB free, 24GB buff/cache — don't assume headroom without
  checking `free -h` again before spinning up more containers). Docker 24.0.7 available. cgroup v2
  unified hierarchy present (`cpuset cpu io memory ...` controllers available) — `taskset` also
  available at `/usr/bin/taskset`.
- **Subnets already in use** (avoid collision — see also memory `uat-docker-subnet-map.md`):
  `infra_jarv1s` network = `10.251.0.0/24` (this is the SHARED dev Postgres, container
  `jarv1s-postgres`, port host `55433` — **do not touch, do not reuse, other sessions may depend
  on it**), `jarv1s-prod_jarv1s` = `10.252.0.0/24` (**never target**, prod). No `uat*` networks
  currently up. Port `55433` is bound (shared dev PG) — pick a different host port for the repro
  container. **Not yet checked**: which subnet to use for the isolated repro network — `10.253.0.0/24`
  looks free based on `docker network ls` (no collision seen) but re-verify with `docker network
  inspect` / `ip addr` before use, since other agent sessions may have started something since.

## Next steps (in order) — for whoever picks this up (fresh session or self)

1. **Build the isolated repro harness** (nothing here touches the shared `infra_jarv1s` stack):
   - New standalone docker-compose (put it in the scratchpad dir, NOT committed to the repo —
     this is throwaway local tooling for one investigation, not a permanent fixture) with just a
     `postgres: pgvector/pgvector:pg17` service, own container name (e.g. `jarv1s-repro-pg`), own
     host port (NOT `55433`), own bridge network on an unused subnet (verify `10.253.0.0/24` is
     still free first), `deploy.resources.limits.cpus: "2"` or `cpu_count`/`--cpus 2` at run time
     to emulate CI's ~2 vCPU, no custom Postgres `command:` tuning (match CI's plain-defaults
     posture exactly).
   - Run `pnpm db:migrate` against it (point `JARVIS_BOOTSTRAP_DATABASE_URL` /
     `JARVIS_MIGRATION_DATABASE_URL` / `JARVIS_APP_DATABASE_URL` / `JARVIS_AUTH_DATABASE_URL` /
     `JARVIS_WORKER_DATABASE_URL` env vars at the new port — see `infra/docker-compose.yml`
     lines 74-78 for the exact role/password pattern to replicate).
   - Wrap the actual `vitest`/`test:integration` invocation with `taskset -c <2 cores>` to
     constrain the Node process itself to ~2 logical CPUs too (don't just constrain Postgres).
   - Instrumentation: a background loop (`Monitor` tool or backgrounded `Bash`) polling
     `docker exec jarv1s-repro-pg psql -U postgres -d jarv1s -c "select pid, usename,
     application_name, state, wait_event_type, wait_event, now()-query_start as query_age,
     now()-state_change as state_age, left(query,120) from pg_stat_activity where
     datname='jarv1s' order by query_start;"` every ~1s to a timestamped log file in scratchpad,
     running for the duration of each test run. Cross-reference snapshot timestamps against the
     vitest per-test timing output afterward to see what's blocked/waiting during the ~10.9s
     window (lock wait? no server-side connection at all = client-side pool queuing? idle-in-
     transaction pileup?).
2. **Run the failing trio** (`tests/integration/multi-user-isolation.test.ts`,
   `tests/integration/account-self-deletion.test.ts`,
   `tests/integration/auth-bootstrap-recovery.test.ts`) under the constrained harness on branch
   head `874759ec` (current worktree, already checked out, tree clean except the pre-existing
   unrelated `.claude/context-meter.log` diff — don't touch it, don't `git add -A`).
3. **Repeat identically on `origin/main` HEAD.** Do NOT `git checkout`/`reset` this shared
   worktree to get there — use a separate detached worktree per the CLAUDE.md Grounding
   Discipline: `git worktree add /tmp/<something-in-scratchpad-or-tmp> origin/main` (never `git
   pull` that worktree). Run `pnpm install` there if needed, point it at a SEPARATE repro Postgres
   instance (or the same one after a clean migrate/reset — simplest is a second isolated compose
   instance on yet another port/subnet to avoid any cross-contamination between the two runs).
4. **Analyze**: does the trio fail/hang the same way on both branch and main under these
   constraints, or only on the branch? Name the exact resource/constant the ~10.9s maps to from
   the `pg_stat_activity`/pool evidence (don't guess — cite the actual snapshot rows).
5. **Post the verdict as a `gh issue comment` on #1123** — evidence-backed (link/quote the actual
   snapshot data, timestamps, pass/fail counts for both runs). Then:
   - Branch-only repro → fix on `build/1110-app-map`, re-verify locally under the same
     constraint, push ONCE, ping coordinator.
   - Repro on main too → do NOT touch PR #1122 code. File a separate CI-infra task/issue with the
     evidence instead.
6. **Ping the coordinator** (re-resolve pane fresh via `herdr pane list` — was `Coord-1109-1110-g7`
   / `w1:pT3` as of this checkpoint, will likely have moved) with the verdict, terse mode.
7. Still do NOT start the #1110 manual live-path-walk follow-up (issue #1110 comment
   `5000410700`) — that's after CI clears, separate task, not authorized to start yet.

## LIVE STATE UPDATE (harness built, mid-run — read this before "Next steps" above)

Task #12 harness is DONE and live. Task #13 (branch trio run) is IN PROGRESS right now.

- Isolated repro Postgres is UP: `docker run -d --name jarv1s-repro-pg --network
  jarv1s-repro-net --cpus=2 -p 55599:5432 -e POSTGRES_DB=jarv1s -e POSTGRES_USER=postgres
  -e POSTGRES_PASSWORD=postgres pgvector/pgvector:pg17` (network `jarv1s-repro-net` created
  first: `docker network create --subnet 10.253.0.0/24 jarv1s-repro-net`). Confirmed healthy.
- `pnpm db:migrate` run against it with `JARVIS_PGHOST=localhost JARVIS_PGPORT=55599` under
  `taskset -c 0,1` (2 vCPU) — **EXIT=0, all migrations applied cleanly**. Log:
  `<scratchpad>/repro/db-migrate-branch.log`. (`<scratchpad>` =
  `/tmp/claude-1000/-home-ben-Jarv1s--claude-worktrees-build-1110-app-map/f8124cd9-d875-4190-b6c8-9fcad2bc412b/scratchpad`)
- **Key discovery this pass** (don't re-derive): `db:migrate` only needs to run ONCE per fresh
  Postgres instance — it creates the runtime ROLES (instance-level, e.g.
  `jarvis_migration_owner`) and migrates the base `jarv1s` db, but `pnpm test:integration`
  (via `scripts/test-integration.ts`) does NOT reuse that db. When `JARVIS_PGDATABASE` is unset,
  it creates its own fresh isolated `jarvis_test_<pid>_<hex>` database via `CREATE DATABASE`
  (no TEMPLATE) and sets `JARVIS_PGDATABASE` before spawning vitest. Each integration test file
  then bootstraps ITS OWN schema into that isolated db by calling
  `resetFoundationDatabase()`/`resetEmptyFoundationDatabase()` (`tests/integration/test-database.ts`)
  in its own `beforeAll` — full bootstrap SQL + all migrations run again, per file, against
  `connectionStrings.bootstrap`/`.migration` (which read `JARVIS_PGHOST`/`PORT`/`PGDATABASE` at
  call time via `getJarvisDatabaseUrls()`). This is CI's real flow too (CI never sets
  `JARVIS_PGDATABASE`, confirmed via `grep -n JARVIS_PGDATABASE .github/workflows/ci.yml` = no
  matches). **Do NOT set `JARVIS_PGDATABASE` yourself** — leave it unset so the isolation/
  passthrough logic in `scripts/test-integration.ts` matches CI exactly.
- Instrumentation poller is RUNNING IN BACKGROUND right now: script
  `<scratchpad>/repro/poll-pg-stat.sh` (polls `pg_stat_activity` on `jarv1s-repro-pg` every 1s,
  pid/usename/application_name/state/wait_event_type/wait_event/query_age/state_age/query,
  pipe-delimited, timestamped) writing to `<scratchpad>/repro/pgstat-branch-trio.log`. Its PID is
  in `<scratchpad>/repro/poller-branch.pid` (nohup'd, detached from this shell). **If this
  session restarts, check `kill -0 $(cat <scratchpad>/repro/poller-branch.pid)` — if dead,
  restart it before running the trio test** (`nohup <scratchpad>/repro/poll-pg-stat.sh
  <scratchpad>/repro/pgstat-branch-trio.log > <scratchpad>/repro/poller-branch.out 2>&1 &`).

### Immediate next command (was about to run this when checkpoint fired)

```bash
cd ~/Jarv1s/.claude/worktrees/build-1110-app-map
export JARVIS_PGHOST=localhost
export JARVIS_PGPORT=55599
taskset -c 0,1 tsx scripts/test-integration.ts \
  tests/integration/multi-user-isolation.test.ts \
  tests/integration/account-self-deletion.test.ts \
  tests/integration/auth-bootstrap-recovery.test.ts \
  2>&1 | tee <scratchpad>/repro/branch-trio-run.log
echo "EXIT=${PIPESTATUS[0]}"
```
(`test:integration`'s `package.json` script hardcodes the target dir to `tests/integration` —
this bypasses that via `scripts/test-integration.ts <files...>` directly, matching how the file
itself declares its own args are `process.argv.slice(2)`, verified in the file already.)

After this run: **stop the poller** (`kill $(cat <scratchpad>/repro/poller-branch.pid)`), then
repeat the whole harness+run against `origin/main` HEAD (Task #14) — remember: separate
worktree via `git worktree add` (never disturb this shared tree), and either reuse
`jarv1s-repro-pg` after a fresh `db:migrate` (simplest — CREATE DATABASE isolation means old
test dbs don't leak state into a new run anyway) or spin up a second instance on a different
port (e.g. `55600`) if running both back-to-back for comparison ever needs true parallelism —
sequential reuse is fine and lower-risk given this host's tight memory (was 2.6GB free/25GB
"available", swap fully exhausted at last check — don't run both repro Postgres instances
simultaneously without rechecking `free -h` first).

## Do NOT re-derive

- The `errors.ts` root cause / fix (checkpoint 19) — done, correct, pushed. Not in question.
- Why hypothesis-1 (my diff causing the timeout via resolution overhead) was already weakened —
  covered in checkpoint 19-era reporting (test:unit ran normal speed with the identical new
  alias; the fix is type-only, zero runtime code). Fable's ruling in #1123 comment 5000410449 is
  the current authoritative framing — work from that, not the earlier back-and-forth.
- Subnet/port collision facts above — already checked once this session; only re-verify
  immediately before creating the network (state can change between agent sessions), don't
  re-derive from scratch.
