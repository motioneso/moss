# Relay — #1319-A phase 2 build (catalog-verify lane)

**Status:** plan approved by coordinator. Zero code written yet. Resume at `coordinated-build`
step 2 (Build).

**Plan (approved, use this, not the master plan's line numbers):**
`docs/superpowers/plans/2026-08-23-1319a-phase2-catalog-verify.md`

**Master plan (context only — Tasks 3-4 have the full type/behavior/test spec; this lane's own
plan already re-verified line numbers against the current tree, so trust its citations over the
master plan's):** `docs/superpowers/plans/2026-08-18-1319-signed-module-catalog.md`

**Branch/worktree:** `build/1319a-catalog-verify`, this worktree. `node_modules` already
installed — skip `pnpm install`.

**Coordinator:** registered name `coordinator` (Codex, pane label "Coordinator"). Approved this
plan with these preservation constraints — keep them front of mind while implementing:
- fixed verification enums (`CatalogVerification`, `CatalogVerificationFailureReason`) exactly as
  specified in master-plan Task 3
- raw-byte digest/signature contract — switch `registry-source.ts` index fetch to `arrayBuffer()`,
  digest and verify over those exact bytes, never re-serialized JSON
- atomic snapshot invariant — one `fetchRegistryIndex` call produces entries + verification +
  digest together; cache never mixes fields from different fetches; `"unavailable"` is never
  cached
- non-admin-before-fetch ordering — `assertAdminUser` runs before `fetchRegistryEntries` is ever
  called (test this explicitly, per this lane's plan Task B)
- hard boundary — do NOT touch pipeline enforcement/409/override (Phase 3), ensure-at-boot
  (Phase 3 Task 6), web UI (Phase 3 Task 7), UAT (Phase 4), or any migration. Phase 1
  (catalog-signing.ts) is already merged to main (PR #1684) — read-only for this lane.

**Next concrete steps:**
1. Implement Task A (`registry-source.ts` verification) with TDD, commit.
2. Implement Task B (snapshot cache + envelope + route wiring across
   `module-distribution-port.ts`, `routes-module-registry.ts`, `platform-api-modules.ts`) with
   TDD, commit.
3. Run the Phase-2 e2e finish line:
   `pnpm test:integration tests/integration/module-distribution.e2e.test.ts` → exit 0.
4. Pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`) + `git fetch origin main &&
   git rebase origin/main`.
5. `coordinated-wrap-up`: full gate via the `verify-gate` skill (isolated DB, never unscoped),
   push, open a non-draft PR, report exact-head evidence to the coordinator. This is a
   backend/API-only change (no user-facing UI in this phase) — confirm with the coordinator
   whether a live-path UAT proof is still expected for this lane or deferred to the Phase-3 lane
   before claiming done.
6. Do not merge, move the board, or touch coordination files — report to the coordinator and stop.

**Reminders:** security tier — build defensively, no crypto/library error detail in product-facing
messages or logs. Never `git add -A`/bare-commit in this shared-checkout worktree — use the
`shared-checkout` skill's explicit-path procedure. Resolve `coordinator` fresh via `herdr agent
list` before messaging (confirm exactly one live match) — never reuse a pane number from this doc.
Sign every message to the coordinator with your own pane id.
