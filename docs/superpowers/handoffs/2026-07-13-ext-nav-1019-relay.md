# ext-nav-1019 relay — 2026-07-13

Bug #1019, sensitive tier. Worktree: this one (`.claude/worktrees/ext-nav-1019`, branch
`ext-nav-1019`, off `origin/main` @ `031eb67e`). Coordinator: label `Coordinator`, session
`58a78927-385c-4b1d-8fa0-94db20255d6f`.

**Plan (approved, do not re-litigate):**
`docs/superpowers/plans/2026-07-13-external-module-navigation-abi.md` — 8 TDD tasks. Read it by
task, not front-to-back.

**Coordinator approval, verbatim requirement:** D7 approved — leave `compatibility.jarv1s` and
`CORE_VERSION` unchanged at `0.1.0`. Hard requirement in exchange: `FORBIDDEN_FIELDS`
unknown-field rejection is now the SOLE fail-closed guard for an old core parsing this manifest
addition — Task 2 must NOT just delete the old navigation-forbidden test, it must add an
equivalent test against a still-forbidden field (plan uses `"routes"`). This is already written
into the plan's Task 2 — just follow it as written.

**Done (committed, green):**
- Task 1 — `packages/module-sdk/src/index.ts`: `ExternalModuleNavigationEntry` interface +
  `JsonJarvisModuleManifest.navigation?` field. Test in
  `tests/unit/module-sdk-external-types.test.ts` passing (17/17). Commit `14ac4047`.

**Next — Task 2 (start here):** `packages/module-registry/src/external/validate.ts` — positively
validate `navigation`, remove it from `FORBIDDEN_FIELDS`, add the `"routes"` regression test per
the plan's Task 2 exactly as written (full validation block, full test list, exact diffs — no
placeholders in the plan, copy verbatim). Then Tasks 3-8 in order (reconcile carry-through → shell
iconMap → serializeExternalModule → app-route-metadata Modules section → job-search manifest →
full-suite + verify:foundation).

**Process reminders:**
- `superpowers:test-driven-development`, self-driven task-by-task (executing-plans /
  subagent-driven-development disabled in this repo).
- Stage only each task's own files. No `git add -A`.
- Pre-push trio before any push: `pnpm format:check && pnpm lint && pnpm typecheck`, then
  `git fetch origin main && git rebase origin/main`.
- Relay again at the next 70% context-meter warning or on seeing a compaction summary — don't
  wait for a felt sense of degradation.
- After Task 8 passes: dev-UAT is a HARD exit gate (isolated stack e.g. `jarvis-uat-1019`, new
  port, Playwright under `scripts/uat/`, click-not-goto nav, assertions into scratchpad devproof
  dir) — see the original handoff doc
  `docs/coordination/handoff-1019-external-module-nav.md` (in the coord worktree, not this one)
  for the exact procedure, then `coordinated-wrap-up` (PR only, `Closes #1019`, report PR # to
  Coordinator, no merge).
- Guardrails: no Instance-modules settings UI edits (UX-owned #986), no `docs/coordination/`
  edits, no repo-wide `pnpm format`, escalate any shell/app-route-metadata conflict with #986 to
  the Coordinator instead of hand-resolving.
