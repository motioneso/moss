# #1110 app-map relay — checkpoint 21

Worktree: `~/Jarv1s/.claude/worktrees/build-1110-app-map`, branch `build/1110-app-map`, PR #1122.
Coordinator: re-resolve label fresh via `herdr pane list` before messaging (was
`Coord-1109-1110-g8` as of this checkpoint). **Never merge/board/close** — report only.

## Status: #1123 CI-repro task DONE. Verdict posted. Awaiting coordinator's next instruction.

Supersedes checkpoint 20's "LIVE STATE UPDATE" section — that harness (container/network/
poller) has been torn down; do not resume it.

### What happened

Fable's ruling on #1123 authorized one local CI-conditioned repro of the failing trio
(`multi-user-isolation.test.ts`, `account-self-deletion.test.ts`, `auth-bootstrap-recovery.test.ts`)
under matched constraints (2 vCPU taskset, fresh pgvector:pg17, CI defaults) on both branch head
and `origin/main`, to discriminate branch-defect vs infra-fragility and name the exact ~10.9s
constant.

**Result: does not reproduce on either ref.** Branch head `874759ec`: 27/27 pass, 68.09s.
`origin/main` `65b8a7f8`: 27/27 pass, 69.76s. `pg_stat_activity` polling showed no contention.

**Named constant:** pg-boss's internal `connectionTimeoutMillis` default = 10000ms
(`node_modules/pg-boss/dist/db.js`), never overridden by `packages/jobs/src/pg-boss.ts`. All 3
failing test files call `createApiServer()` without a `boss` override →
`apps/api/src/server.ts:195` spins a fresh real pg-boss client per test, hitting a
runner-load-dependent 10s ceiling. `git diff origin/main` on all implicated files = empty —
100% pre-existing, zero #1122 diff.

**Verdict: suite/infra fragility, not a #1122 defect.**

### Actions taken (all done, none reversible left to redo)

1. Posted full evidence as a comment on #1123:
   https://github.com/motioneso/Jarv1s/issues/1123#issuecomment-5000739946
2. Filed separate CI-infra issue with the same evidence + suggested fix direction (not
   implemented — needs its own spec/triage): #1124
3. Saved the pg-boss timeout invariant to agentmemory (project: jarv1s, type: bug).
4. Messaged coordinator (label `Coord-1109-1110-g8` at time of send — re-resolve fresh) with
   the terse verdict + both links. Delivered/queued, confirmed via `herdr pane read`.
5. Repro scratch infra (container `jarv1s-repro-pg`, network `jarv1s-repro-net`, `main-worktree`
   git worktree) all stopped/removed/confirmed gone.

## Next steps for whoever picks this up

- **Do not re-run the repro** — evidence is complete and posted, nothing more to gather.
- **Do not touch PR #1122 code** for this CI-flake path — verdict says infra, not defect.
- Await coordinator instruction. Most likely next task: the **deferred #1110 exit-criterion
  manual live-path walk** (3 spec §8 scenarios, bounded action logs and DOM/network assertions as a `gh pr comment`) —
  approved deferral to #1121 earlier in this relay chain, but must happen before #1122 merges.
  Not started yet.
- Standing rules unchanged: never merge, move board, or close anything; never `gh run rerun`
  again on PR #1122; never touch other agents'/production's containers/networks/worktrees on
  this shared host.
