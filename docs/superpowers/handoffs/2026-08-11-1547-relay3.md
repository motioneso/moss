# #1547 manual-run job idempotency — relay3 continuation

Spec: `docs/superpowers/specs/2026-08-11-1547-job-idempotency-race.md`
Branch/worktree: `build/1547-manual-run-job-idempotency` in this worktree.
PR: #1573 (open).
Coordinator: label "Coordinator" (session id resolved fresh via `herdr pane list`, do not
reuse a stale pane number).

## Why this relay happened

The **user** (not the Coordinator) sent QA feedback declaring PR #1573 RED, with an explicit
instruction: "Push a fix commit, confirm gate still green, then report back. I'll re-run QA
before merge." QA's BLOCKING finding: the spec (line 50) explicitly forbids touching
`tests/integration/external-modules-routes.test.ts`, but the prior relay (commit `82cc0f083`)
added the new race test there. relay2 (this session) fixed that violation. It hit the
context-meter 70% warning twice while doing so and is relaying per the mandatory trigger
(`.claude/skills/relay`), per box-wide CLAUDE.md context-diet rules.

## What's done (uncommitted in the working tree as of this doc — verify before assuming)

1. **Created** `tests/integration/manual-run-job-idempotency-race.test.ts` — a new dedicated
   integration test file (the spec's second compliant placement option, line 46: "at the
   implementer's discretion, if the boundary-forcing harness is heavy enough to deserve its own
   describe"). Full DB-epoch-grounded boundary-forcing harness (`waitForBoundaryApproach`,
   `waitForDbEpochAtLeast`, never `Date.now()` for the boundary math itself), synthetic
   `acme-widgets` fixture module with its own `acme-widgets.manual-race` queue, `appDb`
   `maxConnections: 2`. One `it()`: dedupes two genuinely concurrent manual-run calls that
   straddle a singleton bucket boundary. Read the file in full before touching it again —
   already verified correct against the spec in this session.
2. **Reverted** `tests/integration/external-modules-routes.test.ts` back to its pre-#1547 state:
   removed the `acme-widgets.manual-race` queue block, removed the `it("#1547 dedupes...")`
   test, removed the `readDbEpoch`/`waitForBoundaryApproach`/`waitForDbEpochAtLeast` helpers,
   restored `appDb` `maxConnections` from `2` back to `1`. `git diff --stat` confirms
   `1 insertion(+), 106 deletions(-)` — a clean revert, no residue. File now has its original 5
   tests only (list discovered, enable, 404 unknown, hide deny-listed, deny non-admin GET).
3. Production code (`apps/api/src/external-module-jobs.ts`,
   `packages/jobs/src/module-jobs.ts`, `packages/jobs/src/pg-boss.ts`'s `hasRecentJob`) was
   **already correct** from before this relay — untouched in this session, re-confirmed by
   reading it in full.
4. `pnpm typecheck` was kicked off in background — **check `/tmp/tc-1547.log`** for the
   `### FINAL rc=` line. If relay3 doesn't have that context, just re-run
   `pnpm typecheck` fresh (don't trust a stale log from a different session).

## What's NOT done — pick up here in order

1. **Confirm typecheck is green** (see above — check the log or re-run).
2. **Run lint** on the two changed files (`pnpm lint`, or scoped if faster).
3. **Run the new test file against an isolated test DB** to confirm it passes green. The
   red-run evidence from commit `82cc0f083` (1 failed/5 passed, on the now-reverted file)
   already satisfies the spec's "red then green" criterion — no need to reproduce red, only to
   record that the *relocated* test is green.
   - Use whatever this repo's per-file integration-test invocation is (check
     `tests/integration/test-database.js` / existing CI config for the DB env vars this suite
     expects — do NOT invent a new one). Do not run the full `verify:foundation` gate against
     the live dev DB — follow `verify-gate` skill: fresh `JARVIS_PGDATABASE`, exported, DROP+CREATE.
4. **Run `tests/integration/external-modules-routes.test.ts`** to confirm it's back to 5 green
   tests, unmodified behavior.
5. **Stage and commit by explicit path only** (never `git add -A` — shared checkout):
   ```
   git add tests/integration/external-modules-routes.test.ts tests/integration/manual-run-job-idempotency-race.test.ts
   git commit -m "fix(jobs): #1547 relocate race test out of forbidden external-modules-routes.test.ts"
   ```
6. **Update PR #1573's body** with the two QA non-blocking items:
   - (a) Add the recorded red-run evidence directly on the PR (commit `82cc0f083`, "1
     failed/5 passed"), not just a reference to the internal handoff doc.
   - (b) Disclose the first request's ~150ms-margin wall-clock dispatch (inside
     `waitForBoundaryApproach`'s `leadMs` window) as a known, deliberate deviation from the
     spec's stated DB-side-hold preference — note that the *second* request's placement is
     DB-boundary-confirmed via `waitForDbEpochAtLeast` (the actual boundary-forcing mechanism
     the spec requires), so the test's determinism does not depend on the wall-clock margin
     alone.
7. **Pre-push trio + rebase**: `pnpm format:check && pnpm lint && pnpm typecheck`, then
   `git fetch origin main && git rebase origin/main`.
8. **Push** the fix commit.
9. **Run the full isolated-gate-DB `pnpm verify:foundation`** via `scripts/run-gate.sh` (never
   bare — see `verify-gate` skill). Confirm exit 0.
10. **Report back to the user directly** (not the Coordinator — this is the user's explicit
    instruction from earlier in the conversation, override the usual coordinated-wrap-up
    "report to coordinator" default for this one turn) with: fix summary (test relocated,
    forbidden file reverted, PR body updated), gate result (exit code + pass counts), PR link.
    Match their instruction: "Push a fix commit, confirm gate still green, then report back.
    I'll re-run QA before merge."

## Traps already avoided — don't redo this work

- Do NOT re-add the race test to `external-modules-routes.test.ts` — spec line 50 forbids it,
  this is the exact violation being fixed.
- Do NOT bump `external-modules-routes.test.ts`'s `appDb` `maxConnections` above 1 — reverted
  intentionally, no reason to touch it again.
- Test 6 in `tests/integration/job-search-worker-surface.test.ts` was never touched — leave it
  alone (spec: "stays green and unmodified in intent").
- The new file's `waitForBoundaryApproach`/`waitForDbEpochAtLeast` pair IS the spec-required
  boundary-forcing technique — margin-only wall-clock scheduling was explicitly ruled
  insufficient by the spec (lines 64-72); do not simplify it away.
