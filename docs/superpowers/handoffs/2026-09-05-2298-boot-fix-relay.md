# Pull request 2298: unblock build, relay 1

Task brief: /home/ben/.coord-briefs/boot-fix-2298.txt (read it in full, it is the real
instructions). Working folder: this worktree, branch build/2282-news-sources, pull request 2298,
repository motioneso/moss. The feature itself is done and already has live proof on the pull
request; do not touch the development instance.

## Done so far

1. Split the oversized test file. It was one thousand and one lines against a one thousand line
   limit. Now three files: tests/integration/news-chat-tools.test.ts,
   tests/integration/news-chat-tools-write-tools.test.ts, and a shared setup class in
   tests/integration/news-chat-tools-harness.ts. Every test kept, none changed. This is already
   committed, commit d6e58f498 on this branch.
2. Ran the full integration test gate through the verify-gate skill, scoped to
   pnpm test:integration, using an isolated database, never the live one. It came back green:
   two hundred forty test files, two thousand three hundred fourteen tests passed, two skipped,
   nothing failed.
3. Checked the database change number this branch adds, number 0218, against everything on the
   main branch right now. It is still free; main's highest numbers are in the low 0220s and none
   of them is 0218. No renumbering needed. Do not touch this unless the rebase itself shows a
   real conflict on that exact file.
4. Fetched main fresh. Main has 9 commits this branch does not have. This branch has 34 commits
   main does not have. The rebase itself has not started yet.

## What is left, in order

1. Rebase this branch onto origin/main using the resolving-merge-conflicts skill. Resolve each
   conflict on its merits, keep this branch's work. Watch specifically for a conflict in the news
   module's database change files, though the check above says there should not be one.
2. After the rebase, if you see a package cannot be found error when test files load, that means
   the rebase left a workspace link missing. Fix is a fresh pnpm install, then rerun.
3. Run the full gate again through the verify-gate skill after the rebase, the same careful way
   described above. There is a new rule from Ben as of today: only one full gate may run at a
   time across all lanes, so check with the coordinator or check herdr pane list before starting
   one. If the machine is short on memory, the gate can sit alive but stalled for a long time; if
   it goes fifteen minutes with no new log output, stop it by its exact process number, never by
   matching a name, tell the coordinator, and wait rather than restart into a starved machine.
   This happened three times already in this run before it finally passed.
4. Push the branch. A force push is expected and normal after a rebase. Only force push this one
   branch, build/2282-news-sources.
5. Take pull request 2298 out of draft.
6. Watch its checks with a background command that ends on its own, not a polling loop, and tell
   the coordinator as soon as checks start appearing, and again when they finish, with the plain
   result.

## Rules to keep following

Plain English in every message a human reads, no jargon, no invented shorthand, plain keyboard
punctuation, at most one piece of code formatting per sentence. Pass this rule on to anyone you
spawn. Sign every message to the coordinator with your own pane identifier, found through
herdr pane list matched to your own session, not a name you picked. Never git add with a dot or
with all, never a bare git commit, use the shared-checkout skill first since other sessions share
this checkout. Never touch docs/coordination. Waits are event driven, never a polling loop. Do
not touch the development instance until the coordinator says Ben is done checking it by hand.

The coordinator's agent name is "coordinator" and is reachable with
`herdr agent prompt coordinator "your message"`. It has changed panes several times already in
this run; that is normal, always send to the name, not a remembered pane number.
