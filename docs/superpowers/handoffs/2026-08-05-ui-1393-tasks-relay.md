# #1393 Tasks + Notifications UI consolidation — relay handoff (v3)

Supersedes v2 (v2 correctly recorded Tasks 0-2 + part of 3; this version adds a full
selector-triage starting point for the CSS-split, gathered by grep not manual reads — v2's
"read the 3 files fresh" instruction burned a whole context on manual cataloguing without
finishing it; don't repeat that). Branch `feat/1393-ui-tasks`, worktree
`/home/ben/Jarv1s/.claude/worktrees/ui-1393-tasks`, based on `origin/main` @ `5ff2423d`.
Tree clean except `.claude/context-meter.log` (harness noise, don't stage it).

**v3 relay reason**: third consecutive context-meter 70%+ checkpoint in this lane without
landing Task 3's CSS-split. Per the `relay` skill, spawning a successor now instead of a
fourth compact-and-continue cycle in the same session lineage.

Plan: issue #1393 comment `5187960649` (build spec, Task 0-6), sign-off `5188097889`
(approves 4 visible-change amendments + 3 build-time corrections, flags 2 wrong D6 items).
**Do not re-plan.** Standing instruction: chain straight through Tasks 1-5, no stopping for
approval, open PR at the end (**do not merge it**). Coordinator pane label "DESIGN ELEMENTS"
(re-resolve by label — pane numbers reflow; if no such pane is found, report to Ben directly).

## Done (commits, oldest→newest — don't redo)

`96aa0ae5` Task 0 (empty-state capture scenarios) · `922f7315` Task 1 (EmptyState/Field/
FormLabel added to `@jarv1s/ui`) · `21b194f3` Task 2 (notifications page migrated) ·
`c6eaaf07` Task 3 partial (task-capture.tsx, task-list-view.tsx buttons, task-details-
sections.tsx buttons/chips) · `41577e5f` Task 3 (tasks-page.tsx → Segmented/Chip/EmptyState)
· `c6a5181b` Task 3 (task-details-dialog.tsx → Dialog/Field/FormLabel/Segmented/Button,
Select import moved `@jarv1s/settings-ui`→`@jarv1s/ui`).

**Confirmed by grep this session: `tk-panel` and `tk-person` do NOT appear in
`task-details-dialog.tsx`.** The plan's "tk-panel×6→Card, tk-person×3→Avatar" items belong
to OTHER files — `tk-panel` lives in `task-list-view.tsx` (group wrapper), `tk-person`
lives in `task-details-sections.tsx` (`AssignedPersonField`). Both already ruled below.

Two deliberate skips in `tasks-page.tsx`, judged correct, don't revisit: `tk-tagmenu`
(`ListFilterMenu`/`TagFilter`) and `tk-listbtn` stay raw `tk-*`. `Menu` closes on every
`onSelect` (incompatible with these cycling tri-state menus); `MenuItem` has no
`className` hook for per-item state; `Button` can't express the combobox trigger's
`is-open` visual state without a component source change. Outside guard 5's scope (scans
`jds-*` only) — fine to leave raw, handle as CSS-only in Task 3's CSS-split.

## Ruled, not yet executed — do these as part of the Task 3 CSS-split (no re-approval needed)

Both are CSS-relocate-only (rename the class, author in `packages/ui`, update the TSX
`className`), **not** component conversions — neither was named by the plan as a
component-conversion target by class name (unlike `tk-person`/`tk-modal`/`tk-field`/
`tk-flabel`/`tk-native`, which the plan counted explicitly):

- `Ava` helper's `tk-ava` class, and the `tk-person`/`tk-person__nm`/`tk-person__me`
  wrapper in `task-details-sections.tsx` (`AssignedPersonField`, used again inside
  `TaskActivityPanel`). Keep the local `Ava` component itself as-is, just rename the CSS.
- `tk-listchip`, `tk-metatag`, `tk-panel`(+`__head`/`__dot`/`__name`/`__count`/`__body`),
  `tk-effort-fill` in `task-list-view.tsx`. Same treatment.
- Note: `task-list-view.tsx` also owns an 11-selector `tk-task` row family (`TaskRow`),
  distinct from Today's `jds-task`. **`jds-task*` is reserved for Today** (memory:
  `ui-1393-tasks-scoping.md`) — do not rename `tk-task`→`jds-task`. Before deciding its
  final class name, check `components-wellness-1/2.css` or `components-chat.css` for
  precedent: did #1392/#1396 keep a section's own prefix (e.g. `jds-tasktask` or similar)
  when CSS-only-authoring into `packages/ui`, or something else? Resolve when you reach it.

## CSS-split starting material (gathered by grep this session — don't re-read the 3 files
in full, work from this + targeted greps instead)

Banned-property declaration counts (grep `^\s*(color|background|background-color|
background-image|border|border-color|border-radius|font-family|font-size|font-weight|
box-shadow|outline|fill|stroke|filter)\s*:`): `kit-tasks.css` 207, `tasks/tasks.css` 50,
`kit-tasks-modal.css` 194. Every one of these needs its selector either deleted (dead) or
relocated to `packages/ui/src/styles/components-tasks.css` — the app-side files must end
up with zero matches for that regex (that's what guard 4 / `check:design-tokens` checks
once these 3 files are registered).

**Confirmed live via direct TSX reads this session** (`task-list-view.tsx`,
`task-details-sections.tsx` both read in full, content matches prior catalogue exactly —
no drift): `tk-panel`+`__head`/`__dot`/`__name`/`__count`/`__body`, `tk-foot`+`.ic`,
`tk-task`+`__bar`/`__check`/`__main`/`__title`/`--done`/`__meta`/`__right`/`__open`,
`tk-meta-due`+`.ic`+`--overdue`/`--today`, `tk-listchip`+`__dot`, `tk-metatag`,
`tk-task__src`, `tk-effort-fill` (defined in `tasks.css`), `tk-ava`, `tk-peoplefield`,
`tk-person`+`__nm`/`__me` (no `__x` — no remove button rendered), `tk-tagedit`,
`tk-tagsugg`+`__btn`, `tk-subs`, `tk-sub`+`__box`/`--done`/`__rm`/`__add`, `tk-subadd`,
`tk-statusctl` (wrapper only — `__main`/`__more` are dead, see below),
`tk-statusctl__menu`+`tk-tagmenu`+`tk-tagmenu__item`, `tk-activity`, `tk-act-list`,
`tk-act`+`__body`/`__head`/`__who`/`__when`/`__text`, `tk-act-composer`, `tk-act-hint`.
**Confirmed dead** (component-superseded, no longer referenced in these two files):
`tk-statusctl__main`/`__more` (+ `.is-done`/`.is-archived`/adjacent-sibling variants —
replaced by `<Button variant="quiet"|"secondary">` in `TaskStatusControl`), `tk-act__bot`,
`tk-act--jarvis`, `tk-act__typing`, `tk-dots`/`@keyframes tk-pulse`, `tk-act-mention-menu`,
`tk-act-empty` (replaced by bare `<EmptyState title="No activity yet...">`), `tk-act-send`
(replaced by `<Button icon={<ArrowUp/>} variant="quiet".../>`). These 8 are either
component-superseded or scaffolding for a feature that was never built (typing indicator,
@mention menu, bot badge) — since `TaskActivityPanel`'s actual JSX has zero markup for
any of them, treat as dead and delete; this isn't a "future feature" judgment call, it's
CSS with no corresponding markup anywhere in the file that renders it.

**`tasks-page.tsx` confirmed** (diff read this session, matches prior record exactly):
`tk-empty`/`__mark`/`__title`/`__sub` fully replaced by `<EmptyState>` — dead, delete.
`tk-bar__sep` still present (small `background` decl — relocate). `tk-activetags`+`__lbl`/
`__clear`/`__note` still present (still raw, `Chip` only replaced the individual chips
inside it, not the wrapper — relocate+rename). Two deliberate raw-`tk-*` skips already
ruled above (`tk-tagmenu`/`tk-listbtn` families) — CSS-only relocate, no markup change.

**Still needs a grep before finalizing disposition** (not yet checked this session —
each is a single `grep -rn "<selector>" apps/web/src/tasks/*.tsx
apps/web/src/notifications*.tsx` against current branch state, cheap, do these before
writing `components-tasks.css`): `tk-modal-scrim`, bare `tk-modal` (the `__head`/
`__eyebrow`/`__title`/`__x` children ARE still live — passed via Dialog's `title` prop, per
prior segment's dialog.diff read — but the outer scrim/modal/`__body`/`__foot` wrapper
selectors may be Dialog-superseded), `tk-titlein` vs `tk-modal__titlein` (which survived
the dialog conversion, if either), `tk-textarea` (Notes field — did it get renamed to
`jds-input` alongside the 3 date inputs, or is it untouched?), `tk-people` (bare wrapper —
not seen in `AssignedPersonField`'s actual JSX, likely dead), `tk-people__menu`/`__item`
(a picker dropdown not present in the current `AssignedPersonField` — likely dead, same
"no markup renders it" logic as the activity-panel scaffolding above), `tk-effort`/
`tk-effort-dots`/`tk-effort-label`/`tk-effort-time` (parallel effort-readout designs in
`kit-tasks.css` alongside the confirmed-live `tk-effort-fill` in `tasks.css` — likely dead
alternate designs), `tk-div` (criticality divider — presumed used by `TaskMatrixView`, not
yet checked), `tk-matrix`/`tk-quad`+variants (Eisenhower matrix — `TaskMatrixView`
component file not yet read this lane at all), `tasks--panels`/`tasks--compact` modifier
classes (density/layout toggles — confirm still applied before keeping their layout-only
rules; note `tasks--compact`'s font-size overrides trip the banned-property regex even
though they're a layout toggle, so may still need relocating despite being "layout intent").

**`jds-task` naming collision — resolve before naming the CSS-only class for the `tk-task`
row family**: `jds-task*` is reserved for Today's section (memory
`ui-1393-tasks-scoping.md`). Check `packages/ui/src/styles/components-wellness-1.css` or
`components-wellness-2.css` or `components-chat.css` for how #1392/#1396 named a CSS-only
class for a section-owned family during authoring (one grep: `grep -n "^\.jds-" 
packages/ui/src/styles/components-wellness-1.css` and skim the naming pattern) — likely
something like a compound name, not a bare reuse of another section's prefix. Apply the
same pattern here.

## Not started

1. **Grep-confirm the "still needs a grep" list above** — targeted single-selector greps,
   not full-file reads. Budget: ~15 small `grep -n` calls, not 3 file reads.
2. **Create `packages/ui/src/styles/components-tasks.css`.** Move component-superseded
   dead selectors → delete (e.g. `.tk-statusctl__main`/`__more`+variants, `.tk-modal*`
   family now that Dialog owns its CSS, `.tk-flabel`, `.tk-native` — confirm no more TSX
   references first). Relocate+rename unmapped visual-property selectors (the two ruled
   items above, plus whatever `kit-tasks.css`'s fresh read turns up — `tk-tagedit`,
   `tk-tagsugg__btn`, `tk-subs`/`tk-sub` family, `tk-people__menu`/`tk-people__item`,
   `tk-activity`/`tk-act*` family, `tk-dots`, `tk-act-composer`, `tk-act-hint` are
   candidates flagged by an earlier read of `kit-tasks-modal.css` — re-verify each still
   exists and still carries banned properties before moving it).
   `BANNED_VISUAL_PROPERTIES` (`scripts/check-design-tokens.ts:53-69`): `color`,
   `background`, `background-color`, `background-image`, `border`, `border-color`,
   `border-radius`, `font-family`, `font-size`, `font-weight`, `box-shadow`, `outline`,
   `fill`, `stroke`, `filter` — matched by property name regardless of value.
   Strip all 3 section CSS files to layout-only once their visual rules are relocated.
3. **Property-by-property split check**: zero shared CSS properties between each section
   CSS file and its `packages/ui` destination.
4. Task 4: dead-selector burn-down in root `apps/web/src/styles.css`, diffed against
   **BASE commit `5ff2423d`** (never plain `git diff` — a relocated selector reads as a
   deletion otherwise). Keep: `.empty-state`, `.notification-link`, `.notification-badge`,
   `page-stack`/`page-heading`/`eyebrow`. **Do not delete `.notification-row.unread`**
   until the `10-notifications` capture proves `.jds-task--unread` renders correctly —
   kill gate (stop, report to coordinator, don't self-approve) if it doesn't.
6. Task 5: register both guard lists in the same PR — `MIGRATED_SECTION_CSS_FILES` 11→14
   (`scripts/check-design-tokens.ts`, add `kit-tasks.css`, `kit-tasks-modal.css`,
   `tasks/tasks.css`; do NOT add root `styles.css`); `MIGRATED_SECTION_PATHS` 49→56
   (`scripts/check-migrated-sections.ts`, add the 7 section TSX paths: 6 in
   `apps/web/src/tasks/` + `notifications-page.tsx`).
7. Task 6: run every guard/gate **unpiped**, echo exit codes explicitly
   (`cmd > /tmp/x.log 2>&1; echo "EXIT=$?"` — never pipe through `tail`/`grep`, the pipe's
   exit code masks a red gate as green): `check:design-tokens`, `check:migrated-sections`,
   `check:ui-classes`, `check:ui-catalogue` (after `pnpm build:ui-catalogue`),
   `verify:foundation` (freshly exported gate DB only — never the live dev DB, see
   CLAUDE.md). Browser assertion changes must be confined to the 4 approved amendments
   from sign-off `5188097889` (reread that comment for the exact 4 — roughly: empty/
   loading states adopt `jds-empty`, notifications toolbar picks up component metrics,
   `.tk-native`→`.jds-input`, 2 extra dead selectors). **Kill gate** on any other visible
   change or lost unread-vs-read styling. Plus live-path proof on a dev instance (tasks
   list+matrix, details dialog, edit date field, tag menu, change status; notifications
   filter/mark-read/both empty states) recorded on the PR.
8. Post the required note on issue #1405 (notifications adopted raw `jds-task`, family
   gained an authored `--unread` modifier).
9. When green: `git fetch origin main && git merge origin/main`, pre-push trio
   (`pnpm format:check && pnpm lint && pnpm typecheck`), push, open PR against `main` with
   a release-note-style user-facing summary, post the live-path proof + #1405 note, report
   PR number + measured guard numbers (should land at 14 and 56) to the coordinator pane
   ("DESIGN ELEMENTS", re-resolve by label) or directly to Ben if no such pane is found.
   **Do not merge, close the issue, or move the board** — coordinator's job.

## Reminders that bit before

- Shared checkout: never `git add -A`/`git add .`, never bare `git commit` — explicit
  paths only. `git diff <path>` then `git show --name-only HEAD` after every commit.
- `Button` is not forwardRef-wrapped (React 19) — use `useId()` + `document.getElementById`
  for focus-restore, not refs (already the pattern in `TaskStatusControl`).
- Relay again on the next context-meter 70% warning or compaction summary — don't wait for
  felt degradation. Bring state fully current (commit + update this doc) before relaying.
