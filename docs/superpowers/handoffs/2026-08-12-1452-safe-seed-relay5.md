# Relay 5 — fix-1452-safe-seed

**Issue:** #1452 (part of #1440). **Risk tier:** `routine`. Relaying at the context-meter 70%
warning, per brief — this time WITH code committed and gate green, not another over-read.

**Worktree:** `/home/ben/Jarv1s/.claude/worktrees/coord-overnight-20260810/.claude/worktrees/fix-1452-safe-seed`
**Branch:** `fix-1452-safe-seed`. `node_modules` already installed — do NOT re-run `pnpm install`.

**Coordinator:** label `Coordinator` — re-resolve session id via fresh `herdr pane list` before
messaging (never a cached pane id/session id from this doc).

## Done

1. Everything relay3/relay4 already listed as done (plan committed at `e87fda722`, no open design
   questions, no blocker).
2. **Task 1 built and committed** (`28ef3076b`): `tests/uat/specs/1452-briefing-live-content.uat.spec.ts`.
   Followed the plan's `## Task 1` section + relay4's "Extra findings" verbatim (bare-level signup,
   onboarding-wizard skip, `.jds-brief` kicker-scoped locator, `test.setTimeout(180_000)`, 30×2s
   poll loop, cookie-authed `page.evaluate` fetch pattern, `/Jarvis/i` residue assertion).
3. **Task 2 built and committed** (`5a6b27895`): the two TSV rows added verbatim to
   `.claude/skills/coordinate/uat-trigger-map.tsv`.
4. **Format fix, own file** (`97f1dc03c`): the new spec file had prettier violations; fixed and
   committed separately from Task 1's content commit.
5. **Format fix, pre-existing plan doc** (`c7c653bb2`): `pnpm format:check` was also failing on
   `docs/superpowers/plans/2026-08-12-fix-1452-safe-seed.md` — a violation pre-dating this relay
   (committed at `e87fda722`), unrelated to Task 1/2. Confirmed whitespace-only (prettier
   normalizes numbered-list continuation indentation; CommonMark's lazy-continuation rule means
   this doesn't change rendering) and fixed it in its own commit so `pnpm verify:foundation` can go
   green for anyone on this branch. **Flagging this explicitly since it wasn't this relay's file to
   start with** — if that call was wrong, revert `c7c653bb2` alone.
6. `pnpm typecheck` standalone: exit 0.
7. Pre-push trio: `pnpm format:check` exit 0, `pnpm lint` exit 0, `pnpm typecheck` exit 0 (all
   re-verified after the format commits).
8. `git fetch origin main && git rebase origin/main`: no-op, branch already current.
9. **Full isolated-DB gate run, green**: `GATEDB=jarvis_gate_1452safeseed` (dropped after use, per
   `gate-db-isolation-mandatory` — never left behind). `pnpm verify:foundation` → `### FINAL rc=0`.
   190 test files, 1890 tests passed, 2 skipped. ~26 min wall time (`test:integration` alone is
   ~24 min — expect this, don't treat a long-quiet gate run as hung).

## Not done — resume here

1. **Live-path UAT run** (this is the actual proof step, separate from `verify:foundation` which
   does NOT include `test:uat`): run
   ```bash
   pnpm test:uat -- 1452-briefing-live-content > /tmp/1452-uat.log 2>&1; echo "EXIT=$?"
   ```
   in the background (it spins up the UAT harness's own ephemeral Docker stack — do not run this
   inline/foreground, it's long and output-heavy; use `run_in_background` + a sentinel + Monitor,
   per the box-wide context-diet rule). Expect EXIT=0. The screenshot lands at
   `test.info().outputPath("briefing-card.png")` inside the Playwright test-results dir — locate it
   via `find` scoped to the UAT run's output dir once the run finishes, don't grep/cat the whole
   Playwright HTML report.
2. If the UAT run's poll loop times out reproducibly (not a one-off flake, twice in a row): **stop
   and escalate to coordinator per the plan's "Kill gate" section** — that's new information about
   the real worker path, not a test-authoring bug, and is out of this task's scope to fix.
3. Push the branch, open the PR (title referencing #1452), post live-path proof via
   `gh pr comment` — the rendered-card screenshot from step 1, plus a line confirming the shared
   dev DB was left untouched (the UAT harness's own `down -v` + `assertNoLeakedResources()`
   teardown IS that proof — cite plan seam #1, no manual cleanup needed or expected).
4. Report to coordinator: Task 1 + Task 2 done, gate green, live-path proof posted, PR link.
5. **Never merge, never touch board/milestones/`docs/coordination/`** — that's the coordinator's
   call per standing process.

## Verification commands already run (all green)

```bash
pnpm format:check   # EXIT=0 (after c7c653bb2)
pnpm lint            # EXIT=0
pnpm typecheck        # EXIT=0
pnpm verify:foundation  # rc=0, isolated GATEDB, dropped after
```

Still to run: `pnpm test:uat -- 1452-briefing-live-content` (see "Not done" #1).

Never end your turn between steps. Relay again only on the next 70%-warning or compaction summary.
