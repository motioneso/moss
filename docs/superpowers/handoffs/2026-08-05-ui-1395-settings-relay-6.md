# #1395 Settings relay 6 → 7 handoff

Context-meter forced checkpoint at 70%. Tree is clean except this doc. HEAD `c672168e`.

## Read order (unchanged)

1. This doc
2. `gh api repos/motioneso/Jarv1s/issues/comments/5195260909 --jq .body` (plan)
3. `gh api repos/motioneso/Jarv1s/issues/comments/5195303202 --jq .body` (approval — wins on conflict)
4. Prior relay docs (`2026-08-05-ui-1395-settings-relay-5.md` etc.) only if you need Task 0-3c
   detail; not required for what's left.

## Done: Tasks 0-3d (all of Task 3, including 3d, complete)

Tasks 0-3c: see relay-5 doc, unchanged.

Task 3d, this session, commit `c672168e` (8 files):
- **IconButton** (2 refs, coordinator-verified count): `settings-admin-panes.tsx` (menu trigger,
  needed `ref`) and `settings-ai-admin-pane.tsx` (remove button, no ref) both converted.
- **Segmented**: extended `packages/ui/src/segmented.tsx`'s object-form `SegmentedOption` with
  optional `disabled`/`title` per the coordinator's Ruling 1 (additive, bare-string form
  unchanged). Converted `settings-appearance-pane.tsx`'s color-mode control. Verified the CSS
  (`components-forms.css:248-249`) keys active styling off `[aria-pressed="true"]` **or**
  `.is-active` — the shared component's `aria-pressed` already covers it, no visual regression.
  Ran `pnpm build:ui-catalogue` after — `catalogue.json`/`OPTIONS.md` regenerated byte-identical
  (these are boolean/ref props, not enum-style variant/size/tone options the catalogue tracks), so
  nothing new to commit there. `check:ui-catalogue` still green.
- **Ruling 2a** (ButtonLink): `settings-profile-subviews.tsx` download anchor → `ButtonLink` with
  `icon={<Download size={15}/>}`, `download` passes through via the existing rest-spread.
- **Ruling 2b** (Button ref): added `readonly ref?: Ref<HTMLButtonElement>` to **both**
  `ButtonProps` and `IconButtonProps` in `packages/ui/src/button.tsx` /
  `packages/ui/src/icon-button.tsx`. Picked this over restructuring call sites because the exact
  same problem occurred in **two** places this session (admin-panes' `menuTriggerRef` for the
  IconButton conversion, and skills-pane's `listActionRef` for the Button conversion) — one type
  addition covers both, no runtime change (React 19 handles `ref` as a plain prop; the existing
  `{...rest}` spread in each component already forwards it to the underlying `<button>`). Converted
  `settings-skills-pane.tsx`'s upload button to `<Button ref={listActionRef} ...>`.
- Verified: `npx tsc --noEmit` clean from `apps/web/`. Diffed every changed file against intent
  before committing (shared-checkout discipline). `git show --name-only HEAD` confirmed the 8
  files match.

**Task 3c/3d is now fully closed** — no more raw `jds-btn`/`jds-iconbtn`/`jds-segmented` refs
inside the scope of this section (settings panes + the shared primitives they use). The two
out-of-scope exclusions stand: `packages/settings-ui/src/priority/index.tsx:276` (IconButton, same
exclusion as Task 3c) and `packages/settings-ui/src/index.tsx:80-98` (package's own local
`Segmented` definition, same precedent as the Badge/BadgeTone duplicate ruling).

## Next: Task 3e — guard-6 burn-down (NOT STARTED)

I did not find the actual guard script before the checkpoint hit. What I ruled out:
`scripts/check-design-tokens.ts` is guard 4 (banned visual properties in migrated CSS files, keyed
off `MIGRATED_SECTION_CSS_FILES`) — it is where `--st-swatch` needs to go in its `allowList`
(currently has `--tk-ava-bg`, `--tk-ava-fs`, `--tk-swatch` as the Tasks-section precedent; add
`--st-swatch` there, scoped to settings, following the same out-of-scan-root comment pattern if
applicable), but it is **not** where "5 static inline styles" get checked — that's a different
guard I hadn't located yet. `INLINE_STYLE_EXEMPT_PATHS` lives in `scripts/check-migrated-sections.ts`
(confirmed via grep, not yet read) — start there; it's almost certainly also where the "5 static
inline styles" count is enforced, or adjacent to it. **Do not add anything to
`INLINE_STYLE_EXEMPT_PATHS`** — the task is to fix the 5 inline styles, not exempt them (binding
constraint from the coordinator).

The "2 swatch sites" are presumably the `--swatch`→`--st-swatch` rename sites in the settings CSS
(open question 1 in the approval: use `--st-swatch`, not `--swatch`; do NOT rename the existing
merged `--tk-swatch` from Tasks). Find them with
`grep -rn "swatch" apps/web/src/settings/ apps/web/src/styles/settings-*.css`.

Find the "5 static inline styles" with `grep -rn "style={{" apps/web/src/settings/` (or the
project's existing inline-style guard pattern — check `check-ui-classes.ts` and
`check-migrated-sections.ts` for how prior sections did this burn-down; Wellness and Onboarding
both did one, per memory `[Wellness](ui-1392-wellness-progress.md)` — the property-by-property
split-check precedent is there).

## Then: Task 4 CSS split, Task 5 guard registration, Task 6 e2e, Task 7 gate + PR

All unchanged from relay 3/4/5 docs and the coordinator's relay-6 brief. Key points, restated
because they're easy to get wrong:

- **Registration**: merged main was 14 CSS / 56 TSX as of relay 5's briefing; target delta +4
  CSS / +31 TSX — but **recompute absolutes against current `origin/main`** (`#1394` Modules may
  have landed at 23/59 by now — check `git fetch && git log origin/main -1` and re-derive). Union-
  resolve `packages/ui/src/styles.css` against #1394's three added `@import` lines
  (`components-news.css`, `components-sports-1.css`, `components-sports-2.css`) if that PR merged
  first. Prove the registration bit isn't a no-op: run each guard red-before/green-after on an
  injected violation, and confirm every added path resolves to a real file on disk (`test -f`).
- **Gate discipline**: never pipe a gate command through `tail`/`grep` (pipeline exit code lies).
  `pnpm verify:foundation` against a **freshly self-exported gate DB**, never live dev. It excludes
  `test:e2e` — run that separately, unpiped: `pnpm test:e2e > /tmp/e2e-1395.log 2>&1; echo "EXIT=$?"`.
  Grep `tests/e2e` for assertions keyed to any control converted this session (IconButton
  aria-label text, Segmented color-mode buttons, ButtonLink download, Button upload) BEFORE
  treating the gate as sufficient proof — do this even though it's after the fact for this
  session's conversions, since e2e wasn't checked for them yet.
- **PR body must state**: CI's "Verify foundation and app" runs the browser suite, the local gate
  does not; the standing split-check result (parse each layout CSS file against its
  `packages/ui` destination property-by-property, zero shared properties); that browser assertion
  is corroboration not proof (one state per section — `Switch`/`Segmented`/`Select`/`Badge`/
  `Avatar`/`Indicator`/`ComingSoon` need the live-path walk instead, or a second capture per
  Ben's addition #2 in the approval — a dialog shot, since decision 3 carries real visual risk);
  which `#1416` importers this closes (settings panes) and which it does not (six module packages
  + `task-details-dialog.tsx`) — post that same note as a factual comment on #1416, do not
  re-scope it.

## Reminders that still apply

Never pipe gate output through `tail`/`grep`. Explicit-path commits only in this shared tree (never
`git add -A`/bare `git commit`); `git diff` before, `git show --name-only HEAD` after every commit.
Re-check `herdr pane list` before any tree-wide action. If another context-meter checkpoint hits:
write+commit a handoff doc and tell the coordinator — do not spawn a successor as an Agent-tool
subagent, use `herdr-handoff`. Report to the coordinator via `herdr-pane-message` (pane
"DESIGN ELEMENTS", w1:p1) — not to Ben. Do NOT merge the eventual PR and do NOT close the issue.
