# Handing off: rebase of pull request 2279 onto main

Written by rebase-2279 (pane w1:pNG) at seventy percent context, following the relay skill.
This is a build lane, not the coordinator. Depth: this is the first relay for this task, so the
next session should be named rebase-2279-relay1.

## The job

Working folder: this worktree, branch feat/email-chief-of-staff, pull request 2279, repository
motioneso/moss. The feature is finished and was fully tested before this task began. Two things
are blocking the merge, and neither is about the code: the branch conflicts with main, so GitHub
is not running any checks, and the pull request is still marked as a draft.

Steps, in order, and where we are:

1. Install packages - done.
2. Fetch main fresh and rebase this branch onto it, resolving every conflict on its merits -
   in progress, see below.
3. Watch for two known traps: a database change file number that collides with one main has
   since taken (rename ours, never edit a file already applied), and a rebase that leaves a
   package link missing (fresh package install, then try again).
4. Run the full test gate through the verify-gate skill - not started.
5. Push the rebased branch, force push allowed only for this one branch - not started.
6. Take the pull request out of draft - not started.
7. Watch its checks with a background command that ends on its own - not started.

Report progress to the coordinator using the command `herdr agent prompt coordinator "<message>"`,
signing every message with your own pane identifier.

Standing rules: never use git add with a dot or with all, never a bare git commit (this checkout
is shared with other sessions - use the shared-checkout skill before any commit), never touch the
docs/coordination folder, and do not touch, restart or start a server on the usual ports because
Ben is checking the development instance by hand right now.

## Where the rebase actually is right now

A rebase is currently open in this worktree (`git status` will show "interactive rebase in
progress"). Eighteen of thirty one steps are done. Do not abort it - a lot of careful conflict
resolution is already in it. Resume with normal `git add <file>` on each resolved file followed
by `git rebase --continue`.

The current conflict is in one file: `tests/integration/foundation-schema-catalog.test.ts`. It is
a straightforward case of the first known trap: this branch's commit
`6f0ec9cea` adds a database change file numbered 0215 for a new email thread lookup table, but
main has since taken 0215 for something else (a connector sync change). The fix is to rename our
new file to the next number nobody has used yet, and update every place that names it.

Files currently staged or waiting in this one conflicted commit:
- `packages/email/sql/0215_email_thread_lookup.sql` - the new file; rename it to the next free
  number (check the highest number in use across the whole repository first - it was 0222 as of
  this writing, so 0223 is likely free, but check again since other work may have landed since).
- `packages/email/src/manifest.ts` - lists the change file by name, needs the new number.
- `packages/email/src/repository.ts`, `packages/email/src/index.ts`,
  `packages/email/src/thread-provider.ts`, `tests/unit/email-thread-provider.test.ts` - already
  staged clean, no rename needed inside them.
- `tests/integration/foundation-schema-catalog.test.ts` - the conflicted file itself; it lists
  every change file by number and name in order. Resolve the conflict by keeping both sides'
  entries in number order, using the new number for our file instead of 0215.

Important: a later commit still waiting in this same rebase, `d97e9336fc72`, already renames this
exact file from 0215 to 0220 for a similar reason from an earlier point in time (its message says
"0214 to 0219 are claimed by other open pull requests"). By the time you reach that commit it will
likely produce another version of the same conflict, or become a no-op. Resolve it the same way:
pick whatever number is actually free against main at that moment, never reuse a number, and never
edit a database change file that machinery has already marked as applied.

## A pattern you will keep seeing as you resume

Commits early in this rebase (nine of them, already resolved and applied) were all about
recognizing sign-in code emails and skipping them before the AI model ever sees them. It turns
out someone already landed the exact same finished feature on main under a different pull request
number, file for file identical in its final form. Each of those nine commits in this branch was
an intermediate step toward that same finished feature, so every one of them conflicted with what
main already had. The resolution used throughout was: keep the side already in place (which came
from main), discard the older intermediate version being replayed. This was checked carefully
each time by comparing the text actually being kept against main's real current file, not
assumed. That pattern is now finished - the shared commit both sides have in common
(`05390bd00`) has already been passed. Everything after that point is new work unique to this
branch (thread judgement, a first-pass sorting gate, and so on), so conflicts from here on are
likely to be genuine and need real judgement, not this shortcut.

## Verification before trusting any of the above

Don't take this document's word for exact numbers or file states - re-check with
`git status`, and re-check the highest database change file number in use with a fresh search,
since time has passed and other work may have landed.
