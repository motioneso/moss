# Finish lane for PR 2280, relay 1 (2026-09-05, about 01:25 PDT)

Pointer-style continuation for the successor. Facts live in the files named here; do not re-derive.

## Read these, in this order

1. `/home/ben/.coord-briefs/boot-finish-2280.txt` - the task brief. Follow it exactly. It carries
   the plain English rule and the standing rules (ports never to touch, no pipes on gates, etc).
2. `/tmp/finish-2280-state.md` - the live state doc: every verified fact and the exact commands.
   Keep updating it as you work.

## Where the work stands

- Step 1 (install): done. Do NOT run it again.
- Step 2 (rebase onto origin/main): done, head `0bdf04179`, not yet pushed. The one conflict was
  in packages/ai/src/model-discovery.ts: main's new release-date argument and this branch's
  command-line flag were both kept; prettier is clean on that file.
- Step 3 (gate): scoped unit tests are already green (152 files, 1555 tests, exit 0, log
  /tmp/finish-2280-unit.log). The static gate (lint, format check, typecheck) has NOT run yet. It
  was waiting on another lane's gate per the verify-gate skill's stagger rule. Check that gate's
  status first, then start ours through the verify-gate skill with the narrower script
  `verify:static`, and wait with the background follow command the skill describes.
- Steps 4 to 7: not started. The PR comment for the rebase must list the conflict resolution above
  and the gate results with exit codes.

## Live proof instance (step 5) is already running from this worktree

- runner socket /run/jarv1s/finish-2280.sock, api on 3280, web on 5280. Pids and process groups
  are in /tmp/finish-2280-pids.txt; stop with `kill -- -<pgid>`, never by name.
- Logs: /tmp/finish-2280-runner.log, /tmp/finish-2280-api.log, /tmp/finish-2280-web.log.
- Sign-in through the web on 5280 works (proved with curl: sign-in 200, session 200).
- The browser script /tmp/web-search-liveproof/r3/step1.py failed only because it waited a fixed
  3 seconds; the app shows "Loading Moss" longer on first load. Wait for the app shell to appear
  (long timeout) before reading pages. Ignore the Agentation toolbar text in the page body.
- Remaining proof steps and the exact question to ask are in the state doc under "proof plan".

## Rules the successor inherits

PLAIN ENGLISH RULE: every message you send is plain English. No jargon, no coined shorthand,
ASCII punctuation only, at most one backtick per sentence. Pass this rule on verbatim to any
agent you spawn.

Relay depth is now 1. If your own meter warns again before the PR comments are posted, do not
relay a second time: push what you have, update the state doc, and report to the coordinator.
