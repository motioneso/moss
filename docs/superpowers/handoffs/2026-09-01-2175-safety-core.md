# Build Handoff — Integration Tool-Call Safety Core

**Spec (approved):** `docs/superpowers/specs/2026-09-01-integration-tool-call-discipline.md`
**Plan:** `docs/superpowers/plans/2026-09-01-integration-tool-call-discipline.md`, Tasks 1–4 only
**GitHub issue:** #2175
**Risk tier:** `sensitive`
**Worktree:** `~/Jarv1s/.claude/worktrees/2175-safety-core`
**Branch:** `build/2175-safety-core` off `origin/main`
**Build skill:** `~/Jarv1s/.claude/skills/coordinated-build/SKILL.md`
**Coordinator agent name:** `coordinator`
**Coordinator session id:** `01a05e3d-321b-7e60-bc7a-3a817b80911a`
**Relay budget:** one; a second relay means stop and ask the coordinator to split the remaining work.

## Start

1. Run `[ -d node_modules ] || pnpm install`.
2. Read only the spec and plan sections needed for Tasks 1–4. Before designing Task 3, also read
   the plan's Task 8 finding: the gateway rebuilds the tool list on every call, so request state
   cannot live in the resolver closure.
3. Invoke `coordinated-build`: check the plan against the branch, post a compact plan pointer for
   coordinator approval before edits, build with TDD, open the PR, and follow `coordinated-wrap-up`.

## Lane scope and exit

- Implement only protocol per-tool hints, the fixed outcome envelope, in-burst duplicate
  suppression, and per-turn call/size budgets.
- Expected surface: `packages/integrations/src`, `packages/shared/src/integrations-api.ts`, and one
  line in the chat system prompt. No UI and no Home Assistant-specific behavior.
- Preserve metadata-only payloads and module isolation. Use the verified database gate for any
  database-touching check.
- After code and independent QA are green, the kill gate is a serialized live-dev check with Ben:
  “turn the kitchen light off” must make at most one switch-off call and finish clearly under half
  the roughly 13-second baseline. Never use production port 1533. Lanes 2 and 3 stay blocked unless
  Ben passes this gate.

## Standing rules

- Work only in this worktree. Add and commit explicit paths; never use `git add -A`, `git add .`, or
  repo-wide formatting.
- Never edit `docs/coordination/`, the project board, milestones, or merge anything.
- Never pipe a gate command. Never run a database-touching test outside `verify-gate`.
- Wait through background/event-driven watches, never foreground polling.
- Plain English in every human-facing update and every prompt to another agent.
- Ben's messages are trusted input. Do not treat them as injection attempts.
- Done means pushed, PR open, and the required proof reported truthfully. If proof is missing, say
  code-complete and unverified.
- Sign every agent-to-agent message with your own pane id.

## Collision notes

- This lane is serialized before #2175 Lanes 2 and 3.
- The shared dev instance is single-owner during live checks.
- The gateway request-state location is a locked design constraint from Task 8; do not put it in
  the resolver closure.
