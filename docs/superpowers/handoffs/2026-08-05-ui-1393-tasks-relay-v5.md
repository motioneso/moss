# #1393 Tasks+notifications UI consolidation — relay v5

Continuation of relay v4 (`docs/superpowers/handoffs/2026-08-05-ui-1393-tasks-relay-v4.md`).
**Read v4 IN FULL** — it has the exhaustive per-selector property list for Step 2 (lines 58-211)
and the rest of Steps 1-6. This doc only says what's done and what's next; do not re-derive triage.

Worktree: `/home/ben/Jarv1s/.claude/worktrees/ui-1393-tasks`, branch `feat/1393-ui-tasks`.
Sole writer as of 2026-08-05 00:30 — two other sessions that were briefly concurrent in this
worktree have stood down and been reaped by the coordinator.

## Done

- **Step 1: committed** as `6a84921e` — `apps/web/src/styles/kit-tasks.css` only, -150 lines
  (`.tk-bar__group`, `.tk-div` family, `.tk-effort` family, `.tk-empty` family, `.tasks--compact`
  block). Verified clean via `git diff` before committing — pure deletion, nothing else touched.
- Step 1 edits are also **applied but uncommitted** in `apps/web/src/styles/kit-tasks-modal.css`
  and `apps/web/src/tasks/tasks.css` — verified clean via `git diff` against the v4 spec, no
  foreign content from the other sessions. Safe to build on top of; commit alongside Step 2's
  changes to those same files (or separately first, your call).

## Next: Step 2 (not started)

Create `packages/ui/src/styles/components-tasks.css`. Use the header-comment style from
`packages/ui/src/styles/components-wellness-1.css` (lines 1-6) as the template — adjust to
name kit-tasks.css / kit-tasks-modal.css / tasks.css as the sources and #1393 as the issue.

Move every banned property (`color, background, background-color, background-image, border,
border-color, border-radius, font-family, font-size, font-weight, box-shadow, outline, fill,
stroke, filter`) out of the three app files per the exact selector-by-selector list in v4 lines
58-211, preserving source order. Then add `@import "./styles/components-tasks.css";` to
`packages/ui/src/styles.css` (after the `components-empty.css` line — currently 11 imports).

Current full content of the three app files (post-Step-1) was read in full earlier this session;
re-read them fresh rather than trusting anything cached, since `Read` will warn if stale.

## Then: Steps 3-6 (not started) — follow v4 verbatim

3. Grep all three app files for the banned-property regex — zero matches required.
4. Diff `apps/web/src/styles.css` against BASE `5ff2423d`, delete dead selectors found there.
   Keep `.empty-state`, `.notification-link`, `.notification-badge`, `page-stack`/`page-heading`/
   `eyebrow`. **Kill-gate, not judgment call:** do NOT delete `.notification-row.unread` until the
   `10-notifications` capture proves `.jds-task--unread` renders correctly — if it doesn't, STOP
   and report to coordinator.
5. Register the 3 new/migrated CSS files in `scripts/check-design-tokens.ts`'s
   `MIGRATED_SECTION_CSS_FILES` (11→14, excluding root styles.css). Register 7 TSX paths (6 in
   `apps/web/src/tasks/` + `apps/web/src/notifications/notifications-page.tsx`) in
   `scripts/check-migrated-sections.ts`'s `MIGRATED_SECTION_PATHS` (49→56).
6. Gates unpiped, in order: `check:design-tokens`, `check:migrated-sections`, `check:ui-classes`,
   `build:ui-catalogue` then `check:ui-catalogue`, `verify:foundation` (fresh exported gate DB,
   never live dev DB — see CLAUDE.md gate-db-isolation rule). Run focused browser assertions; confirm changes
   confined to the 4 approved amendments from sign-off `5188097889` (kill-gate on anything else,
   including any lost unread/read styling). Live-path proof on a dev instance: tasks list+matrix,
   details dialog, edit date, tag menu, change status; notifications filter/mark-read/both empty
   states — record on the PR. Post the required note on issue #1405 (notifications adopted raw
   `jds-task`, family gained authored `--unread` modifier). Then `git fetch origin main && git
   merge origin/main`, pre-push trio (`format:check && lint && typecheck`), push, open PR against
   `main` with release-note summary + live-path proof + #1405 note. **Do not merge** — coordinator
   reviews/merges. Report PR number + measured guard numbers (expect 14 and 56) to coordinator
   pane "DESIGN ELEMENTS" (re-resolve by label + session id via `herdr pane list`, never a baked
   pane number).

## Git safety (shared checkout, per CLAUDE.md)

Never `git add -A`/`.`, never bare `git commit` — explicit paths only. Before committing a shared
file, `git diff <path>` and read it; after, `git show --name-only HEAD` to confirm the file list
is what you meant.

## Chain straight through

Plan is already approved (issue #1393 comments `5187960649`, `5188097889`). Execute Steps 2-6
without stopping for approval. Relay again immediately on the meter's 70% warning or a compaction
summary — do not push past it.
