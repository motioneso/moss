# Relay 2 — #1319-A phase 2 build (catalog-verify lane)

**Status:** Task A done and committed. Task B not started (research done, no code written).
Resume via `coordinated-build` step 2 (Build), on Task B.

**Plan:** `docs/superpowers/plans/2026-08-23-1319a-phase2-catalog-verify.md`
**Master plan (Task 4 has the full spec):**
`docs/superpowers/plans/2026-08-18-1319-signed-module-catalog.md` (search "Task 4")

**Branch/worktree:** `build/1319a-catalog-verify`, this worktree. `node_modules` already
installed — skip `pnpm install`.

**Coordinator:** registered name `coordinator` (Codex, pane label "Coordinator" in workspace w1).
Re-resolve fresh via `herdr agent list` before messaging — never reuse a pane id from this doc.

## What's done

Task A committed at `f786077a3` — "feat(#1319): verify catalog signature in fetchRegistryIndex".
`registry-source.ts` now exports `REGISTRY_SIGNATURE_MAX_BYTES`, `CatalogVerification`,
`CatalogVerificationFailureReason`, `FetchRegistryIndexResult`; `fetchRegistryIndex` fetches the
index via `arrayBuffer()`, digests those exact bytes, verifies a `.sig` fetched through the same
wrapper, folds every signature failure to `"unverified"` with a reason, never throws.
`pnpm test:unit tests/unit/module-distribution-pipeline.test.ts` → 15/15 green.
`pnpm typecheck` → exit 0 (ran right after committing Task A, before Task B changes).

**One incident worth knowing:** partway through Task A, my uncommitted edits to
`registry-source.ts` and the test file were silently reverted to HEAD on disk (confirmed by diff
and no reflog entry) while I was the only live agent in this worktree. Cause unknown. I redid the
work and committed it immediately afterward with no further loss. Lesson for you: commit each
task's green work right away — don't let uncommitted work sit if you can help it.

## Task B — snapshot cache + envelope + route wiring (not started)

Full spec is master plan Task 4 (`docs/superpowers/plans/2026-08-18-1319-signed-module-catalog.md`,
section "### Task 4"). This lane's own plan doc re-verifies the line numbers — trust its citations,
not the master plan's, for exact locations. Re-grep before editing since line numbers may have
drifted further after Task A landed.

**Files to touch:**
1. `apps/api/src/module-distribution-port.ts` — add `RegistryEntriesSnapshot` interface
   (`entries`, `catalogVerification`, `catalogDigestSha256`); `fetchRegistryEntries` returns
   `Promise<RegistryEntriesSnapshot>` instead of `readonly ModuleRegistryEntryLike[] | null`.
   Cache slot holds one snapshot written atomically from one `fetchRegistryIndex` call.
   `"unavailable"` results are never cached; `"unverified"`/`"verified"` cache for the existing 10
   minutes (`REGISTRY_CACHE_TTL_MS`, currently at line ~35).
2. `packages/settings/src/routes-external-module-types.ts` — `ModuleDistributionDependencies`
   interface (line ~96-104): `fetchRegistryEntries` return type updates to the snapshot type
   (import it or redeclare the shape — check whether settings should import the type from
   `@moss/module-registry` or `apps/api` defines its own; apps/api already imports
   `fetchRegistryIndex` from `@moss/module-registry/node`, so `CatalogVerification` is reachable
   from there — settings package must NOT import module-registry per module-isolation, so
   redeclare the literal union `"verified" | "unverified" | "unavailable"` in settings' own type
   instead of importing it).
3. `packages/settings/src/routes-module-registry.ts` — both GET constructions (~92-97, ~102-107)
   and the three post-mutation refetch call sites (download ~157, remove ~221, purge-cancel ~261)
   consume the snapshot instead of the raw entries array. Invariant: on `enabled: true` responses,
   `registryUnavailable === (catalogVerification === "unavailable")`. The `enabled: false` early
   return keeps `registryUnavailable: false`, sends `catalogVerification: "unavailable"`,
   `catalogDigestSha256: null`. **No row-shape change** — `deriveRows`, `ModuleRegistryDeriveInput`,
   `module-registry-rows.ts`, `ModuleRegistryRowDto`, row schema all untouched.
4. `packages/shared/src/platform-api-modules.ts` — `GetModuleRegistryResponse` (line ~520) +=
   `catalogVerification: "verified" | "unverified" | "unavailable"` and
   `catalogDigestSha256: string | null`. `getModuleRegistryRouteSchema`'s 200 response schema
   (line ~585-605) gets both properties added to BOTH `properties` and `required` — fast-json-stringify
   strips undeclared fields, so missing either silently drops the field from the real HTTP response.

**Fixture call sites to extend (grep-confirm each exists before editing):**
`tests/e2e/settings-modules.spec.ts`, `tests/unit/settings-instance-modules-pane-render.test.tsx`
(3 `satisfies GetModuleRegistryResponse` fixtures), `tests/unit/instance-modules-dedup.test.tsx`
(stale `.tsx`, not typechecked, update anyway), `tests/integration/module-distribution.e2e.test.ts`
(`registryUnavailable` assertion, and the D6 harness change — the e2e mock registry needs to start
serving a test-key-signed index here, per the Phase-2 e2e finish line below).

**New/extended tests — IMPORTANT, verified this session, corrects the lane plan doc:** the lane
plan doc says "extend `tests/integration/module-registry.test.ts`" for the new route-level tests,
but I read that file in full this session and **it does not test the HTTP route** — it only tests
`assertModuleRegistryConsistency` and `getExternalModuleDeletionTables` (module-registration
consistency checks, unrelated surface). The actual `/api/admin/module-registry` GET route is
exercised in `tests/integration/module-distribution.e2e.test.ts` and `tests/e2e/settings-modules.spec.ts`.
Before writing the new tests (`enabled:false` envelope fields, the `registryUnavailable` identity,
schema-strip check, non-admin-before-fetch ordering with a spy on `fetchRegistryEntries`, cache
behavior), find where route-level fixtures/mocks for this route are actually built and place the
new tests there — very likely `tests/integration/module-distribution.e2e.test.ts` — rather than
`module-registry.test.ts` as the plan says. If genuinely no home fits, that's worth flagging to the
coordinator as a plan correction, not silently guessing.

**Verify commands (from the plan):**
`pnpm test:integration tests/integration/module-registry.test.ts` → exit 0 (or whichever file
actually ends up holding these tests — adjust the command to match reality).
`pnpm typecheck` → exit 0.

## Phase-2 e2e (this lane's finish line, after Task B)

`pnpm test:integration tests/integration/module-distribution.e2e.test.ts` → exit 0. The e2e mock
registry starts serving a test-key-signed index here (D6 harness change) — proves the verified
listing end-to-end before Phase 3 enforcement exists.

## Next concrete steps

1. Implement Task B with TDD (locate the real route-level test home first, per the correction
   above), commit.
2. Run the Phase-2 e2e finish line, confirm exit 0.
3. Pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`) + `git fetch origin main &&
   git rebase origin/main`.
4. `coordinated-wrap-up`: full gate via the `verify-gate` skill (isolated DB, never unscoped),
   push, open a non-draft PR, report exact-head evidence to the coordinator. Backend/API-only
   change (no UI in this phase) — confirm with the coordinator whether live-path UAT proof is
   still expected for this lane or deferred to Phase 3 before claiming done.
5. Do not merge, move the board, or touch coordination files — report to the coordinator and stop.

## Preservation constraints (still binding, from the coordinator's original approval)

- fixed verification enums exactly as specified in master-plan Task 3 (already shipped in Task A)
- raw-byte digest/signature contract (already shipped in Task A)
- atomic snapshot invariant — one `fetchRegistryIndex` call produces entries + verification +
  digest together; cache never mixes fields from different fetches; `"unavailable"` never cached
- non-admin-before-fetch ordering — `assertAdminUser` runs before `fetchRegistryEntries` is ever
  called (test this explicitly)
- hard boundary — do NOT touch pipeline enforcement/409/override (Phase 3), ensure-at-boot
  (Phase 3 Task 6), web UI (Phase 3 Task 7), UAT (Phase 4), or any migration. Phase 1
  (catalog-signing.ts) is merged to main (PR #1684) — read-only for this lane.

## Reminders

Security tier — build defensively, no crypto/library error detail in product-facing messages or
logs. Never `git add -A`/bare-commit in this shared-checkout worktree — use the `shared-checkout`
skill's explicit-path procedure, and given this session's file-revert incident, diff-check and
commit each task's work immediately once green, don't batch. Resolve `coordinator` fresh via
`herdr agent list` before messaging (confirm exactly one live match) — never reuse a pane number
from this doc. Sign every message to the coordinator with your own pane id. Write in plain English
to the coordinator and in any spawn prompts — no invented jargon, name only what Ben must act on.
