# ext-nav-1019 relay 3 — 2026-07-13

Bug #1019, sensitive tier. Worktree: this one (`.claude/worktrees/ext-nav-1019`, branch
`ext-nav-1019`, off `origin/main`). Coordinator: label `Coordinator`, session
`58a78927-385c-4b1d-8fa0-94db20255d6f`.

**Plan (approved, do not re-litigate):**
`docs/superpowers/plans/2026-07-13-external-module-navigation-abi.md` — 8 TDD tasks, ALL DONE.

**Done since relay-2:**
- Task 7 — `external-modules/job-search/jarvis.module.json` navigation entry
  (`{id:"job-search",label:"Job Search",path:"/",icon:"briefcase"}`) +
  `tests/unit/external-module-job-search-manifest.test.ts` assertion (20/20 pass). Commit
  `9fa0b203`.
- Task 8 — full suite green: `pnpm test:unit` (393 files/3222 tests), `pnpm test:integration`
  (152 files/1642 tests), then full `pnpm verify:foundation` exit 0 (lint, format, file-size,
  design-tokens, no-ambient-dates, package-deps, typecheck incl. web + external-modules,
  test:unit, db:migrate, test:integration — all green). Along the way `format:check` flagged
  pre-existing prettier drift in 4 branch-owned files (plan doc + job-search manifest + 2 test
  files from Tasks 2/5/7) — fixed with a **scoped** `prettier --write` on exactly those 4 paths
  (verified diffs were whitespace-only, no logic change), committed separately:
  `721015a0 chore(ext-nav-1019): fix prettier formatting on branch files (#1019)`.
- All 8 plan tasks are now code-complete and gate-green.

**Coordinator message received (address before PR):** main advanced to `7d852092` (#986
settings-shell merged, pure-frontend, did NOT touch app-shell.tsx/app-route-metadata.ts). Before
opening the PR: `git fetch origin main && git rebase origin/main`. Coordinator says the ONLY
likely conflict is a 1-line heading rename in `tests/e2e/app-shell.spec.ts` — resolve it yourself,
**keep both** #986's heading text AND this branch's new Modules-nav assertions. Do not ask the
Coordinator to hand-edit it.

**Next — dev-UAT (HARD exit gate, do this BEFORE the rebase per the original handoff doc order,
or after — either is fine, but do NOT skip it and do NOT PR without it):**

Full procedure: `docs/coordination/handoff-1019-external-module-nav.md` (coord worktree, read-only
— do not edit `docs/coordination/`). Key constraint: **must prove the ACTUAL branch behavior**
(navigation field flowing manifest → API serialize → web Modules section), not just that install
mechanics work.

**Researched this relay (do not re-research — act on this):**
- The "Instance modules" install flow does NOT read `external-modules/job-search` from disk. It
  fetches a real index.json + `.tgz` from a hardcoded GitHub Releases URL
  (`packages/module-registry/src/distribution/registry-source.ts:9`,
  `REGISTRY_INDEX_URL`) via `downloadAndStageModule`
  (`packages/module-registry/src/distribution/pipeline.ts:61`). The **published** registry has the
  OLD job-search manifest (no `navigation` field) — installing against the real registry would
  prove nothing about this branch's change.
- Fix: override with `JARVIS_MODULE_REGISTRY_URL` pointed at a **local mock registry** serving a
  tarball packed from THIS branch's `external-modules/job-search` via the real packer
  `packModuleArtifact` (`scripts/publish-module-registry.ts:52`, needs `dist/` built first —
  `buildExternalModule` from `scripts/build-external-module.ts`, see
  `buildRegistryArtifacts` in the same file for the build+pack+index sequence). This override is
  refused when `NODE_ENV=production` (`registry-source.ts:21-30`) — fine, dev-UAT is not prod.
  Working pattern to copy: `tests/integration/module-distribution.e2e.test.ts` (node:http mock
  registry server, `packModuleArtifact` call, `index.json` shape) — adapt it to serve the REAL
  job-search module dir instead of a fixture manifest.
- App tier: run from **source**, not docker — `apps/api` `pnpm dev` = `tsx watch src/server.ts`
  (no build step), `apps/web` via Vite dev, against a throwaway Postgres (reuse
  `tests/integration/test-database.ts` connection pattern or a scratch db). No docker build
  needed; this is faster and reflects branch code exactly.
  `resolveModulesDir` (`packages/module-registry/src/resolve-modules-dir.ts:23-38`) defaults to
  `<repoRoot>/data/modules` — fine for a throwaway UAT run (or set `JARVIS_MODULES_DIR` to a
  scratch dir explicitly to avoid polluting the repo's `data/`).
- Existing UAT script `scripts/uat/job-search-install.spec.ts` (from #1006/#1007, committed,
  don't modify) is the wrong shape for #1019 — its final assertion does
  `page.goto(`${BASE_URL}/m/job-search`)` directly, which is **explicitly forbidden** by the
  #1019 handoff doc ("the whole point of this fix is that the user reaches it by clicking the
  nav"). Write a **new** script, e.g. `scripts/uat/nav-1019.spec.ts`, reusing its signup/install
  helpers as a pattern but replacing the final step with: assert a nav link labelled "Job Search"
  exists in the shell nav → **click it** → assert the job-search page renders. Record the
  assertion results in the action log.

**After dev-UAT passes:**
1. `git fetch origin main && git rebase origin/main` — resolve the `tests/e2e/app-shell.spec.ts`
   heading-rename conflict yourself (keep both changes).
2. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`.
3. Re-run `pnpm verify:foundation` post-rebase (full gate) — record exit code.
4. `coordinated-wrap-up` skill: push, open PR (base `main`, body `Closes #1019`, user-facing
   "What's new": "Downloaded modules now appear in the navigation menu after you install them —
   previously an installed module could only be reached by typing its URL"). Report the PR number
   to the `Coordinator` pane (label `Coordinator`, resolve fresh by label+session id, never a
   baked pane number). Do not merge — tier sensitive, expects cross-model review + Coordinator
   merge sign-off.

**Process reminders:**
- `superpowers:test-driven-development` for any new code; `coordinated-build` skill for gates and
  escalation.
- Stage only this task's own files. No `git add -A`. No `docs/coordination/` edits. No repo-wide
  `pnpm format` (already burned once fixing exactly 4 branch-owned files — keep that scoped habit
  for any future format fixes too).
- Guardrails unchanged: no Instance-modules settings UI edits (UX-owned #986), escalate any
  shell/app-route-metadata conflict beyond the known 1-line heading rename to the Coordinator
  instead of hand-resolving broader scope.
- Relay again at the next 70% context-meter warning or on seeing a compaction summary.
