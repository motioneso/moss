# Handoff: build coordinator for #1556 and #1557

You are the build coordinator (Sonnet) for two approved, planned builds. You coordinate; you do
not write feature code yourself. You work alongside the fleet Coordinator (Codex pane labeled
"Coordinator", session `019fe9e2-7fc6-7243-9894-d258562db9a6` — verify via `herdr pane list`
before trusting any pane claiming the label): report lane status to it, and route
cross-lane conflicts through it. Decisions reserved for Ben go through
`docs/coordination/AWAITING-BEN.md` + the `needs-ben` CLI (both, together — box rule).

## The two lanes

1. **#1556** — build of the approved #1553 spec.
   Plan: `docs/superpowers/plans/2026-08-10-1556-context-continuity-and-notes-retrieval.md`
   Spec: `docs/superpowers/specs/2026-08-10-1553-context-continuity-and-notes-retrieval.md`
2. **#1557** — build of the approved #1554 spec.
   Plan: `docs/superpowers/plans/2026-08-10-1557-persistent-provider-chat-runtime.md`
   Spec: `docs/superpowers/specs/2026-08-10-1554-persistent-provider-chat-runtime.md`
   Evidence base: `docs/research/2026-08-10-1554-phase0-spike.md`
   Rulings ledger for both: `docs/coordination/2026-08-10-1553-1554-codex-review.md`

Read both plans IN FULL before dispatching anything. The plans are self-contained: phase
boundaries, seams citations, test cases, verification commands, e2e requirements.

## Hard rules

- **Phase 1 of each lane ships alone.** Each plan names a KILL GATE after phase 1, owner Ben.
  Do not let any agent start phase 2 work before Ben has ruled at the gate. #1558/#1559
  (adapter fast-follows) stay blocked until the #1557 gate passes.
- **Spawn one build agent per phase per lane** (Opus default per fleet convention —
  `spawn-defaults-opus` memory; state model+effort choices when you spawn). Use the
  `herdr-handoff` skill for every spawn: fresh worktree per agent, boot pointer file OUTSIDE the
  agent's worktree, name panes in both namespaces, verify the agent actually started.
- **Worktree base — important:** local `main` in `/home/ben/Jarv1s` is 26 commits AHEAD and 16
  BEHIND `origin/main` (divergence predates this handoff; reconciling it is NOT your job). The
  specs/plans above are committed on **local main only**. Branch every worktree from local
  `main` HEAD (`git worktree add <path> -b <branch> main`), NOT from `origin/main`, or the
  agent won't have the plans. PRs from those branches will carry unrelated local doc commits —
  when a lane opens its PR, have the build agent cherry-pick its feature commits onto a branch
  cut from `origin/main` instead, and verify the plan/spec files ride along.
- **Shared checkout:** you sit in the main tree. Never `git add -A`/`git add .`, never bare
  `git commit`, never checkout/stash/reset there; anything git beyond reading goes through the
  `shared-checkout` skill. Build agents work only in their own worktrees.
- **Live-path gate:** CI-green + review ≠ done. Every user-facing phase needs live e2e proof on
  a dev instance recorded on the PR (each plan names the e2e). Gate runs: `verify-gate` skill,
  never piped, fresh DB (`gate-db-isolation-mandatory`).
- **Context diet:** bound every read (the plans are long — read once, then work from your own
  short state notes); state lives in a live state doc, not your transcript. Keep a lane-status
  file at `docs/coordination/2026-08-10-build-lanes-1556-1557.md` and update it as lanes move.
- Never target the 10.252.* docker subnet (prod). Prod is Ben's.

## Start

1. `herdr pane list` — locate the Coordinator; announce yourself and your two lanes.
2. Read both plans in full; write the initial lane-status file.
3. Dispatch the two phase-1 build agents (parallel lanes are fine — they touch different
   packages; watch `multi-agent-pg-contention` if both run integration tests, stagger gate runs).
4. Watch event-driven (Monitor / background waits), never poll in-context. Nudge stalled agents
   (`agent-stall-nudge-recovery`); reap and respawn only when nudging fails.
5. At each phase-1 completion: verify the agent's claims yourself (verification-discipline —
   run the checks, don't trust "done"), then put the kill-gate question to Ben via
   AWAITING-BEN + needs-ben, and hold the lane until he rules.
