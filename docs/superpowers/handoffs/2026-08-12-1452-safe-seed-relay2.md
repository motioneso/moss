# Relay 2 — fix-1452-safe-seed

**Issue:** #1452 (part of #1440). **Risk tier:** `routine`. **No code written yet, no commits.**
Relaying at the context-meter 70% warning — planning-stage research only, zero build progress.

**Worktree (confirmed real, not the stale decoy path):**
`/home/ben/Jarv1s/.claude/worktrees/coord-overnight-20260810/.claude/worktrees/fix-1452-safe-seed`
**Branch:** `fix-1452-safe-seed`. `node_modules` already installed — do NOT re-run `pnpm install`.

**Coordinator:** label `Coordinator`, session `0bb9f516-c026-454f-bc97-dc9faf43bd20` — verify via
fresh `herdr pane list` before messaging (never a cached pane id). Already notified of this relay
and given the findings summary below.

## What #1452 requires (do not re-read the issue — this is settled)

Design decision LOCKED by Ben (2026-08-12, issue comment) — do not re-litigate:
1. UAT spec: throwaway account → trigger real briefing generation via the actual worker path (not
   a fixture insert) → poll/wait for the row → live walk of Today page showing ≥1 briefing card
   **rendered** (not empty `ALL CLEARTODAY` frame) → screenshot → confirm zero old-product-name
   occurrences in rendered content → clean up everything created. **No destructive seed/reset of
   the shared dev DB — the entire point of #1452, non-negotiable.**
2. Rejected alternatives (don't reconsider): dedicated non-shared instance; insert-by-recorded-id
   fixtures.

## Done — findings from relay-1's session (all verified against this branch)

1. **UAT harness is fully self-isolating** — `tests/uat/provisioner.ts:454-501`
   (`createUatProvisionPlan`) provisions its own ephemeral per-run Docker Compose stack: unique
   project name, dedicated subnet (`UAT_DOCKER_SUBNET = "10.254.0.0/24"`, line 36), own volumes,
   teardown via `down -v` + `assertNoLeakedResources()` (lines 491-548) as positive proof of no
   leak. **The UAT run itself never touches the shared dev DB** — resolves handoff-1's open
   question about what "shared DB" risk the design decision guards against (it's not the UAT
   harness; it's guarding against a *fixture/insert* approach, which is exactly what's rejected).
2. **Real (non-fixture) briefing trigger path located:**
   - `POST /api/briefings/definitions` (`packages/briefings/src/routes.ts:108-129`) creates a
     definition.
   - `POST /api/briefings/definitions/:id/run` (`routes.ts:158-220`) — **the real manual-trigger
     route**: mints `runId`, builds a `BriefingRunPayload` (`packages/briefings/src/jobs.ts:22-31`)
     with `runKind: "manual"`, computes an idempotency-namespaced `singletonKey`, calls `sendJob`,
     returns `202 {jobId, runId}`. This enqueues onto `BRIEFINGS_RUN_QUEUE`
     (`jobs.ts:69-86`, policy `exclusive`, `retryLimit: 0`), consumed by the real
     `registerBriefingsJobWorkers()` (`jobs.ts:130+`). **This is the worker path to drive from the
     UAT spec — not a DB insert.**
   - `GET /api/briefings/definitions/:id/runs` (`routes.ts:222+`) — poll target; **response shape
     not yet read**, needed for the poll-until-succeeded loop (see "Not done" #1 below).
3. **No real/scriptable AI model needed to satisfy "card rendered."**
   `packages/briefings/src/fallback.ts` (full file, 50 lines) — the deterministic degraded-synthesis
   path always returns `status: "succeeded"` with non-empty `summaryText` (line 31:
   `text || "Briefing did not produce visible source items."`). The UAT harness's only seeded AI
   provider is fake and scoped to `module.news` only (#1121 gap) — irrelevant here, because the
   fallback path alone guarantees a renderable card. **#1121 does not block this spec.**
4. **Rendering target identified:** `apps/web/src/today/today-page.tsx:730-753`,
   `MorningBriefingSection` — `hasSummary = Boolean(props.run?.summaryText.trim())` gates
   `<BriefingProse>` vs. the `"Your morning briefing is not ready yet."` placeholder. Kicker text
   `"Morning briefing"`, title `"Your day, in focus"`. This is the durable assertion target for
   "≥1 briefing card rendered." Separately, `apps/web/src/today/today-labels.ts:33-47`
   `buildHeadline()` confirms the `ALL CLEAR`/`TODAY` empty state (line 46) is driven by
   task/event counts, not briefing-run state — so it's an independent exit condition to check
   alongside the rendered card, not the same signal.
5. **PR #1429 (`fix-1429-briefing-css`) confirmed NOT merged into this branch** — `git
   merge-base --is-ancestor a1212fabf HEAD` → not an ancestor. `apps/web/src/today/briefing-action-rows.tsx`
   exists in-tree but its CSS classes (`.loose-row`, `.briefing-catchup`) may not be styled/stable
   yet on this branch. **Plan must use durable role/text selectors, not those CSS classes.**
6. **Exemplar UAT spec read in full:** `tests/uat/specs/runtime-context.uat.spec.ts` (123 lines) —
   conventions: `uatLevel = { level: "solo-admin", without: [] } as const` export;
   `requireBaseURL()` reads `process.env.JARVIS_UAT_BASE_URL`; `signIn(page)` pattern scopes to
   `.locator("form.auth-form")` to dodge a duplicate-accessible-name collision between the auth-mode
   tab and the submit button, then handles the "Skip setup" → "Skip anyway" onboarding-skip flow;
   `openChat(page)` uses `getByRole("button", { name: "Chat with Moss" })` — **confirms current
   assistant name is "Moss"**, useful for the zero-old-product-name assertion. **Caveat:** this
   exemplar signs in as the **pre-seeded admin** (`UAT_ADMIN_EMAIL`/`UAT_ADMIN_PASSWORD` from
   `tests/uat/seed/admin.js`), not a fresh signup — #1452 wants a genuine throwaway account, so this
   pattern needs adapting, not copying verbatim.

## Not done — next steps in order (pick up here)

1. **Read `apps/web/src/api/client.ts` lines ~1245-1290** (`listBriefingDefinitions` L1250,
   `createBriefingDefinition` L1254, `listBriefingRuns` L1273) — confirm request/response shapes,
   especially the run `status` field values, to write the poll-until-succeeded loop.
2. **Read `packages/shared/src/briefings-api.ts`** (still unopened) — resolves the one open design
   question below. Read by section (the relevant contract types only), not front-to-back.
3. **Read `tests/uat/seed/levels.ts`** (still unread) — confirm `UatSeedLevel` semantics, in
   particular whether `"bare"` supports a self-serve signup flow, and what the onboarding-chunk
   gating at (per the exemplar's comment) `levels.ts:65-67` means for a fresh signup vs. the seeded
   `solo-admin`.
4. **Locate and read the real sign-up UI** (likely `apps/web/src/auth/auth-screen.tsx`, unconfirmed
   — grep first) for the throwaway-account-creation selectors (email/password fields, submit
   button, any post-signup onboarding step distinct from the seeded-admin onboarding-skip flow
   already seen in the exemplar).
5. **Resolve the one open design question** before finalizing the plan: creating a briefing
   definition through the real Settings UI requires the account to have ≥1 module enabled with a
   read-risk AI tool — confirmed at `apps/web/src/settings/settings-module-subviews.tsx:121-169`
   (`selectedToolNames = readToolNames(...)`; empty list keeps the Morning-briefing time input
   disabled via `busy`, and the mutation throws `"No read tools available for briefings"`
   client-side at lines 141-143 when the list is empty). A fresh throwaway signup starts with zero
   enabled modules. Two paths:
   - (a) walk the throwaway account through enabling a module first via real UI, then create the
     definition via Settings UI — fully UI-driven, closest to a real user journey, more steps/more
     UAT flake surface.
   - (b) after signup, drive `POST /api/briefings/definitions` directly via an authenticated
     `page.evaluate(() => fetch(...))` call — still exercises the real route → repository → RLS
     layer (not a fixture/DB insert, so it doesn't violate the locked design decision), just skips
     the Settings UI click-through for definition creation. The `.../run` trigger and the Today-page
     walk stay 100% real UI either way.
   Relay-1's lean was (b) for scope/flake reasons, but flagged it as a judgment call rather than
   deciding unilaterally — **this was included in the coordinator notification already sent this
   session; check for a reply before finalizing the plan, but don't block indefinitely on it if the
   coordinator doesn't have a strong preference (either path satisfies the "real worker path, not a
   fixture" constraint).**
6. **Write the plan** via `plan-build` → `docs/superpowers/plans/2026-08-12-fix-1452-safe-seed.md`
   (seams check w/ file:line citations from the findings above; UAT spec path
   `tests/uat/specs/<slug>.uat.spec.ts`; add a row to `.claude/skills/coordinate/uat-trigger-map.tsv`).
7. **Message the coordinator for plan approval** — STOP and wait, do not write code first.
8. TDD build, one task per commit, `Co-Authored-By: Claude` trailer, explicit `git add` paths only.
9. Pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`) + `git fetch origin main &&
   git rebase origin/main` before every push.
10. `coordinated-wrap-up`: own isolated-DB gate run, push, PR rebased on `origin/main`, live-path
    proof via `gh pr comment` (rendered-card screenshot + shared-DB-left-as-found evidence), report
    to coordinator. Never merge, never touch board/milestones/`docs/coordination/`.

Read the issue/plan by SECTION only for your current step — full-reads bloat a fresh context and
force a premature relay with no progress. BUILD and commit; that's what counts as progress.
