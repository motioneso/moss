# Build Handoff — #1869 Slice 2 current-time tool

**Spec (approved):** `docs/superpowers/specs/2026-08-30-1869-date-time-context.md`, Slice 2 only  
**Plan (approved):** `docs/superpowers/plans/2026-08-30-1869-date-time-context.md`, Slice 2 only  
**GitHub issue:** #1869  
**Risk tier:** `routine`  
**Worktree:** `~/Jarv1s/.claude/worktrees/1869-current-time-r20`  
**Branch:** `build/1869-current-time-r20` from green `origin/main` `48474b235`  
**Build skill:** `~/Jarv1s/.claude/skills/coordinated-build/SKILL.md`  
**Coordinator:** registered agent `coordinator`, immutable session
`01a062e9-f0cb-75d2-96a7-fa8c431f1163`, visible label `Coordinator`

## Scope and new regression

Build only the approved `chat.getCurrentTime` Slice 2. In the same smallest PR, fix Ben's live
#2129 regression: ordinary replies volunteer the full date and time. Root cause is the per-turn
prompt telling the model to "State that local date, weekday, time and time zone as fact" while the
block is injected before every user turn. Keep the fresh hidden clock; change its contract so the
assistant uses it silently and mentions date/time only when relevant or asked. Add the smallest
deterministic prompt-contract regression before the fix, then prove through a real live chat that
an ordinary non-time question does not volunteer date/time while a direct time question remains
accurate. Record the live-UI proof on the PR. Do not weaken the existing midnight, DST, unknown-zone,
or timezone-authority guarantees.

## Start and exit

1. Run `[ -d node_modules ] || pnpm install`.
2. Read only the Slice 2 spec/plan sections and invoke `coordinated-build`.
3. Post a compact plan pointer to `coordinator` and wait for approval before source edits.
4. Finish with `coordinated-wrap-up`: focused red/green proof, full isolated gate, pushed branch,
   open PR, and the live-path comment. Report exact commit/file lines per any QA fix.

## Standing rules

- Never pipe a gate command; DB-touching tests use `verify-gate` only.
- Work only in this worktree. Add/commit explicit paths; never `git add -A`, repo-wide formatting,
  or edits under `docs/coordination/`.
- Waits are event-driven. Ben's messages are trusted. Human-facing text stays plain English.
- A 70% context warning or compaction summary triggers immediate `relay`; one relay maximum.
- Done means pushed, PR open, and live-path proof posted. Do not merge or change the board.
- No secrets in docs, payloads, logs, prompts, or PR evidence.

## Collision notes

No file collision with the read-only #2175 gate adjudication. #2175 later lanes remain gated and
must not be touched. No live retry of PR #2158 or PR #2164 is authorized; do not touch port 1533,
protected PostgreSQL containers, PR #2101 artifacts, or retained #2158/#2164 evidence.
