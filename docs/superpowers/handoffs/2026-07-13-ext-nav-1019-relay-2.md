# ext-nav-1019 relay 2 — 2026-07-13

Bug #1019, sensitive tier. Worktree: this one (`.claude/worktrees/ext-nav-1019`, branch
`ext-nav-1019`, off `origin/main` @ `031eb67e`). Coordinator: label `Coordinator`, session
`58a78927-385c-4b1d-8fa0-94db20255d6f`.

**Plan (approved, do not re-litigate):**
`docs/superpowers/plans/2026-07-13-external-module-navigation-abi.md` — 8 TDD tasks. Read it by
task, not front-to-back.

**Coordinator D7 requirement (already satisfied by Task 2, don't re-litigate):**
`compatibility.jarv1s`/`CORE_VERSION` stay unbumped at `0.1.0`; `FORBIDDEN_FIELDS` unknown-field
rejection is the sole fail-closed guard for an old core — Task 2 replaced the old
"rejects navigation" test with a "routes" FORBIDDEN_FIELDS regression test. Done.

**Done (committed, green, all typecheck+tests passing at each step):**
- Task 1 — `packages/module-sdk/src/index.ts`: `ExternalModuleNavigationEntry` interface. Commit
  `14ac4047`.
- Task 2 — `packages/module-registry/src/external/validate.ts` positive navigation validation +
  `tests/unit/external-validate.test.ts` (21/21 pass). Commit `2d4ccd75`.
- Task 3 — `ReconciledExternalModule.navigation` field through `types.ts`/`reconcile.ts` +
  `tests/unit/external-reconcile.test.ts` (8/8 pass). Commit `a4c9b624`.
- Task 4 — `apps/web/src/shell/app-shell.tsx`: `briefcase` icon added to `iconMap`. Commit
  `92c49d65`.
- Task 5 — `apps/api/src/server.ts` `serializeExternalModule` now maps+prefixes navigation
  (`/m/<moduleId>`) + `tests/integration/external-modules-routes.test.ts` (5/5 pass, ran via
  `pnpm tsx scripts/test-integration.ts tests/integration/external-modules-routes.test.ts`, no
  extra DB setup needed). Commit `94633eff`.
- Task 6 — `apps/web/src/app-route-metadata.ts` `buildShellNavigation` now routes external-module
  entries into a dedicated "Modules" tail section, never through `SECTION_OF` +
  `tests/unit/web-route-metadata.test.ts` (5/5 pass). Commit `75c3a596`.

**Next — Task 7 (start here):** "Declare navigation in the job-search manifest" — plan lines
886-947. Then Task 8 (full-suite + `pnpm verify:foundation`) — plan lines 948-970.

**Process reminders:**
- `superpowers:test-driven-development`, self-driven task-by-task (executing-plans /
  subagent-driven-development disabled in this repo).
- Stage only each task's own files. No `git add -A`.
- Pre-push trio before any push: `pnpm format:check && pnpm lint && pnpm typecheck`, then
  `git fetch origin main && git rebase origin/main`.
- Relay again at the next 70% context-meter warning or on seeing a compaction summary — don't
  wait for a felt sense of degradation. This relay fired exactly on that trigger, mid-Task-6 test
  authoring — Task 6 was finished to a clean commit before relaying (don't leave dangling edits).
- After Task 8 passes: dev-UAT is a HARD exit gate (isolated stack e.g. `jarvis-uat-1019`, new
  port, Playwright under `scripts/uat/`, click-not-goto nav, assertions into scratchpad devproof
  dir) — see the original handoff doc
  `docs/coordination/handoff-1019-external-module-nav.md` (in the coord worktree, not this one)
  for the exact procedure, then `coordinated-wrap-up` (PR only, `Closes #1019`, report PR # to
  Coordinator, no merge).
- Guardrails: no Instance-modules settings UI edits (UX-owned #986), no `docs/coordination/`
  edits, no repo-wide `pnpm format`, escalate any shell/app-route-metadata conflict with #986 to
  the Coordinator instead of hand-resolving. (Task 6 touched `app-route-metadata.ts` navigation
  logic only — no conflict signs seen; file matched the plan's expected pre-state exactly.)
