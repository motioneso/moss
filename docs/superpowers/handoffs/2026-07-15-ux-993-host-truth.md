# Build Handoff — #993 Host Truth

**Spec (approved):** `docs/superpowers/specs/2026-07-15-settings-host-account-truth.md`  
**Scope:** Delivery Slice 1 — Host truth only  
**GitHub issue:** #993  
**Risk tier:** `security` — adversarial Opus QA, live UAT, and Ben's explicit merge sign-off required  
**Worktree:** `~/Jarv1s/.claude/worktrees/ux-993-host-truth`  
**Branch:** `ux/993-host-truth` off `origin/main` at `2f4f553d`  
**Build skill:** `~/Jarv1s/.claude/skills/coordinated-build/SKILL.md`  
**Coordinator:** exact label `UX Coordinator`, immutable Codex session
`019f6479-18a8-7782-ab34-a2e1d9c59c82`

## Start

1. `[ -d node_modules ] || pnpm install`.
2. Read only the spec sections needed for Delivery Slice 1; do not implement the account/email slice.
3. Invoke `coordinated-build`: verify scope, write the plan, publish its pointer, and wait for
   coordinator approval before editing implementation files.
4. Build with focused tests, then invoke `coordinated-wrap-up` for the PR and compact report.

## Run-Specific Bans

- Work only in this worktree/branch. Stage explicit paths; never `git add -A` or run repo-wide
  formatting.
- Never touch `docs/coordination/`, the project board, milestones, or merge state.
- Never add a generic shell route, request-controlled command/URL/path/version/argument, or Docker
  socket mount.
- Never emit installer stdout/stderr, secrets, verification material, or environment values.
- Do not implement any profile/account/email work in this PR.

## Collision and Security Map

The required one-shot Opus map was grounded on `origin/main` `2f4f553d`.

1. Extract one shared Root-workspace predicate in `@jarv1s/ai`, including
   `JARVIS_HERDR_ROOT_TAB`, and refactor both `decideMultiplexer` and
   `makeMultiplexerUsableProbe` to call it. Unit-test this first.
2. Keep shared DTO and Fastify response schemas synchronized; undeclared fields are silently
   dropped. Cover the real response with `app.inject`.
3. Add the fixed POST install route to the module-registry route guard allowlist.
4. The approved #993 spec explicitly supersedes the old `scripts/install-herdr.sh` comment that
   forbids API invocation; reconcile the comment without weakening its pinned/checksummed installer
   guarantees.
5. Reuse Settings audit events; add no table or migration.
6. Use one injected executor function for testability, not an abstraction hierarchy. Invoke the
   fixed repository installer with `execFile`, no shell, no request-derived arguments, bounded
   timeout, structured results, and metadata-only audit.
7. A process-local single-flight lock is approved for the single-API Compose topology. Mark the
   ceiling in code: `ponytail: process-local lock; use a database advisory lock if API replicas are
   introduced`.
8. Keep `logLevel` in the diagnostics DTO as collapsed read-only metadata; remove only the
   standalone UI row.

## Verification and Proof

- Focused unit/integration coverage for Root tab/pane/runtime-pane detection; auth rejection;
  request-input exclusion; concurrent install; timeout/failure audit; success re-probe; health
  summary ordering and recovery copy.
- `pnpm verify:foundation` must pass.
- The PR is code-complete but unmergeable until a live deployment proves: missing Herdr → one-click
  install → Installed → Root-workspace guidance or Usable, plus one deliberately unavailable health
  dependency with a clear next action. Post assertions/evidence on the PR.

## Communication

Use `herdr-pane-message` to exact label `UX Coordinator` and confirm its immutable session before
sending. Relay immediately on the context-meter warning or compaction summary.
