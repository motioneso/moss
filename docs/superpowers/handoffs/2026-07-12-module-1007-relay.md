# Relay — module-persist-1006 (#1007 ENOENT fix + Stage 2 UAT proof)

Worktree: `/home/ben/Jarv1s/.claude/worktrees/module-persist-1006`, branch `module-persist-1006`
(off `origin/main`, currently at `3ca138eb`, clean except local `.claude/context-meter.log`).
Coordinator label: `Coordinator` (re-resolve pane fresh via `herdr pane list` — never reuse a
cached `…-N`). Skill: `coordinated-build`. Tier: **security**. No code written yet — pure research.

## Binding scope (Coordinator's verbatim GO, already approved — do not re-litigate)

Original handoff at
`/tmp/claude-1000/-home-ben-Jarv1s--claude-worktrees-coord-2026-06-30-rfa-fleet/58a78927-385c-4b1d-8fa0-94db20255d6f/scratchpad/devproof/handoff-1006-persist-and-prove.md`
had two fixes (#1007 + #1006). **#1006 is a no-op — already shipped** (commit `7484f449`/#964:
`infra/docker-compose.prod.yml` already has `jarv1s-modules:/data/modules` volume +
`JARVIS_MODULES_DIR: /data/modules` on the `jarv1s` service, confirmed by full read). Coordinator's
GO:
1. Proceed **FIX A (#1007) ONLY**.
2. Do **NOT** add any `/app/data` volume — dead config, violates no-stale-scaffolding rule.
3. PR body: `Closes #1007`; note #1006 was already fixed upstream by 7484f449/#964, empirically
   reconfirmed by the Stage 2 recreate test.
4. Keep Stage 2's container-recreate assertion — doubles as the #1006 persistence proof.
5. Restart-to-activate (admin Install → DB install happens only at next boot, not live) is
   **by-design** (confirmed by `scripts/start-jarv1s.ts` boot sequence + the UI's own `<Note>`
   banner in `settings-module-registry-section.tsx`) — empirically reconfirm in Stage 2 and
   **report plainly to Ben as a finding**, do not try to "fix" it.

`infra/docker-compose.yml` (dev) needs **no edit either** — it bind-mounts the whole workspace,
doesn't run the packaged image at all, no `JARVIS_MODULES_DIR` set there.

## Stage 1 — FIX A, exact change (not yet made)

File: `packages/db/src/migrations/module-sql-runner.ts:130-146`, function `loadModuleMigrationFiles`.
Current body starts:
```ts
export async function loadModuleMigrationFiles(directory: string): Promise<ModuleMigrationFile[]> {
  const entries = (await readdir(directory)).filter((name) => name.endsWith(".sql")).sort();
```
Fix: wrap the `readdir` call in try/catch; on `ENOENT` return `[]` (DB-less module, e.g.
job-search, has no `sql/` dir — valid, not an error); rethrow anything else. Add a why-comment
citing #1007. Codebase's established idiom (NOT `existsSync` pre-check — TOCTOU): see
`packages/notes/src/write-tools.ts:131,177`, `packages/people/src/notes-service.ts:268`,
`packages/vault/src/vault-ops.ts:17`, `packages/settings/src/data-export-jobs.ts:170` for the
try/catch + `(error as NodeJS.ErrnoException).code === "ENOENT"` pattern to match.

Call chain confirming the bug is real and this is the only fix site: `scripts/module-reconcile.ts`
Phase 6 (~line 249-287) does `sqlDir = join(discovery.dir, "sql")` with **no existence check**,
passes it to `installModule` (`scripts/module-install.ts:57` calls
`loadModuleMigrationFiles(options.migrationsDirectory)`), which throws raw ENOENT for job-search
(no `sql/` dir) → reconcile's catch pins the module `status='disabled'`,
`disabled_reason='database install failed'`.

Regression test to add: `tests/integration/module-migration-ledger.test.ts`, inside the existing
`describe("loadModuleMigrationFiles", ...)` block (has 2 tests already: happy-path sorted-load,
and "throws with the file name when a file violates the wire contract" using
`mkdtempSync`/`writeFileSync(join(dir,"0001_bad.sql"), "DROP TABLE app.a;")` +
`.rejects.toThrow(/0001_bad\.sql/)`, `afterEach(rmSync)` pattern). Add a 3rd test: "returns []
when directory doesn't exist" — construct a path under `tmpdir()` that is never created (or
mkdtemp+immediately rmSync before calling), assert `await loadModuleMigrationFiles(path)` resolves
to `[]`. Must NOT touch the unrelated `describe("module migration ledger", ...)` block in the same
file (tests `getAppliedModuleMigrations`/`recordModuleMigrations`).

## Stage 2 — UAT proof research so far (selectors/routes discovered, script not yet written)

**Settings navigation:** Settings page is `/settings` (see
`apps/web/src/onboarding/onboarding-wizard.tsx:111` navigates there post-onboarding;
`apps/web/src/shell/app-shell.tsx:334` has a nav trigger `onNavigate("/settings")`). Settings page
component `apps/web/src/settings/settings-page.tsx` — admin section id `"instmods"`, icon
`Package`, label **"Instance modules"** (line ~150), lazy-loads
`apps/web/src/settings/settings-instance-modules-pane.tsx` → `InstanceModulesPane`. Sections use
`useSearchParams`/`coerceSettingsSectionId` — likely reachable via `/settings?section=instmods` or
by clicking the sidebar entry labeled "Instance modules" (still need to confirm exact query-param
name/tab-click mechanic — read `settings-page.tsx` render body past line 70, and
`settings-navigation.ts`, before writing the script).

**Instance Modules pane** (`settings-instance-modules-pane.tsx`, already read in full, DO NOT
EDIT — owned by "Codex UX" agent): renders `PaneHead` "Instance modules", a `<Group>` of built-in
toggles, then (only when `external?.enabled`) an "External modules" group, then always renders
`<ModuleRegistrySection externalModules={external?.modules} onSetEnabled={...}
settingEnabledPending={...} />` — **this registry section is where job-search install/enable will
actually happen** for a registry-downloaded module.

**`settings-module-registry-section.tsx`** (already read in full, DO NOT EDIT): `<h3>Available
modules</h3>`, "Refresh from registry" button. Each module row `<li>`: `<strong>{row.name}</strong>
<code>{row.id}</code>`, description `<p>`, state label via `STATE_LABELS` map (`"not-installed"→
"Not installed"`, `"pending-restart"→"Downloaded — restart to apply"`, `"installed-enabled"→
"Installed"`, `"installed-disabled"→"Installed (disabled)"`, etc.). `canInstall(row)` → button
`className="jds-btn jds-btn--primary"` text **"Install"** (or "Download update"/"Retry download"),
`onClick` opens a confirm dialog (`role="dialog" aria-modal="true" aria-label={title}` — title =
`"Install ${row.name}?"`, confirm button text **"Download"**) → on confirm calls download mutation
→ toast `"${name} downloaded — restart Jarvis to apply"`. When state is `installed-enabled`/
`installed-disabled`, row renders `<Switch ariaLabel={`Enable ${row.name}`} checked=...
onChange=... />` for enable/disable. A page-level `<Note>` banner appears whenever any row is
`pending-restart`/`update-pending-restart`: *"Downloaded modules apply on the next restart. From
your deployment directory: `docker compose pull && docker compose up -d` (or restart the
container)."* — this is direct UI text corroborating directive #5 (restart-to-activate by design).
Confirm-dialog component is `useFeedback()` from `apps/web/src/settings/settings-feedback.tsx:41`
(`confirm: (options: ConfirmOptions) => void`), dialog markup at that file's line ~133
(`role="dialog"`).

**job-search web module** (`external-modules/job-search/src/web/`) — file tree enumerated but
**NOT YET READ**: `root.tsx`, `router.ts`, `states.tsx`, `store.ts`, `api.ts`, `index.ts`,
`format.ts`, `runtime.ts`, `styles.ts`, and `screens/{onboarding,profile,opportunity-detail,
opportunities,monitors,overview}.tsx`. Read these next to find the real in-module route/nav
element to assert against once job-search is enabled (Stage 2 requirement: "assert a real
job-search route/page/nav element responds, not just the list row").

**Signup/onboarding for owner account:** `apps/web/src/onboarding/onboarding-wizard.tsx` +
`onboarding-ui.tsx` exist — not yet read. Need real signup form selectors (email/password fields,
submit button) before writing the Playwright script's login step.

## Immediate next steps for successor (in order)

1. Read `external-modules/job-search/src/web/root.tsx`, `router.ts`, and 1-2 screens (`overview.tsx`
   likely the landing screen) to get the real route path / nav text for the post-install
   assertion. Read `apps/web/src/settings/settings-page.tsx` lines 70-end + `settings-navigation.ts`
   for the exact section-switch mechanic. Read `apps/web/src/onboarding/onboarding-wizard.tsx` +
   `onboarding-ui.tsx` for signup selectors. Keep these reads targeted (grep first), not full-file
   sweeps beyond what's needed for selectors.
2. Invoke `superpowers:writing-plans` → write
   `docs/superpowers/plans/2026-07-12-module-1007-enoent-guard-and-uat-proof.md` covering: Stage 1
   FIX A as bite-sized TDD tasks (code above is exact — no placeholders needed there), Stage 2 as
   concrete tasks (docker stack up on `jarvis-uat-1006`:1545, owner signup, Playwright script at
   `scripts/uat/job-search-install.spec.ts`, restart-to-activate check + plain report, container
   recreate + persistence re-assert).
3. Message Coordinator (fresh-resolved label `Coordinator`) with the plan path, per
   `coordinated-build` step 1: **"plan ready for module-1007-enoent-guard-and-uat-proof: <path>.
   Approve, or flag a fork."** STOP and wait for approval — do not write code before it lands.
4. On approval: build FIX A via TDD (own commits, `Co-Authored-By: Claude`, explicit `git add`
   paths only, never `-A`), `pnpm verify:foundation` green, then Stage 2 (isolated `jarvis-uat-1006`
   stack per the original handoff's exact commands — reread that doc's STAGE 2 section verbatim,
   it's short and was already read in full this session, no need to re-derive).
5. Pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck` + `git fetch origin main &&
   git rebase origin/main`) before push. PR body per directive #3 above. `coordinated-wrap-up` to
   close out (PR + report only — no merge/board, that's the Coordinator's).
6. Final report to Coordinator per original handoff's "Report" section: VERDICT GREEN/RED, owner
   login (URL/email/password), whether restart was required (plain finding, expected yes),
   Playwright assertion/log path, script path, LAN URL; leave `jarvis-uat-1006` UP if GREEN, tear
   down if RED. Guardrails from original handoff still apply verbatim (isolated stack only, no
   editing Instance-modules UI code, no `git add -A`, no touching `docs/coordination/`, no
   repo-wide `pnpm format`).

## Full original handoff doc

Still at
`/tmp/claude-1000/-home-ben-Jarv1s--claude-worktrees-coord-2026-06-30-rfa-fleet/58a78927-385c-4b1d-8fa0-94db20255d6f/scratchpad/devproof/handoff-1006-persist-and-prove.md`
— read it fresh if any Stage 2 mechanical detail here is ambiguous (env file path, port, project
name are all specified there exactly).
