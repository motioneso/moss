# Handoff — #1869 slice 2 (chat.getCurrentTime), relay 1

**Issue:** #1869 slice 2 only (the on-purpose clock check tool). Do NOT build slice 3A or 3B —
separate lanes own those.
**Spec:** `docs/superpowers/specs/2026-08-30-1869-date-time-context.md` — Decisions #7-8, "Explicit
clock check" architecture, Exit Criteria 4, 5, 12.
**Plan:** `docs/superpowers/plans/2026-08-30-1869-date-time-context.md` — already approved, already
has a full Slice 2 section (lines 162-206) with exact contracts, output fields, manifest entry,
test cases, and verification commands. Read that section only — do not re-plan, do not read the
whole plan file, do not read the whole spec file.
**Branch/worktree:** this one (`resume-1869-clock-tool`, branch `resume/1869-clock-tool`).
**Coordinator:** agent name `coordinator` (confirm via `herdr agent list` before messaging).
**No code has been written yet.** Working tree is clean. This is a pure "plan verified, now build"
handoff — nothing to lose.

## What's confirmed (don't re-derive)

- No duplicate-lane risk: coordinator confirmed (see its message) this session is sole owner of
  slice 2; the old `issue-1869-slice2` lane (pane w1:p3T, worktree `build/1869-current-time`) is
  retired — never message or use it.
- Seams check already done and matches the plan's own seams-check section exactly:
  - `packages/chat/src/manifest.ts:202-210` — `chat.getCurrentView` manifest entry to sibling.
  - `packages/chat/src/current-view-tool.ts:86` — `ToolExecute` executor shape, exported via
    `packages/chat/src/index.ts:20`.
  - `packages/module-sdk/src/index.ts:90-97` — `ToolContext.localTimezone?: string`, already
    populated by the gateway on every tool call (read AND write paths) —
    `packages/ai/src/gateway/gateway.ts:185-190,342,481-482`. Zero new plumbing needed.
  - `packages/module-sdk/src/time.ts` — `isValidTimeZone` (18), `localDayKey` (44),
    `timeZoneOffsetMinutes` (64), `resolveLocalDay` (95). Reuse these; do not add new SDK
    functions (that's slice 3A's job, different file ownership).
  - `packages/chat/src/live/time-context.ts` — slice 1's sibling pattern for local-time formatting
    (`Intl.DateTimeFormat("en-GB", { hourCycle: "h23", ... })`), useful reference for the
    `localTime` (`HH:mm:ss`) formatter this tool needs (note: slice 1's block omits seconds; this
    tool's `localTime` field needs `HH:mm:ss`, so add `second: "2-digit"`).
  - Test pattern to copy: `tests/unit/chat-attachment-tool.test.ts` (direct `execute(scopedDb,
    input, ctx, services)` calls, `ctx = { actorUserId, requestId, chatSessionId, localTimezone? }`,
    no DB needed since this tool never touches `scopedDb`).

## Next concrete steps

1. Read plan lines 162-206 (`docs/superpowers/plans/2026-08-30-1869-date-time-context.md`) for the
   exact contract (function signatures, output field names, manifest description wording, three
   required test cases).
2. TDD: write `tests/unit/chat-current-time-tool.test.ts` first (the 3 cases in the plan), then
   `packages/chat/src/current-time-tool.ts`, then wire `packages/chat/src/manifest.ts` (new
   `assistantTools` entry, sibling of `chat.getCurrentView`) and `packages/chat/src/index.ts`
   (export beside line 20).
3. Run the plan's exact Slice 2 verification block (lines 199-206) — unpiped, check exit codes.
4. Run the scoped `verify-gate` skill (not full `pnpm verify:foundation` directly).
5. Live-path proof for exit criterion 12: call `chat.getCurrentTime` through the real chat UI on
   the live dev instance (http://192.168.50.36:5173, login `ben@ben.com` / `jarvistest123!`) and
   capture the tool result in the transcript/UAT output.
6. `coordinated-wrap-up`: push (after the pre-push trio — format:check, lint, typecheck, rebase),
   open PR against `main`, post live-path proof as a PR comment, report to coordinator via
   `herdr-pane-message` with your pane id.

## Relay budget

This is relay 1. Relay depth is budgeted at ONE for build lanes (Ben's ruling, 2026-08-23). If
your own 70% trigger fires again before you have an open PR, do NOT relay again — push what's
green, write state, and report to the coordinator that the slice needs re-scoping. It should not
need to: slice 2 is small (one new file, two small edits, one new test file).
