# Relay: finish lane for PR 2273 (sports source photos, slice 2)

Relay depth 1. The predecessor hit the 70 percent context warning after the rebase and the first
gate run. Do not relay again: if your own warning fires, push what you have, update this doc, and
tell the coordinator the slice needs re-scoping.

## Read first

- Task brief: `/home/ben/.coord-briefs/boot-finish-2273.txt` (read it in full; it carries the plain
  English rule and every standing rule). You resume at STEP 4's verdict; steps 1 to 3 are done.
- Live state doc: `/tmp/finish-2273-state.md` (keep it current as you work).
- Worktree `~/Jarv1s/.claude/worktrees/photos-s2`, branch `feat/sports-source-photos-s2`.

## Done (steps 1 to 3)

- Rebased: slice 1 was already on main (PR 2245, squash), so only the slice 2 commit was replayed
  onto origin/main (`e3f06586e`). Branch is now one commit ahead of main. Five conflicts resolved
  by keeping main's versions and re-applying slice 2's small additions where main moved the code
  (`tests/unit/sports-public-source-reader-helpers.ts`, `tests/unit/sports-routes-sources.test.ts`).
- Migration renamed `0214_sports_source_photos.sql` to `0222_sports_source_photos.sql`
  (main tops out at 0217; the dev database already has 0220 and 0221 applied). Every reference
  updated: manifest, manifest test, schema catalog test, migrations test, plan doc, SQL header,
  commit message.
- Gate run 1 went red on one thing: the merged Sports settings description was over the 240
  character app-map limit. Shortened to 239 characters. `pnpm build:app-map` now exits 0.
- Gate run 2 started 00:57 on the fixed commit. Log:
  `/tmp/jarv1s-gate/photos_s2-20260905-005708.log`. Read the verdict with
  `scripts/run-gate.sh status` (exit code is the verdict; 0 green, 1 red, 2 dead). Never pipe it.
- Not pushed yet. Nothing on the remote has changed.

## Next (steps 4 to 8 of the brief)

1. Gate verdict via `scripts/run-gate.sh status`. If red, read the log with a bounded grep, fix,
   amend the single commit (path-scoped add), re-run through the verify-gate skill.
2. Sports browser tests: confirm port 4173 is free first (`ss -ltn` and look for 4173; another
   lane's browser run would be silently reused). Then run, with output to a file and no pipe:
   `pnpm exec playwright test tests/e2e/sports-photos.spec.ts tests/e2e/sports-settings.spec.ts tests/e2e/sports-result-row.spec.ts > /tmp/finish-2273-e2e.log 2>&1; echo EXIT=$?`
3. Push: `git push --force-with-lease origin feat/sports-source-photos-s2`. PR comment headed
   "Rebased onto main, migration renumbered" (use a file plus `gh pr comment 2273 -F`). Then the
   coordinator message from STEP 5 of the brief, and the bounded read-back.
4. Migration on dev: `pnpm db:migrate` from this worktree hits the live dev database on purpose
   here (it is the only step that should). Then reconcile:
   `JARVIS_RECONCILE_CONFIRM_OWNER_EMAIL=ben@ben.com pnpm db:reconcile`. The memory note
   `~/.claude/projects/-home-ben-Jarv1s/memory/dev-module-install-needs-manual-reconcile.md` was read;
   its key point is that reconcile must be run by hand and the api restarted after.
5. Start the instance: `/tmp/finish-2273-start.sh` (api 3273, web 5273, sources the chat env
   file, sets trusted origins, records pids in `/tmp/finish-2273-pids.txt`). Watch
   `/tmp/finish-2273-api.log` for the listening line with a bounded tail.
6. Live proof script is written: `/tmp/finish-2273-live.cjs`. Run from the worktree with
   `LIVE_PASSWORD='jarvistest123!' node /tmp/finish-2273-live.cjs > /tmp/finish-2273-live.out 2>&1; echo EXIT=$?`.
   It signs in, opens Sports settings, reads each source row's "Photos: ..." line, crops a
   screenshot of the rows to `/tmp/finish-2273-shots/`, posts the real retry endpoint for each
   source so the refresh records a photo outcome, reloads, and reads the rows again. Expected:
   rows read "Photos: checking" before any refresh and "Photos: working" or "Photos: none found"
   after; the GET sources response carries `photoStatus` and `photosFoundByMoss`; no row offers
   "Stop using Moss's photos" because no rule is saved yet (that is slice 3). Ben's dev sources:
   FotMob, r/LiverpoolFC, r/buffalobills. Match api log lines with a bounded grep on
   `/tmp/finish-2273-api.log` for `sports` and `photo`.
7. PR comment "Live proof (slice 2)" with the URL and evidence, `gh pr ready 2273`, stop the
   instance by the pids in `/tmp/finish-2273-pids.txt` only (kill each pid, never a name pattern).
8. Final coordinator report per STEP 8 of the brief, with a bounded read-back.

## Traps met this session

- A Bash hook blocks any command whose text contains a pipe character near gate-like words, even
  inside a heredoc or a comment. Write files with the Write tool, not heredocs, when the text has
  a pipe in it.
- App-map descriptions are capped at 240 trimmed characters; the check runs inside typecheck.
- Ports 3000 and 5173 belong to other lanes' instances; 3280, 5280, 3281, 5281 too. 1533 is prod.
