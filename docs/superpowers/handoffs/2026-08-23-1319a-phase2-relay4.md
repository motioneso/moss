# Relay 4 — #1319-A phase 2 build (catalog-verify lane)

**Status: Task B is DONE and committed at `26ec5c458` on `build/1319a-catalog-verify`.**
All green: `pnpm typecheck` exit 0; `tests/unit/module-distribution-port.test.ts` 3/3
passed; `tests/integration/module-distribution.e2e.test.ts` (isolated gate database)
14/14 passed. Coordinator was told (message sent, no reply needed).

**Plan:** `docs/superpowers/plans/2026-08-23-1319a-phase2-catalog-verify.md`
**Prior relay docs:** `docs/superpowers/handoffs/2026-08-23-1319a-phase2-relay3.md` (has
full background — only read it if something here is unclear).

**Branch/worktree:** `build/1319a-catalog-verify`, this worktree. `node_modules` already
installed — skip `pnpm install`.

**Coordinator:** registered name `coordinator`. Re-resolve fresh via `herdr agent list`
before messaging — never reuse a pane id from this doc.

## What changed since relay 3 (this session's commit, `26ec5c458`)

- `tests/integration/module-distribution.e2e.test.ts`: mock registry signs every served
  `index.json` (new `/index.json.sig` branch using the already-committed
  `catalogTestKeypair`); `registryIndexRequestCount` now increments and is asserted
  unchanged across the non-admin-denial test; added `catalogVerification` /
  `catalogDigestSha256` assertions to the verified-listing test; added a new
  `"returns the disabled envelope when external modules are turned off"` test.
- `apps/api/src/server.ts`: new TEST-ONLY `CreateApiServerOptions.__testExternalModulesEnabled`
  (defaults to the existing always-on `true`) — a coordinator-approved decision (option 1
  of two offered), so the `enabled:false` module-registry response is reachable from a
  test without any production toggle. Never set outside tests.
- New `tests/unit/module-distribution-port.test.ts`: exercises
  `createModuleDistributionPort`'s registry cache directly (fake fetch, no DB) — cache
  hit returns the identical snapshot object, `refresh:true` bypasses the cache, a failed
  refetch never overwrites a previously-cached good snapshot.

Also needed and run once (not part of the commit, regenerate if missing):
`pnpm build:app-map` — the integration test run failed the first time with
`ENOENT dist/app-map.json` until this was run. If your `dist/` is fresh/clean, run it
again before `pnpm test:integration`.

## What's NOT done — pick up here

Task B itself is complete. What's left is the **wrap-up**, per relay 3's "Next concrete
steps" section (still accurate, repeated here so you don't have to open that doc):

1. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck` (run un-piped, check
   real exit codes — see `verify-gate` skill for why piping lies). Then
   `git fetch origin main && git rebase origin/main`.
2. Use the `coordinated-wrap-up` skill: full gate via the `verify-gate` skill (fresh
   isolated gate database, never the live dev database — drop+create a `jarvis_gate_*`
   name, `export JARVIS_PGDATABASE=...`, drop it again when done), push, open a
   non-draft pull request, post the exact commands and their exit codes as evidence on
   the pull request.
   This lane is backend/API-only (no user interface in this phase) — confirm with the
   coordinator whether live end-to-end proof on a running instance is still expected for
   this lane or deferred to phase 3, before you claim the work is fully done.
3. Do not merge, do not move the project board, do not touch coordination files — report
   to the coordinator and stop.

## Preservation constraints (still binding)

- Fixed verification enums exactly as specified in the master plan's Task 3 (already
  shipped).
- Raw-byte digest/signature contract (already shipped).
- Atomic snapshot invariant — one `fetchRegistryIndex` call produces entries,
  verification, and digest together; the cache never mixes fields from different
  fetches; a failed fetch is never cached. Now covered by both the new unit test and
  the D6 signing coverage.
- The check that runs "is this user an admin" happens before the code ever calls out to
  the registry server — now covered by an explicit test, not just by reading the code.
- Hard boundary — do not touch pipeline enforcement/409/override (phase 3),
  ensure-at-boot (phase 3 task 6), the web interface (phase 3 task 7), user acceptance
  testing (phase 4), or any database migration. Phase 1 (the signing code itself) is
  already merged to the main branch in pull request #1684 — treat it as read-only for
  this lane.

## Reminders

Security-sensitive area — build defensively, never put internal error details from the
cryptography or the signing library into a message a normal user or an external log
could see. Never `git add -A` or a bare `git commit` in this shared worktree — use the
`shared-checkout` skill's explicit-file-path method every time, including for this doc.
Resolve `coordinator` fresh via `herdr agent list` before messaging (confirm exactly one
live match) — never reuse a pane number written in this or any other document, since
those numbers change every time a pane opens or closes. Sign every message to the
coordinator with your own pane id.

Write everything you say to the coordinator, or in any handoff document you write, in
plain everyday English. Do not invent shorthand words. Only give the exact name of a
command or a file when the reader has to go type it or open it themselves. This applies
to every message, not just the first one.
