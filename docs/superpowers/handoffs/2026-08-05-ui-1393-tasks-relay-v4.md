# #1393 Tasks CSS-split relay v4 — execute, don't re-derive

Supersedes `2026-08-05-ui-1393-tasks-relay.md` (v3). All grep triage is DONE and the full
property-by-property split is worked out below. Do not re-grep, do not re-read the source files
for triage — just execute steps 1-6.

Standing instruction unchanged: chain straight through steps 1-6 without stopping for approval.
Open the PR at the end, do NOT merge it. Report to the coordinator pane labeled "DESIGN ELEMENTS"
(re-resolve by label via `herdr pane list`) only when the PR opens, or on a real blocker.

Ground truth for the split (from `scripts/check-design-tokens.ts`): banned set is EXACT-MATCH on
property name — `color, background, background-color, background-image, border, border-color,
border-radius, font-family, font-size, font-weight, box-shadow, outline, fill, stroke, filter`.
**`border-top`/`border-bottom`/`border-left`/`border-right` are NOT banned** (verified live in
already-migrated `kit-chat.css`) — leave them in the app-side files. `animation`, `opacity`,
`text-decoration`(+`-color`), `transform`, `float`, `font` (shorthand — not `font-family` etc,
never occurs here) are not banned either.

**Correction vs v3**: `.tk-modal-scrim`, bare `.tk-modal__title`, `.tk-modal__body`,
`.tk-modal__foot` **do not exist in the CSS at all** (already gone, v3's notes on them were
stale). `.tk-modal` itself, `.tk-modal .jds-dialog__body`, `.tk-modal__head`, `__headmain`,
`__eyebrow`, `__x`(+`:hover`), and the `@media (prefers-reduced-motion)` block on `.tk-modal` are
all **LIVE** (className passthrough onto the Dialog, confirmed via grep in
task-details-dialog.tsx:265). The `.tk-modal .jds-select/.jds-selectwrap/.jds-segmented/
.jds-segmented__opt` descendant rules are also live (Select/Segmented render inside this Dialog) —
no banned props in them anyway, so no action needed either way.

## Step 1 — DEAD selectors to delete entirely (not moved anywhere)

`apps/web/src/styles/kit-tasks.css`: `.tk-bar__group`, the whole `.tk-div*` family (`.tk-div`,
`:first-child`, `::before/::after`, `__label`, `__dot`, `__name`, `__count`), the whole
`.tk-effort*` family EXCEPT `tk-effort-fill` (delete `.tk-effort`, `__tick`(+nth-child, `.on`),
`.tk-effort-dots`(+span, `.on`), `.tk-effort-label`, `.tk-effort-time`(+`.ic`)), the whole
`.tk-empty*` family (`.tk-empty`, `__mark`, `__title`, `__sub`), the whole `.tasks--compact *`
block (5 rules).

`apps/web/src/styles/kit-tasks-modal.css`: `.tk-flabel`, bare `.tk-titlein`(+`:focus`), bare
`.tk-native`(+`:focus`), bare `.tk-people`(+`__menu`+`__item`+`:hover`), `.tk-act__bot`,
`.tk-act--jarvis .tk-act__text`, `.tk-act__typing`, `.tk-dots`(+`i`+nth-child+`@keyframes
tk-pulse`), `.tk-act-empty`(+`b`), `.tk-act-send`(+`:hover`+`:disabled`),
`.tk-act-mention-menu`, `.tk-person__x`(+`:hover`).

`apps/web/src/tasks/tasks.css`: `.tk-statusctl__main`(+`:hover`+`.is-done`+`.is-done:hover`+
`.is-archived`), `.tk-statusctl__more`(+`:hover`), the two adjacent-sibling combinators
(`.tk-statusctl__main.is-done + .tk-statusctl__more`, `.tk-statusctl__main.is-archived +
.tk-statusctl__more`).

Everything else in all three files is LIVE — keep the selector, split its properties per Step 2.

## Step 2 — create `packages/ui/src/styles/components-tasks.css`

Header comment same style as `components-wellness-1.css`. For every LIVE selector below, keep
ONLY the listed banned properties in the new file, under the identical unrenamed selector name
(never rename — confirmed by `.wl-hero`/`.chatd*` precedent). Then in the three app-side files,
delete those exact declaration lines (keep the selector block with its remaining layout
properties; if a rule's properties are ALL banned, delete the whole rule from the app file).

### From kit-tasks.css (selector → banned props to relocate)
- `.tk-bar__sep` → `background`
- `.tk-tagfield` → `border, border-radius, background`
- `.tk-tagfield:focus-within` → whole rule (border-color, box-shadow)
- `.tk-tagfield .ic` → `color`
- `.tk-tagfield input` → `border, background, font-family, font-size, color`
- `.tk-tagfield input:focus` → whole rule (outline)
- `.tk-tagfield input::placeholder` → whole rule (color)
- `.tk-tagfield__action` → `border, border-radius, background, color`
- `.tk-tagfield__action:hover:not(:disabled)` → whole rule (background, color)
- `.tk-tagmenu` → `background, border, border-radius, box-shadow`
- `.tk-tagmenu__hd` → `font-size, font-weight, color`
- `.tk-tagmenu__item` → `border, background, border-radius, font-family, font-size, color`
- `.tk-tagmenu__item:hover, .tk-tagmenu__item.is-active` → whole rule (background, color)
- `.tk-tagmenu__item .hash` → whole rule (font-family, color)
- `.tk-tagmenu__item:hover .hash, .tk-tagmenu__item.is-active .hash` → whole rule (color)
- `.tk-tagmenu__empty` → `font-size, color`
- `.tk-tagmenu__item .ct` → `font-family, font-size, color`
- `.tk-tagmenu__item.is-active .ct, .tk-tagmenu__item:hover .ct` → whole rule (color)
- `.tk-listbtn` → `border, border-radius, background, font-family, font-size, font-weight, color`
- `.tk-listbtn:hover` → whole rule (border-color)
- `.tk-listbtn.is-open` → whole rule (border-color, box-shadow)
- `.tk-listbtn__dot` → `border-radius`
- `.tk-listbtn__chev` → `color`
- `.tk-listbtn.is-on` → whole rule (border-color)
- `.tk-listbtn__hidden` → whole rule (color, font-weight)
- `.tk-tagmenu__item.is-solo` → whole rule (background)
- `.tk-tagmenu__item.is-solo .nm` → whole rule (font-weight, color)
- `.tk-liststate` → `font-family, font-size, font-weight, border-radius`
- `.tk-liststate--only` → whole rule (background, color)
- `.tk-liststate--hidden` → whole rule (background, color)
- `.tk-tagmenu__hint` → `font-size, color`
- `.tk-activetags__lbl` → `font-size, font-weight, color`
- `.tk-activetags__clear` → `border, background, color, font-family, font-size`
- `.tk-activetags__clear:hover` → `color` (keep `text-decoration` in app)
- `.tk-activetags__note` → whole rule (color, font-size)
- `.tk-add` → `background, border, border-radius, box-shadow`
- `.tk-add:focus-within` → whole rule (border-color, box-shadow)
- `.tk-add__plus` → `color`
- `.tk-add input` → `border, background, font-family, font-size, color`
- `.tk-add input:focus` → whole rule (outline)
- `.tk-add input::placeholder` → whole rule (color)
- `.tk-task` → `border-radius, border`
- `.tk-task:hover` → `background, box-shadow` (keep `transform` in app)
- `.tk-task__bar` → `border-radius`
- `.tk-task__main` → `background, border, color`
- `.tk-task__title` → `font-size, color`
- `.tk-task--done .tk-task__title` → `color` (keep text-decoration+color in app)
- `.tk-meta-due` → `font-family, font-size, color`
- `.tk-meta-due--overdue, .tk-meta-due--today` → whole rule (color)
- `.tk-listchip` → `font-size, font-weight, color`
- `.tk-listchip__dot` → `border-radius`
- `.tk-metatag` → whole rule (font-family, font-size, color)
- `.tk-metatag:hover` → whole rule (color)
- `.tk-metamini` → `font-size, color`
- `.tk-task__src` → `font-size, color`
- `.tk-task__open` → `color, border, background, border-radius`
- `.tk-task__open:hover` → whole rule (background, color)
- `.tk-panel` → `background, border, border-radius`
- `.tk-panel__head` → `background` (border-bottom NOT banned, stays in app)
- `.tk-panel__dot` → `border-radius`
- `.tk-panel__name` → `font-family, font-size, font-weight, color`
- `.tk-panel__count` → `font-family, font-size, color`
- `.tk-quad` → `border, border-radius, background`
- `.tk-quad__tag` → `font-family, font-size, font-weight`
- `.tk-quad__tag .dot` → `border-radius`
- `.tk-quad__verb` → `font-family, font-size, font-weight, color`
- `.tk-quad__n` → `font-family, font-size, color`
- `.tk-quad__empty` → `font-size, color`
- `.tk-quad--do .tk-quad__head` → whole rule (background)
- `.tk-quad--do .tk-quad__tag` → whole rule (color)
- `.tk-quad--do .tk-quad__tag .dot` → whole rule (background)
- `.tk-quad--schedule .tk-quad__head` → whole rule (background)
- `.tk-quad--schedule .tk-quad__tag` → whole rule (color)
- `.tk-quad--schedule .tk-quad__tag .dot` → whole rule (background)
- `.tk-quad--delegate .tk-quad__head` → whole rule (background)
- `.tk-quad--delegate .tk-quad__tag` → whole rule (color)
- `.tk-quad--delegate .tk-quad__tag .dot` → whole rule (background)
- `.tk-quad--eliminate .tk-quad__head` → whole rule (background)
- `.tk-quad--eliminate .tk-quad__tag` → whole rule (color)
- `.tk-quad--eliminate .tk-quad__tag .dot` → whole rule (background)
- `.tk-foot` → `color, font-size`
- `.tk-foot .ic` → whole rule (color)
- `.tk-toolbar`, `.tk-toolbar.single`, `.tk-list`, `.tk-row-actions` → no banned props, untouched

### From kit-tasks-modal.css
- `.tk-modal`, `.tk-modal .jds-dialog__body`, `.tk-modal__head`, `.tk-modal__headmain` → no banned
  props, untouched
- `.tk-modal__eyebrow` → `font-family, color`
- `.tk-modal__x` → `border-radius, border, background, color`
- `.tk-modal__x:hover` → whole rule (background, color)
- `@media (prefers-reduced-motion:reduce) { .tk-modal { animation:none } }` → untouched (drop
  `.tk-modal-scrim` from the selector — it never existed as a rule target, this is just `.tk-modal`)
- `.tk-modal .jds-select/.jds-selectwrap/.jds-segmented/.jds-segmented__opt` → no banned, untouched
- `.tk-form`, `.tk-field`, `.tk-field--full` → no banned, untouched
- `.tk-textarea` → `border, border-radius, font-family, font-size, color, background`
- `.tk-textarea:focus` → whole rule (outline, border-color, box-shadow)
- `.tk-tagedit` → `border, border-radius, background`
- `.tk-tagedit:focus-within` → whole rule (border-color, box-shadow)
- `.tk-tagedit input` → `border, background, font-family, font-size, color`
- `.tk-tagedit input:focus` → whole rule (outline)
- `.tk-tagsugg` → no banned, untouched
- `.tk-tagsugg__btn` → `border, background, color, border-radius, font-family, font-size`
- `.tk-tagsugg__btn:hover` → `border-color, color` (keep `border-style` in app)
- `.tk-subs`, `.tk-sub` → no banned, untouched
- `.tk-sub__box` → `border-radius, border, color`
- `.tk-sub__box.is-on` → whole rule (background, border-color)
- `.tk-sub input` → `border, background, font-family, font-size, color` (keep border-bottom+padding+flex+min-width in app)
- `.tk-sub input:focus` → `outline` (keep border-bottom-color in app)
- `.tk-sub--done input` → `color` (keep text-decoration+color in app... wait: text-decoration-color
  stays, only `color` moves)
- `.tk-sub__rm` → `border, background, color, border-radius`
- `.tk-sub__rm:hover` → whole rule (background, color)
- `.tk-sub__add` → `border, background, color, font-family, font-size, font-weight`
- `.tk-sub__add:hover` → no banned (text-decoration only), untouched
- `.tk-ava` → `border-radius, color, font-family, font-weight`
- `.tk-peoplefield` → `border, border-radius, background`
- `.tk-peoplefield:focus-within` → whole rule (border-color, box-shadow)
- `.tk-peoplefield input` → `border, background, font-family, font-size, color`
- `.tk-peoplefield input:focus, :focus-visible` → whole rule (outline, box-shadow — none banned
  actually: outline IS banned, box-shadow IS banned → whole rule moves)
- `.tk-person` → `border, border-radius, background, font-size, font-weight, color`
- `.tk-person:hover` → whole rule (border-color)
- `.tk-person__nm` → no banned, untouched
- `.tk-person__me` → whole rule (color, font-weight)
- `.tk-activity`, `.tk-act-list`, `.tk-act`, `.tk-act__body`, `.tk-act__head` → no banned, untouched
- `.tk-act__who` → `font-size, font-weight, color` (keep white-space in app)
- `.tk-act__when` → `font-family, font-size, color` (keep margin-left/flex/white-space in app)
- `.tk-act__text` → `font-size, color, background, border, border-radius`
- `.tk-act__text .mention` → whole rule (color, font-weight)
- `.tk-act-composer` → `background, border, border-radius`
- `.tk-act-composer:focus-within` → whole rule (border-color, box-shadow, background)
- `.tk-act-composer textarea` → `border, background, font-family, font-size, color`
- `.tk-act-composer textarea:focus, :focus-visible` → whole rule (outline, box-shadow)
- `.tk-act-composer textarea::placeholder` → whole rule (color)
- `.tk-act-hint` → `font-size, color`
- final combined `:focus-visible` suppression rule (`.tk-add input`, `.tk-tagfield input`,
  `.tk-tagedit input`, `.tk-sub input`) → whole rule (outline, box-shadow)

### From tasks.css
- `.tk-modal__titlein` → `border, background, font-family, font-size, font-weight, color`
- `.tk-modal__titlein:focus` → whole rule (outline)
- `.tk-modal__titlein::placeholder` → whole rule (color)
- `.tk-statusctl`, `.tk-statusctl__menu` → no banned, untouched
- `.tk-effort-fill` → `color`
- `.tk-subadd` → no banned, untouched
- `.tk-subadd input` → `border, background, font-family, font-size, color`
- `.tk-subadd input:focus` → whole rule (outline)
- `.tk-subadd input::placeholder` → whole rule (color)
- `.task-capture`, `.tk-bar__r1`, `.tk-bar__search`(+`.tk-tagfield`) → no banned, untouched
- Inside `@media (max-width:560px)`: `.tk-msrch` → `background, border, border-radius, color`
  (nested inside the same media query in components-tasks.css); `.tk-msrch:hover,
  .tk-msrch.is-active` → whole rule (background, color), also nested in the media query. All
  other rules in that media block have no banned props — untouched.

## Step 3 — property-by-property split check

After writing all 4 files: for each of the 3 app-side files, grep it for the 15 banned property
names using the SAME regex the guard uses — must be zero matches (this is literally
`pnpm check:design-tokens` once Step 5's registration lands, but check pre-registration too via a
manual grep pass so you're not debugging guard failures blind).

## Step 4 — dead-selector burn-down in root `apps/web/src/styles.css`

Diff against BASE commit `5ff2423d` (`git diff 5ff2423d -- apps/web/src/styles.css`, never plain
`git diff`). Keep `.empty-state`, `.notification-link`, `.notification-badge`, `page-stack`/
`page-heading`/`eyebrow`. Do NOT delete `.notification-row.unread` until the `10-notifications`
capture proves `.jds-task--unread` renders correctly — if it doesn't, STOP and report to
coordinator (kill gate, not a judgment call).

## Step 5 — register guard lists

`scripts/check-design-tokens.ts` `MIGRATED_SECTION_CSS_FILES`: add `apps/web/src/styles/
kit-tasks.css`, `apps/web/src/styles/kit-tasks-modal.css`, `apps/web/src/tasks/tasks.css` (11→14).
Do NOT add root `styles.css`.

`scripts/check-migrated-sections.ts` `MIGRATED_SECTION_PATHS`: add the 7 section TSX paths (6 in
`apps/web/src/tasks/` + `apps/web/src/notifications/notifications-page.tsx`) (49→56).

## Step 6 — gates, capture, live-path, PR

Run unpiped (never through `tail`/`grep`, masks red as green), explicit exit codes:
`check:design-tokens`, `check:migrated-sections`, `check:ui-classes`, `pnpm build:ui-catalogue`
then `check:ui-catalogue`, `verify:foundation` on a freshly-exported gate DB (never live dev DB —
see memory `gate-db-isolation-mandatory`). Run focused browser assertions; confirm changes are confined
to the 4 approved amendments from sign-off `5188097889` — kill gate on any other visible change or
lost unread/read styling. Live-path proof on a dev instance (tasks list+matrix, details dialog,
edit date field, tag menu, change status; notifications filter/mark-read/both empty states)
recorded on the PR. Post the required note on issue #1405 (notifications adopted raw `jds-task`,
family gained authored `--unread` modifier). When green: `git fetch origin main && git merge
origin/main`, pre-push trio (`format:check && lint && typecheck`), push, open PR against `main`
with a release-note-style summary including live-path proof + the #1405 note. Report PR number +
measured guard numbers (expect 14 and 56) to coordinator pane "DESIGN ELEMENTS". Do not merge,
close the issue, or move the board.

## Shared-checkout git safety (CLAUDE.md, unchanged)

No `git add -A`/`.`, no bare `git commit` — explicit paths only. `git diff <path>` before
committing a shared file; `git show --name-only HEAD` after.
