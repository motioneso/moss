# Plan — 1686 UI Polish & Layout Implementation

**Spec:** `docs/superpowers/specs/2026-08-18-1686-ui-polish.md` (approved)
**Issue:** Part of #1686
**Risk tier:** routine — CSS/copy only, 5 files, no shared-table/auth/RLS/migration surface.

## Seams check

All six items are pure CSS-value/copy/wrapper-`<div>` edits against classes and tokens that
already exist and are already wired into rendering — no new platform capability assumed.

- `--rail-fg-muted` defined `apps/web/src/styles/tokens.css:275`, consumed by sidebar category
  headers (existing wiring, unchanged).
- `.cmd-empty` used at `apps/web/src/today/today-page.tsx:436` (copy-only change, class unchanged).
- `.tk-list` defined `apps/web/src/styles/kit-tasks.css:404`, applied
  `apps/web/src/notifications/notifications-page.tsx:73`.
- `Segmented` imported/rendered `apps/web/src/settings/settings-appearance-pane.tsx:23,172`.
- Button transition/hover rules `apps/web/src/styles.css:161-166` (transition) and `:201-205`
  (hover).
- `--space-4` (16px) and `--shadow-sm` both defined in `apps/web/src/styles/tokens.css` (two
  `--shadow-sm` definitions exist — one per theme block; task 6 uses the token, not a literal, so
  it resolves correctly in both themes).

No open questions — spec premises verified current on this branch (see conversation: all six grep
checks matched, line numbers drifted by ≤25 lines from the spec's "around line N" estimates, no
semantic drift).

## Determinism boundary

N/A — no model output, no chat turns, no module involved. All six changes are static CSS/JSX/copy.

## Tasks

Each task is one commit, `git add` by explicit path only.

### Task 1 — Sidebar text contrast
- File: `apps/web/src/styles/tokens.css:275`
- Change: `--rail-fg-muted: rgba(237, 229, 210, 0.72);` → `0.90`
- Verify: `grep -n -- "--rail-fg-muted" apps/web/src/styles/tokens.css` shows `0.90`; exit 0.

### Task 2 — Today empty-state copy
- File: `apps/web/src/today/today-page.tsx:436`
- Change: `No events today.` → `No events today. Enjoy the free time!` (keep `.cmd-empty` class
  as-is — that class is the authored empty-state pattern, do not touch it)
- Verify: `grep -n "Enjoy the free time" apps/web/src/today/today-page.tsx`; exit 0.

### Task 3 — Tasks empty-state icon
- No-op per spec. No file touched, no commit.

### Task 4 — Notifications list breathing room
- Files: `apps/web/src/notifications/notifications-page.tsx:73`,
  `apps/web/src/styles/kit-tasks.css:404`
- Change: `<section className="tk-list" ...>` → `<section className="tk-list tk-list--loose" ...>`;
  add new rule directly below `.tk-list { display: grid; gap: 0.65rem; }`:
  ```css
  .tk-list--loose {
    gap: var(--space-4);
  }
  ```
- Verify: `grep -n "tk-list--loose" apps/web/src/notifications/notifications-page.tsx
  apps/web/src/styles/kit-tasks.css`; both files match, exit 0.

### Task 5 — Settings appearance segmented spacing
- Files: `apps/web/src/settings/settings-appearance-pane.tsx:172`,
  `apps/web/src/styles/settings.css` (append at end of file)
- Change: wrap the existing `<Segmented ariaLabel="Color mode" .../>` in
  `<div className="appearance-theme-mode">...</div>` (props unchanged); append:
  ```css
  .appearance-theme-mode {
    margin-bottom: var(--space-4);
  }
  ```
- Verify: `grep -n "appearance-theme-mode" apps/web/src/settings/settings-appearance-pane.tsx
  apps/web/src/styles/settings.css`; both files match, exit 0.

### Task 6 — Button hover shadow + focus guard
- File: `apps/web/src/styles.css:161-205`
- Change A (split transition rule): remove `button` from the `input, select, textarea, button, a`
  transition selector; add a new `button { transition: box-shadow 0.1s var(--ease-out),
  background-color 0.1s var(--ease-out); }` rule immediately after.
- Change B (hover rule): `.secondary-button:hover, .ghost-button:hover, .icon-button:hover { ... }`
  → add `:not(:focus-visible)` to each selector and add `box-shadow: var(--shadow-sm);` alongside
  the existing `background: var(--panel-subtle);`.
- Verify: `grep -n "not(:focus-visible)" apps/web/src/styles.css`; exit 0. Manual check: `button`
  no longer appears in the `input, select, textarea, a` transition selector line (`grep -n "^button,\|^input,\|^select,\|^textarea,\|^a {" apps/web/src/styles.css`
  — confirm `button` has its own standalone rule).

## Verification (per commit and at wrap-up)

```bash
pnpm --filter web typecheck > /tmp/1686-typecheck.log 2>&1; echo "EXIT=$?"
```
Expected: `EXIT=0`. (No unit/integration tests target these files — copy/CSS-only; typecheck is
the correctness gate for the two `.tsx` edits, visual review covers CSS.)

Full gate (`coordinated-wrap-up`, isolated gate DB) at wrap-up time, not per-task.

## E2E / live-path proof (single phase — no phase split, task is too small to phase)

Live dev instance, one Playwright/manual pass touching all five changed screens:
- Sidebar: category header contrast visibly brighter (both themes).
- Today: empty state shows new copy (seed a day with 0 events).
- Notifications: list item gap visibly looser (`--space-4` vs `0.65rem`).
- Settings → Appearance: segmented control has visible bottom margin.
- Any button: hover shows subtle shadow; tabbing to a button and using keyboard focus does NOT
  show the hover background/shadow (only the focus ring) — proves the `:not(:focus-visible)`
  guard.

Screenshot or bounded DOM check per item, posted as `gh pr comment` per `coordinated-wrap-up`.

## Kill gate

None needed — single phase, six independent one-file-or-two-file edits, no architectural risk.
If task 6's `:not(:focus-visible)` guard turns out to regress keyboard-focus visibility on a real
browser (not just DOM inspection), drop change B and keep only change A (transition split), and
flag it to the coordinator rather than improvising a different guard.
