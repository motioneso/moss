# Owner-draft invocation and private storage proof (issue #2267)

Part of #2023. Approved spec: `docs/superpowers/specs/2026-09-04-workshop-projects-and-supervised-builds.md`
(section P2, line 439). Approved plan: `docs/superpowers/plans/2026-09-04-workshop-projects-and-supervised-builds.md`
sections A2 (line 178) and A3 (line 209). This document only re-scopes those two sections into a
single-issue build; it does not add new decisions. All source code is lifted verbatim from the
already-reviewed, already-live-tested branch `build/workshop-phase-a-0904` (recorded evidence:
issue #2267 body, run r4, exit 0, 1 passed in 2.0m). That branch must never be merged, rebased, or
pushed to — files are copied out of it with `git show <branch>:<path>`.

## Gates

- Spec approved: yes (P2, cited above).
- Task issue open: #2267, state OPEN.
- No new module or UI surface: the fixture module and its screen were already designed and
  reviewed as part of the 2026-09-04 spec's UAT harness; this task does not add a new product
  surface, so no fresh mockup review is required.

## Seams check (file:line on current `main`, verified in this worktree)

- Gate to correct: `apps/worker/src/external-module-invoke.ts:170-173` rejects any
  `status !== "enabled"` with `"not-enabled"` before the hash check; drafts are never admitted today.
- `owner_user_id` column already exists: `packages/settings/sql/0187_external_modules_draft_owner.sql:9`.
- `createExternalBriefingInvoker` already exported: `apps/worker/src/external-module-invoke.ts:271`
  (read-risk caller the integration test also exercises).
- Existing enabled-only KV policies to preserve: `packages/settings/sql/0157_module_worker_runtime_access.sql`.
- `app.current_actor_is_admin()` already defined: `packages/settings/sql/0065_module_enablement.sql:44`.
- `installModuleDraft`: `packages/module-registry/src/external/install-draft.ts:38`.
- `listExternalModuleStates` / `setExternalModuleDraft`: `packages/settings/src/repository-external-modules.ts:77,166`.
- UAT helpers already present: `restartUatStack` at `tests/uat/provisioner.ts:632`;
  `UAT_SECOND_OWNER_EMAIL/PASSWORD/ID` at `tests/uat/seed/admin.ts:10-14`.
- Routes already present: `DELETE /api/admin/modules/:id/draft` at
  `packages/module-registry/src/route-guard.ts:117` and `packages/settings/src/routes-modules.ts:353`;
  `POST /api/modules/:moduleId/queues/:queueName/run` at
  `packages/module-registry/src/route-guard.ts:135`; admin promote at `packages/settings/src/routes.ts:604-631`.
- Fixture web/worker SDK surfaces already exist: `packages/module-web-sdk/src/runtime.ts` (h,
  useState, useEffect, ReactNodeLike); `packages/module-sdk/src/worker.ts:173` (`defineModuleWorker`).
- `scripts/build-external-module.ts` exists (imported by extension-less `.js` specifier, matching
  this repo's existing ESM import convention for other `apps/worker/src/*.ts` files).
- Migration numbering: highest applied number on `main` is `0225`
  (`packages/*/sql/022*.sql`, checked across the whole repo, not just `packages/settings/sql`).
  `0226` does not exist anywhere in the tree. The branch's `0215_module_kv_worker_owner_draft.sql`
  is the only new migration this task adds; it is renamed to `0226_module_kv_worker_owner_draft.sql`.
  Re-check `0226` is still free immediately before committing (other lanes are running).
- No other migration numbers from the branch (`0216`, `0217`) are in scope — those belong to a
  different workstream (D1/D2, workshop project tables) and are not part of this issue.

## Determinism boundary

No model is involved anywhere in this task. The invoker correction is a pure authorization check
against database state. The fixture module's UI text ("Saved in your private list.", "Not saved
yet.", etc.) is rendered from the confirmed KV read after each queued mutation completes — never
from generated content. There is no prompt and no guidance budget to state.

## Task 1 — A2: verified invocation for the owner's draft

**Own:** `apps/worker/src/external-module-invoke.ts`,
`tests/unit/external-module-draft-invoke.test.ts` (new),
`tests/integration/module-worker-queue-ai.test.ts`,
`packages/settings/sql/0226_module_kv_worker_owner_draft.sql` (renamed from the branch's `0215`).

**Copy exactly** (content already reviewed on the branch; verified compiling against current
`main` APIs above):

1. `apps/worker/src/external-module-invoke.ts` — the 3-line correction: select `owner_user_id`
   alongside `status`/`manifest_hash`/`package_hash`; compute
   `canInvokeDraft = state?.status === "draft" && state.owner_user_id === args.actorUserId`; the
   `not-enabled` rejection becomes `state?.status !== "enabled" && !canInvokeDraft`. No other
   line in this file changes. `VerifiedInvoke`'s exported signature is unchanged.
2. `tests/unit/external-module-draft-invoke.test.ts` — new file, copied verbatim. Covers: owner
   draft with matching hashes reaches the runtime; another active admin or an inactive actor is
   rejected (`not-enabled` / `not-active` respectively — active-membership is checked before
   ownership, so an inactive non-owner fails `not-active` first); stale manifest or package hash
   fails even for the owner; enabled-module behavior and disabled/missing/undiscovered rejections
   are unchanged.
3. `tests/integration/module-worker-queue-ai.test.ts` — copied diff: import
   `createExternalBriefingInvoker`, build both a job handler and a briefing invoker from the same
   deps, and add three cases: an active owner's draft with matching hashes is invoked; a
   write-risk queued job can persist and read back declared user storage; a read-risk briefing
   invocation cannot mutate that storage (`forbidden_kv_mutation`).
4. `packages/settings/sql/0226_module_kv_worker_owner_draft.sql` — copied from the branch's 0215,
   renamed only. DDL (unchanged from the branch):

   ```sql
   -- #2267: the verified worker invoker admits owner drafts, but 0157's KV policies
   -- admit only enabled modules. Add the narrow user-storage path needed by drafts.
   -- Existing enabled-module and instance-storage policies remain authoritative.
   CREATE POLICY module_kv_worker_owner_draft ON app.module_kv
     FOR ALL TO jarvis_worker_runtime
     USING (
       scope = 'user'
       AND owner_user_id = app.current_actor_user_id()
       AND module_id = app.current_module_id()
       AND app.current_actor_is_admin()
       AND EXISTS (
         SELECT 1 FROM app.external_modules module
         WHERE module.id = module_kv.module_id
           AND module.status = 'draft'
           AND module.owner_user_id = app.current_actor_user_id()
       )
     );
   -- FOR ALL uses the USING expression as WITH CHECK too, so inserts and updates
   -- cannot change the owner, module, scope or draft authority to escape the policy.
   ```

**Verify (unpiped, expected exit 0 each):**

```bash
pnpm --filter @moss/worker exec vitest run ../../tests/unit/external-module-draft-invoke.test.ts > /tmp/t1-unit.log 2>&1; echo "EXIT=$?"
pnpm typecheck > /tmp/t1-typecheck.log 2>&1; echo "EXIT=$?"
```

The integration test (`tests/integration/module-worker-queue-ai.test.ts`) and the migration itself
touch the database — run both only through the `verify-gate` skill's scoped recipe, never directly.

**Exit criteria:** unit test passes; typecheck passes; the migration applies cleanly under
verify-gate's scoped run with the new integration test cases passing. Commit this task's files
together (unit test, invoker fix, migration, integration test diff) once green.

## Kill gate (after Task 1, before Task 2)

**Owner: the build agent, escalate to the coordinator before proceeding either way.** Continue to
Task 2 only if Task 1's unit and integration tests pass under one honest verify-gate run — this is
a straight copy of already-reviewed code, so a first-attempt failure means something in `main` has
drifted since the branch was tested (a changed column, a renamed export), not a design problem.
Two identical failures on the same fix are the stop condition: diagnose the drift once, and if a
second identical failure follows, stop and escalate to the coordinator rather than retrying a third
time or reworking the design — the design was already accepted with recorded live evidence.

## Task 2 — A3: installed private-draft storage proof

**Own:** `tests/uat/specs/workshop-storage.uat.spec.ts` (new),
`tests/uat/fixtures/workshop-word/` (new: `jarvis.module.json`, `package.json`,
`src/web/index.ts`, `src/worker/index.ts`), `tests/uat/seed/chunks/workshop-storage.ts` (new),
`tests/uat/provisioner.ts`, `tests/uat/run-uat.ts`, `tests/uat/seed/cli.ts`,
`tests/uat/seed/levels.ts`, `tests/uat/seed/types.ts`, `tests/uat/compose.override.yml` (new).

**Copy exactly** (all content already recorded as passing on the branch: exit 0, 1 passed, 2.0m):

1. `tests/uat/compose.override.yml` — new file. Adds `-f tests/uat/compose.override.yml` to
   `buildUatComposeArgs` so each UAT run gets its own `jarv1s` container name instead of the fixed
   production one, and gives the `seed` service the `jarv1s-modules` volume (env
   `JARVIS_MODULES_DIR=/data/modules`) so the seed step can stage the fixture package where the
   `jarv1s` app container will discover it.
2. `tests/uat/provisioner.ts` — add the `withWorkshopStorageFixture` opt-in flag through
   `SeedHook`, `composeSeedHook` (env `JARVIS_UAT_WORKSHOP_STORAGE_FIXTURE`), `buildUatComposeArgs`
   (append the override file), `UatProvisionOptions`, and `buildSeedHookInput` — mirrors the
   existing `activityOutcomeFixture` wiring pattern already in this file.
3. `tests/uat/run-uat.ts` — extend the `uatLevel` export regex and its named capture groups to
   recognize `withWorkshopStorageFixture: true`, one more optional group after
   `withActivityOutcomeFixture`.
4. `tests/uat/seed/types.ts` — add `withWorkshopStorageFixture?: boolean` to `SeedOptions`.
5. `tests/uat/seed/cli.ts` — read `JARVIS_UAT_WORKSHOP_STORAGE_FIXTURE` the same way the existing
   activity-outcome flag is read, and pass it into `seedLevel`.
6. `tests/uat/seed/levels.ts` — import `installWorkshopStorageFixture` from
   `./chunks/workshop-storage.js`; call it right after `seedDataChunks` when
   `options.withWorkshopStorageFixture` is set.
7. `tests/uat/seed/chunks/workshop-storage.ts` — new file. `installWorkshopStorageFixture(runner:
DataContextRunner, ownerUserId: string): Promise<void>` copies the fixture source into
   `<modulesDir>/.builds/uat-workshop-word`, builds it with `buildExternalModule`, then installs it
   through `installModuleDraft` with `writeDraftRow` bound to `SettingsRepository.setExternalModuleDraft`
   for `ownerUserId`.
8. `tests/uat/fixtures/workshop-word/jarvis.module.json` — module id `uat-workshop-word`; one
   `user`-scope storage namespace `uat-workshop-word.saved`; one read-risk assistant tool
   `uat-workshop-word.word.list`; two worker queues, `uat-workshop-word.word-save` and
   `uat-workshop-word.word-remove`, both `paramsSchema: { wordId: identifier }`, `retryLimit: 1`.
9. `tests/uat/fixtures/workshop-word/package.json` — `{ "name": "uat-workshop-word", "private": true }`.
10. `tests/uat/fixtures/workshop-word/src/web/index.ts` — deterministic screen: reads saved state
    from the assistant tool, queues save/remove, polls the read tool (30 attempts, 500ms) until the
    queued mutation is confirmed, and only then flips the displayed status. No model call anywhere.
11. `tests/uat/fixtures/workshop-word/src/worker/index.ts` — `defineModuleWorker` with three
    handlers (`word.list`, `word.save`, `word.remove`) reading/writing `ctx.kv` under the declared
    namespace, keyed by the fixed word id `quasar`.
12. `tests/uat/specs/workshop-storage.uat.spec.ts` — new file, copied verbatim. `uatLevel = {
level: "multi-user", without: [], withWorkshopStorageFixture: true }`. Exercises, through real
    browser navigation on the isolated instance: owner saves the word, queue-request body carries
    only bounded metadata (asserted not to contain the display text), persisted `pg-boss` job data
    matches the exact metadata-only shape, reload and a full stack restart both preserve the
    confirmed state, a second admin and a regular user are denied through navigation, the API, and
    a direct SQL probe under the `jarvis_worker_runtime` role with RLS asserted to reject a forged
    insert, deduplication of double-submitted saves, a 429 on a burst of removes with no
    resurrection of the removed word, then draft deletion and a final assertion of zero leftover
    rows, jobs, and on-disk module directories.

**Verify (unpiped, expected exit 0):**

```bash
pnpm typecheck > /tmp/t2-typecheck.log 2>&1; echo "EXIT=$?"
```

The UAT spec itself is DB- and Docker-touching and runs only through the `verify-gate` skill's
isolated recipe:

```bash
JARVIS_IMAGE_TAG=<this task's image tag> pnpm test:uat workshop-storage > /tmp/t2-uat.log 2>&1; echo "EXIT=$?"
```

Expected: exit 0, "1 passed". This is the e2e test for this phase — it must be observed to pass,
not just written, before the task is reported done.

**Exit:** recorded live browser/queue/storage assertions on the isolated instance, posted as a
`gh pr comment` on this task's PR (path, exit code, "1 passed" line). If the run's wall time
threatens this session's single context window, stop immediately, report to the coordinator with
what passed so far, and ask for the fixture-and-proof to be split into a second session on this
same issue — do not silently drop A3 or report the PR done without it.

## Verification and evidence to carry into the PR

- `pnpm format:check && pnpm lint && pnpm typecheck` (pre-push trio) plus `git fetch origin main &&
git rebase origin/main`, all before every push.
- Full local gate only through the `verify-gate` skill, scoped to this task's changed packages.
- Release note: `Category: Fixed`, plain-English description — a private draft the owner is
  building can now actually run and save its own data while it is still a draft; nobody else can
  see or use it.
- App-map: no navigation/settings surface changes in `main`'s product — the fixture module lives
  only under `tests/uat/fixtures/`, not in shipped module manifests, so no app-map entry is added
  for it. Confirm this holds by grepping `packages/shared/src/app-map-core.ts` for any reference
  before closing out; if a real product-facing screen is later wired to this gate, that PR adds its
  own app-map entry, not this one.

## Rulings ledger

- Ben's ruling (recorded in the 2026-09-04 plan, "Concrete decision after the local proofs"):
  A1's state review and A3's storage proof already passed once; this task reproduces A3, it is not
  new design. No further architectural approval is required to copy this diff.
- The branch's `0215`/`0216`/`0217` migration numbers are all taken on `main`. Only `0215`'s content
  (the KV owner-draft policy) is in this task's scope; `0216`/`0217` (workshop project tables)
  belong to a different workstream and are left on the stranded branch for whoever picks up D1.
