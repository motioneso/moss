# Plan: #1429 — briefing action-row CSS, inline styles, dead branch, blind e2e, orphan tracking

**Issue:** #1429 (`task`, tier `routine`, "Part of #1327"). **Spec:**
`docs/superpowers/specs/2026-07-29-1327-briefing-action-rows.md` (approved, referenced sections
only). **Branch:** `fix-1429-briefing-css`. Single phase — all five items are small, mechanical,
and share one e2e proof; no kill-gate split needed at this size.

## 0. Gates

- Spec: `docs/superpowers/specs/2026-07-29-1327-briefing-action-rows.md` — approved, action-row
  section (lines 20-51, 95-135, 390-419) covers the primitives, DTO shape, and View/Reply contract
  this plan touches.
- Issue: #1429, label `task`, body confirms 5 items: undefined CSS classes, inline styles bypassing
  the CSS layer, a blind e2e spec, a dead `primaryAction` branch, and a stale orphan-tracking entry.

## 1. Seams check (file:line, verified on this branch 2026-08-12)

- `apps/web/src/today/briefing-action-rows.tsx:154-206` (`ActionRow`) renders 6 classes with no CSS
  definition anywhere in the tree: `loose-row`, `loose-row__ic`, `loose-row__main`,
  `loose-row__title`, `loose-row__meta`, `loose-row__act`. Only `.loose` exists
  (`apps/web/src/styles/kit-today-feeds.css:2`).
- `apps/web/src/today/today-page.tsx` imports `kit-today-feeds.css` directly as a page-level kit
  file; `apps/web/src/styles/index.css` (19 lines, confirmed in full) does not import kit-*.css —
  each page pulls its own kit. So `kit-today-feeds.css` is the correct, and only, place to add
  `.loose-row*`.
- Modeling reference: `apps/web/src/styles/kit-tasks.css:189-296` (`.tk-task` family) — flex row,
  icon/bar + main + right actions, hover lift (`transform: translateY(-2px)` at `:196-198`),
  `--transition-control` token, `.tk-task__main` reset pattern at `:216-224`. **Correction to the
  prior relay note:** `.tk-task` itself has no `border-top` separator — the only `border-top` in
  that file is `.tk-tagmenu__hint` at `:142`, an unrelated component. `.loose-row` will get its own
  `+ .loose-row` sibling-border rule (below); this is a new pattern, not a copy of an existing one.
- Inline styles to fold, exact citations on this branch: `:132` (`marginTop: 12`, Catch-up
  wrapper), `:154` (`cursor: "default"`, row div), `:161-169` (full reset on
  `<button className="loose-row__main">`), `:176` (`display/gap/alignItems`, `.loose-row__act`).
- Dead branch: `PrimaryControl` (`:211-249`, case `needs_action`/`time_sensitive_info`) reads
  `row.sourceHref` directly at `:236`, never `row.primaryAction`. `rowsFromSuggestedTasks`
  (`:280-311`) builds `primaryAction: { kind: "view", href: ... }` at `:302-307` — same construction
  is mirrored server-side at `packages/briefings/src/action-rows.ts:77-126`. Spec confirms
  `primaryAction` is the canonical, symmetric field (`reply` already reads it at `:219-220`; DTO
  comment at spec `docs/superpowers/specs/2026-07-29-1327-briefing-action-rows.md:318-322`: "a
  view-category row does not require a `sourceHref` and emits `primaryAction: null` without one...
  when it is null the row has no primary action and offers only Accept and Dismiss" — gating is
  specified on `primaryAction`, not `sourceHref`). **Decision: fix (a)** — make `PrimaryControl`
  consult `row.primaryAction?.kind === "view"` and use `row.primaryAction.href`, matching the
  `reply` case's pattern, rather than deleting the DTO variant (smaller diff, and the DTO field is
  also server-shared — deleting it would touch `packages/briefings/src/action-rows.ts` and
  `packages/shared/src/briefing-action-rows.ts` for no behavior change).
- Test: `tests/e2e/briefing-action-rows.spec.ts` locates rows/titles by `.loose-row` /
  `.loose-row__title` class presence only (`:219, :222, :224, :268, :281-286, :312-315, :349-352`)
  — a missing stylesheet does not change class attributes, so this spec structurally cannot catch
  #1429's original defect. No computed-style or layout assertion exists anywhere in the file
  (verified: no `getComputedStyle`, `toHaveCSS`, or bounding-box assertion present).
- Orphan file: `apps/web/src/today/today-suggested-email.tsx` exports `SuggestedFromEmailSection`.
  Repo-wide grep (`apps/`, `packages/`, `tests/`) finds zero references to the filename or the
  export outside the file itself — confirmed genuinely unused, not merely mis-labeled. It is tracked
  as a "migrated section" in `scripts/check-migrated-sections.ts:51`
  (`MIGRATED_SECTION_PATHS`), which feeds `tests/unit/check-migrated-sections.test.ts:110-118`
  (runs guard 5/6 checks against every listed file — reads it off disk, so deleting the file without
  removing the entry would ENOENT that test). Fix: delete the file, remove the array entry.

## 2. Determinism boundary

N/A — this slice is pure CSS, inline-style-to-class migration, one client-side branch fix reading
an existing DTO field, a test-locator rework, and a dead-file removal. No model output, no chat
turn injection, nothing crosses the model/UI boundary.

## 3. Tasks

### Task 1 — add `.loose-row` family + `.briefing-catchup` to `kit-today-feeds.css`

File: `apps/web/src/styles/kit-today-feeds.css` (append after `.loose`, line 2-6).

Classes to add (token-based, no raw colors, no raw font stacks — `check-design-tokens.ts` covers
this file since it's under `apps/web/src/styles/`):

- `.loose-row` — flex row (`display: flex; align-items: flex-start; gap: 12px`), `cursor: default`
  (replaces `:154` inline style), padding matching `.tk-task`'s `11px 10px 11px 8px`,
  `transition: var(--transition-control)`.
- `.loose-row + .loose-row` — `border-top: 1px solid var(--border-subtle)` (new separator pattern,
  not copied from `.tk-task`; see seams note above).
- `.loose-row:hover` — `transform: translateY(-2px)` (matches `.tk-task:hover`).
- `.loose-row__ic` — icon slot, `flex: none`, `margin-top: 1px` (matches `.tk-task__check`
  positioning for a leading icon).
- `.loose-row__main` — absorbs the full inline reset from `:161-169`: `flex: 1; min-width: 0;
  text-align: left; padding: 0; cursor: pointer; font: inherit; color: inherit; background:
  transparent; border: none; display: block`.
- `.loose-row__title` — `display: block; line-height: 1.4;` (matches `.tk-task__title`).
- `.loose-row__meta` — `display: block; margin-top: 2px; color: var(--text-muted); font-size:
  var(--text-xs);` (secondary-line styling; two consecutive `.loose-row__meta` divs render
  explanation then metaLabel, so no `flex`/`gap` needed — each is its own block line).
- `.loose-row__act` — absorbs the inline reset from `:176`: `display: flex; gap: 8px; align-items:
  center; flex: none;`.
- `.briefing-catchup` — `margin-top: 12px;` (replaces `:132` inline style verbatim, one property,
  named for the Catch-up wrapper it targets).

**Test (unit, TDD-first):** no existing unit test targets `kit-today-feeds.css` directly. Coverage
for this task comes from Task 4's e2e computed-style assertions (a CSS-only change has no
meaningful unit test — the browser is the only real oracle here, consistent with
`docs/DEVELOPMENT_STANDARDS.md`'s CSS guidance). Write Task 4's e2e assertion in the same commit's
review context so this task isn't "green" on faith — but the commit boundary stays CSS-only per the
per-task-commit rule; the e2e run proving it happens in Task 4.

**Verification:**
```bash
grep -rhoE "loose-row[a-zA-Z_-]*" apps/web/src/today/briefing-action-rows.tsx | sort -u > /tmp/used-loose.txt
grep -rhoE "\.loose-row[a-zA-Z_-]*" apps/web/src/styles/kit-today-feeds.css | sed 's/^\.//' | sort -u > /tmp/defined-loose.txt
diff /tmp/used-loose.txt /tmp/defined-loose.txt; echo "EXIT=$?"
```
Expected: `EXIT=0` (every used class has a matching definition, and vice versa — an exact-match
diff, stronger than the plan's original one-directional `comm -23`).

### Task 2 — fold inline styles into the new classes in `briefing-action-rows.tsx`

File: `apps/web/src/today/briefing-action-rows.tsx`.

- `:132` `<div style={{ marginTop: 12 }}>` → `<div className="briefing-catchup">`.
- `:154` `<div className="loose-row" style={{ cursor: "default" }}>` → `<div
  className="loose-row">`.
- `:161-169` drop the entire `style={{...}}` block on the `.loose-row__main` button — the class
  alone now carries it.
- `:176` `<div className="loose-row__act" style={{ display: "flex", gap: 8, alignItems: "center"
  }}>` → `<div className="loose-row__act">`.

No other lines change in this task.

**Verification:**
```bash
grep -n "style={{" apps/web/src/today/briefing-action-rows.tsx; echo "EXIT=$?"
```
Expected: `EXIT=1` (grep finds nothing — zero inline `style` props remain in the file).

### Task 3 — `PrimaryControl` reads `primaryAction`, not `sourceHref`, for View

File: `apps/web/src/today/briefing-action-rows.tsx:230-242` (the `needs_action` /
`time_sensitive_info` case in `PrimaryControl`).

Change:
```ts
case "needs_action":
case "time_sensitive_info": {
  if (row.primaryAction?.kind !== "view") return null;
  return (
    <a
      className="jds-btn jds-btn--sm jds-btn--quiet"
      href={row.primaryAction.href}
      target="_blank"
      rel="noopener noreferrer"
    >
      View
    </a>
  );
}
```
(Signature/behavior only — the comment at `:235` about never using model text as a URL still
applies: `primaryAction.href` traces to the same `meta.sourceHref` / server `sourceHref` origin as
before, never row title/explanation text.)

**Test (unit):** add a case to whatever existing unit coverage exercises `PrimaryControl` /
`rowsFromSuggestedTasks` (check `tests/unit/` for an existing `briefing-action-rows` unit spec
before adding a new file) — a row with `primaryAction: { kind: "view", href }` but a `sourceHref`
deliberately left absent/mismatched must still render the View link using `primaryAction.href`.
This is the case that would fail against the current (pre-fix) implementation, which reads
`sourceHref` and would render nothing or the wrong href.

**Verification:**
```bash
pnpm --filter @moss/web test -- briefing-action-rows > /tmp/t3.log 2>&1; echo "EXIT=$?"
```
Expected: `EXIT=0`. (Confirm the actual unit test filter/command against `package.json` scripts
before running — don't assume the invocation; adjust the command, keep the unpiped
`; echo "EXIT=$?"` pattern.)

### Task 4 — rework `tests/e2e/briefing-action-rows.spec.ts` to catch a missing/broken stylesheet

File: `tests/e2e/briefing-action-rows.spec.ts`. Read by section (this task's section only) before
editing — not yet fully read as of plan time.

Requirement: at least one assertion per key `.loose-row` structural class that would fail if
`kit-today-feeds.css` were absent or `.loose-row*` rules deleted, not just "class attribute is
present." Minimum bar: a `toHaveCSS` (or `getComputedStyle`-via-`page.evaluate`) assertion on
`.loose-row` for a property this task's CSS actually sets and that has an observably different
default — e.g. `display` (row default is `block`, `.loose-row` sets `flex`) or `border-top` on a
second `.loose-row` sibling (`0px none` unstyled vs. `1px solid` styled). Keep every existing
`.loose-row` / `.loose-row__title` locator usage intact — this is additive, not a locator rewrite;
adding a computed-style check is the minimum fix, don't restructure locators that already work.

**Test:** this task's own edit IS the test. Prove it catches the regression it's meant to catch —
temporarily comment out the `.loose-row { display: flex; ... }` rule added in Task 1, run the new
assertion, confirm it fails, then restore the rule and confirm it passes. Record both runs in the
task commit's context (not necessarily in the commit itself) as the "would have caught #1429"
proof.

**Verification (the phase's e2e — run against a live dev instance per the live-path gate):**
```bash
pnpm playwright test tests/e2e/briefing-action-rows.spec.ts > /tmp/e2e-1429.log 2>&1; echo "EXIT=$?"
```
Expected: `EXIT=0` on the real (uncommented) CSS, and a confirmed non-zero exit when the rule is
commented out (transient, local-only check — do not commit the commented-out state).

### Task 5 — delete the orphaned `today-suggested-email.tsx`, drop its tracking entry

Files:
- Delete `apps/web/src/today/today-suggested-email.tsx` (confirmed zero references anywhere in
  `apps/`, `packages/`, `tests/` besides the file itself — see seams check).
- `scripts/check-migrated-sections.ts:51` — remove the
  `"apps/web/src/today/today-suggested-email.tsx",` line from `MIGRATED_SECTION_PATHS`.

**Test:** `tests/unit/check-migrated-sections.test.ts:110-118` ("passes against the real repo
tree") already covers this — it reads every path in `MIGRATED_SECTION_PATHS` off disk. Before this
task's fix, confirm it would fail if the file were deleted without removing the array entry
(ENOENT), so the existing test is real coverage, not just incidentally green.

**Verification:**
```bash
test -f apps/web/src/today/today-suggested-email.tsx; echo "EXIT=$?"
```
Expected: `EXIT=1` (file gone).
```bash
pnpm --filter @moss/web test -- check-migrated-sections > /tmp/t5.log 2>&1; echo "EXIT=$?"
```
Expected: `EXIT=0`.

## 4. Kill gate

None needed — five independent, small, mechanical fixes in one file family with no architectural
fork. If Task 3's unit-test discovery step (locating existing `PrimaryControl` coverage) turns up
nothing and adding new test infra would be non-trivial, that's an escalation to the coordinator
before proceeding with Task 3, not a kill of the phase — the other four tasks are unaffected and
ship regardless.

## 5. Full-phase verification (after all 5 tasks committed)

```bash
grep -rhoE "jds-[a-zA-Z0-9_-]+" apps/web/src/today/ | sort -u > /tmp/used.txt
grep -rhoE "\.jds-[a-zA-Z0-9_-]+" apps/web/src/styles/ | sed 's/^\.//' | sort -u > /tmp/defined.txt
comm -23 /tmp/used.txt /tmp/defined.txt; echo "EXIT=$?"
```
Expected: no output lines, `EXIT=0` — confirms the design-system invented-class audit is clean
across `apps/web/src/today/` after this slice, not just for `.loose-row*`.

Then the full local gate on an isolated gate DB (`verify-gate` skill — never improvised) before
`coordinated-wrap-up`.

## Rulings ledger

- **Fix (a) chosen over (b)** for the dead `primaryAction` branch: spec text gates View's presence
  on `primaryAction` being non-null, matching the `reply` case's existing pattern. See seams check
  above for the exact spec citation. Recorded so nobody re-litigates this per-task.
- **`.tk-task` has no border-top separator** — the prior relay-doc description ("border-top
  separators" as a `.tk-task` trait) does not hold; verified `.tk-tagmenu__hint:142` is the only
  `border-top` in `kit-tasks.css` and it's unrelated. `.loose-row`'s separator is new, not modeled.
