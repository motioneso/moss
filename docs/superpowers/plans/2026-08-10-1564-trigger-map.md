# Plan — #1564 correct chat UAT trigger-map baseline for fully-fixme drawer spec

**Task:** #1564 (`Part of #1564`)
**Approved decision:** issue #1564 + Fable's binding ruling, #1557 comment
https://github.com/motioneso/moss/issues/1557#issuecomment-5249826990, §5 "Required trigger-map
correction (one row)". No `docs/superpowers/specs/` doc exists or is needed — the ruling itself is
the approved spec for this metadata-only correction (per the handoff's risk-tier note).
**Risk tier:** routine (coordinator/test metadata only).
**Diff surface:** exactly one file — `.claude/skills/coordinate/uat-trigger-map.tsv`.

## Seams check (file:line citations, current tree)

- Target row: `.claude/skills/coordinate/uat-trigger-map.tsv:19` —
  `blocking\tpackages/chat/**\ttests/uat/specs/1089-1090-chat-drawer-private.uat.spec.ts`.
- That spec file is fully `test.fixme`'d, confirmed by grep — both tests skipped, zero runnable
  assertions: `tests/uat/specs/1089-1090-chat-drawer-private.uat.spec.ts:41` and `:46`.
- Sibling chat rows that stay `blocking` and are NOT fully fixme'd (companion-comment baseline):
  - `1133-chat-attachments.uat.spec.ts` — 3 tests: 2 runnable (`:51`, `:124`), 1 fixme pending
    #1121 (`:155`).
  - `runtime-context.uat.spec.ts` — 4 tests: 2 runnable (`:56`, `:91`), 2 fixme pending #1121
    (`:111`, `:122`).
- The map has no dedicated code parser/test suite. It is consumed at run time by
  `.claude/skills/coordinate/resolve-uat-triggers.sh` (verified present, read in full) — a
  dependency-free Bash glob matcher. Comment lines are skipped via
  `[[ -z "$mode" || "$mode" == \#* ]]`, which only works because comment lines contain no literal
  tab character (confirmed: none of the file's existing `#` lines do). New comment text must
  likewise avoid embedded tabs.
- Existing style precedent for a companion comment block: `uat-trigger-map.tsv:33-35` (the
  `real-chat-onboarding` note), placed directly above/around the row group it documents. This plan
  follows that placement and tone.
- No other file references `uat-trigger-map.tsv` for parsing (grep confirmed: only doc/handoff
  prose elsewhere). "Trigger-map parser/tests" in the issue/handoff means: the resolver script
  above, run against representative input — there is no separate unit-test file to run.

## Determinism / scope boundary

No runtime code, no UI, no UAT spec file is touched. This is a data-only TSV edit plus comments.
Not applicable: model determinism, guidance budget, live-path UI proof (handoff exit criteria
explicitly waives live-path proof for this coordinator-metadata change).

## Task 1 — correct the one row + comments

Edit `.claude/skills/coordinate/uat-trigger-map.tsv`:

1. Change line 19's mode from `blocking` to `advisory` (path glob and spec column unchanged).
2. Immediately above that row, add an inline comment stating: the file is fully `test.fixme`'d (2/2
   tests, #1089/#1090), its real proof lives in `tests/e2e/chat-drawer.spec.ts`, and the row returns
   to `blocking` automatically the moment either test becomes runnable.
3. Add a companion comment above the `packages/chat/**` / `apps/web/src/chat/**` row group (the six
   rows currently at lines 19-24) recording the current fixme baseline so QA diffs against it
   instead of assuming zero skips:
   - `1089-1090-chat-drawer-private`: 2/2 fixme (now advisory, see inline note).
   - `1133-chat-attachments`: 1/3 fixme, pending #1121.
   - `runtime-context`: 2/4 fixme, pending #1121.
4. Leave the `1133-chat-attachments` and `runtime-context` rows (lines 20-21, 23-24) as `blocking` —
   unchanged, per acceptance criteria.
5. No other row, file, spec, or PR is touched.

**Verification** (both commands unpiped, exit code checked):

```bash
printf 'packages/chat/src/routes.ts\n' | .claude/skills/coordinate/resolve-uat-triggers.sh > /tmp/1564-check.out 2>&1; echo "EXIT=$?"
cat /tmp/1564-check.out
```
Expected exit `0`; expected output includes
`advisory\ttests/uat/specs/1089-1090-chat-drawer-private.uat.spec.ts` and
`blocking\ttests/uat/specs/1133-chat-attachments.uat.spec.ts` and
`blocking\ttests/uat/specs/runtime-context.uat.spec.ts` (plus the unrelated #1311 row for that
path, which is expected and out of scope).

```bash
bash -n .claude/skills/coordinate/resolve-uat-triggers.sh; echo "EXIT=$?"
```
Expected exit `0` (script still parses; sanity check the file's tab structure didn't break the
script itself, since the script has no separate test suite).

## Kill gate

None needed — single task, single file, no phase 2. If the verification command's output doesn't
show `advisory` for the 1089-1090 row, stop and re-check the edit before proceeding to wrap-up.

## Exit criteria (from issue #1564, unchanged)

- Trigger map still selects the drawer-private spec for `packages/chat/**`, now advisory.
- Attachments and runtime-context rows remain blocking.
- Comments record the exact intentional-skip baseline and the automatic return-to-blocking rule.
- Verification commands above pass.
- Diff isolated to this TSV file (plus this plan and the already-committed handoff doc).
