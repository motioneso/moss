# Plan — #1659 defect 4: give the UAT scripted provider its own PATH entry

**Task issue:** Part of #1659 (defect 4 only; the issue's other 3 asks are already done via PR
#1660). **Spec:** none in `docs/superpowers/specs/` — explicitly exempted by
`docs/coordination/handoff-1659-uat-chatscript-verify.md` (bounded test-harness fix, not a new
feature; risk tier `routine`). **Fix direction:** (b), mandated by the handoff — do not reopen (a).

## Seams check (file:line, verified against branch tip `20e9fe8bd`)

1. `tests/uat/provisioner.ts:701-712` — `opts.chatScript` mutates the **host**
   `process.env.JARVIS_CLI_TOOLS_PREFIX` to the fixture dir; restored in `cleanupRunScopedState`
   (call site line 724). Confirmed present, unchanged from relay doc.
2. `infra/docker-compose.prod.yml:162` — `JARVIS_CLI_TOOLS_PREFIX: ${JARVIS_CLI_TOOLS_PREFIX:-/data/cli-tools}`,
   same file prod uses. Confirmed.
3. `packages/cli-runner/src/main.ts:97` (`DEFAULT_TOOLS_PREFIX`), `readConfig` reads
   `env.JARVIS_CLI_TOOLS_PREFIX` as `toolsPrefix`. Confirmed.
4. `packages/cli-runner/src/install-service.ts:677-687` `reconcileInstalledProviders()`: for any
   provider where `isExecutable(this.binPath(recipe.binary))` is true, calls `installProvider()` →
   `ensureBinSymlink()` (line 698), which (re)points `${toolsPrefix}/bin/<binary>` at the real CLI.
   Confirmed — **not touched by this plan** (direction (a) stays banned).
5. `packages/chat/src/live/claude-print-chat-engine.ts:78` `submit()` spawns
   `bash -lc <line>`, `detached: true, stdio: "ignore"`, **no explicit `env:`** → inherits full
   container `process.env`. Grepped the whole file: zero references to `sanitized-env`/
   `buildSanitizedCliEnv`. `buildCommand` (line 249) invokes bare `claude` (resolved via `PATH`).
   Confirmed — resolves open question 5 (sanitized-env `ALLOWED_KEYS` not on this path, no change
   needed there).
6. `Dockerfile:72` — `bash -lc` is a login shell, sources `/etc/profile.d/jarvis-cli-path.sh`,
   which the `RUN printf` at Dockerfile:71-73 bakes to
   `export PATH="${JARVIS_CLI_TOOLS_PREFIX:-/data/cli-tools}/bin:$PATH"`, read fresh at
   shell-start. Confirmed sole PATH mechanism for chat-turn spawns.
7. `packages/cli-runner/src/main.ts:262-265` — `toolsBinDir: \`${config.toolsPrefix}/bin\`` feeds
   `terminal-host.ts`/`terminal-session.ts:50`'s **separate** PATH-prepend for the interactive
   terminal panel only. Confirmed irrelevant to this bug (different code path from chat turns);
   `claude-main.ts`'s header comment currently mis-cites this file — corrected in Task 3.
8. `tests/uat/provisioner.ts:246-248` env-conduit precedent —
   `...(input.chatScript ? [\`JARVIS_UAT_SEED_CHAT_SCRIPT=${input.chatScript}\`] : [])` inside
   `writeUatEnvFile`, which is wired to the compose service's `env_file:` (confirmed via
   `provisioner.ts:170-172`'s own comment: "`env_file:` alone never feeds interpolation, only
   container env" — i.e. this reaches container `process.env` directly, no
   `docker-compose.prod.yml` interpolation change needed). Resolves open question re: compose
   changes — **none required**.
9. `tests/uat/fixtures/scripted-provider/bin/claude:16` and Dockerfile `WORKDIR /app` (lines 11,
   33, 49) confirm `/app/tests/uat/fixtures/scripted-provider/bin` is the correct in-container bin
   dir — matches the value the buggy mutation already used.
10. Shell syntax test (isolated `bash -c`, both branches): with
    `JARVIS_UAT_SCRIPTED_PROVIDER_BIN` unset, `PATH` gets exactly one `/data/cli-tools/bin:`
    segment (no stray colon/empty entry); set, `PATH` gets
    `<value>:/data/cli-tools/bin:...` — prepended, inert when unset. Verified locally, not just
    assumed.
11. `tests/uat/fixtures/scripted-provider/claude-main.ts` read in full: `FAILURE_LOG_PATH`
    (line 38) is appended to on the failure branch only (`main()`, lines 285-289). **No success
    marker exists anywhere in the file** — resolves open question 3: one must be added.
12. `tests/uat/specs/1533-chat-surface-live-path.uat.spec.ts` read in full: Phase 1 calls
    `restartUatStack` **twice** (lines 160 and 170 — once right after `docker cp`, once after
    enabling the module) before the scripted chat turn ever fires in Phase 2. Both restarts run
    `reconcileInstalledProviders()` on boot. This spec already exercises "across a full container
    boot cycle" — in fact two — before any assertion. Resolves open question 4: re-running this
    spec (extended with a success-log assertion) is sufficient proof; no new mid-test restart
    needed.

## Determinism boundary

N/A — no model output reaches user-facing feedback in this change; the scripted provider's replies
are entirely fixture-authored JSON, unchanged by this fix. Test-infra only.

## Task 1 — Provisioner + Dockerfile: move the fixture off `JARVIS_CLI_TOOLS_PREFIX`

**New env var:** `JARVIS_UAT_SCRIPTED_PROVIDER_BIN` (grepped repo-wide for collisions — none).
Value: `/app/tests/uat/fixtures/scripted-provider/bin`.

- `tests/uat/provisioner.ts`:
  - Delete lines 701-712 (`previousCliToolsPrefix`, the `if (opts?.chatScript)` mutation,
    `restoreCliToolsPrefix`) and the `restoreCliToolsPrefix();` call inside
    `cleanupRunScopedState` (currently line 724).
  - In `writeUatEnvFile`'s env-lines array (next to the existing `JARVIS_UAT_SEED_CHAT_SCRIPT`
    conditional, provisioner.ts:246-248), add:
    ```ts
    ...(input.chatScript
      ? [`JARVIS_UAT_SCRIPTED_PROVIDER_BIN=/app/tests/uat/fixtures/scripted-provider/bin`]
      : []),
    ```
    Reuses the existing `input.chatScript` field — no new parameter.
  - Update `UatProvisionOptions.chatScript`'s doc comment (currently provisioner.ts:617-619) to
    drop "points JARVIS_CLI_TOOLS_PREFIX at the scripted-provider fixture binary" and describe the
    new env-var-only mechanism instead.
- `Dockerfile:71-73` — change the baked profile.d line from
  `export PATH="${JARVIS_CLI_TOOLS_PREFIX:-/data/cli-tools}/bin:$PATH"` to
  `export PATH="${JARVIS_UAT_SCRIPTED_PROVIDER_BIN:+$JARVIS_UAT_SCRIPTED_PROVIDER_BIN:}${JARVIS_CLI_TOOLS_PREFIX:-/data/cli-tools}/bin:$PATH"`.
  Add a one-line comment above citing #1659 and stating it's inert unless a UAT scripted-chat run
  sets the var.
- `tests/uat/seed/types.ts:64-70` — update `UatSeedOptions.chatScript` doc comment (same stale
  "points JARVIS_CLI_TOOLS_PREFIX" claim) to match.

**Test cases (unit, no live stack):**
- `tests/unit/uat-provisioner.test.ts`, `describe("provisionForUat setup cleanup")`
  (currently lines 40-145): remove the `process.env.JARVIS_CLI_TOOLS_PREFIX = "sentinel-original"`
  setup line and the `expect(process.env.JARVIS_CLI_TOOLS_PREFIX).toBe("sentinel-original")`
  assertion from all 4 `it()` blocks — leave every other assertion (cleanup called once, other env
  vars restored, error aggregation) untouched. Remove the now-dead
  `originalCliToolsPrefix`/its `afterEach` restore (currently lines 24, 32-33).
- `describe("writeUatEnvFile")` (currently lines 215-271): add a new case —
  *behavior:* `writeUatEnvFile({ webPort, subnet, chatScript: "phase1-smoke" })` produces a file
  containing `JARVIS_UAT_SCRIPTED_PROVIDER_BIN=/app/tests/uat/fixtures/scripted-provider/bin`;
  *would fail against a broken implementation* that forgot the new conduit line, or that wrote the
  wrong path. Sibling case: with no `chatScript`, the file must NOT contain
  `JARVIS_UAT_SCRIPTED_PROVIDER_BIN` (mirrors the existing "omits ... when no fixture URL is
  given" pattern at line 242).
- `tests/unit/prod-compose-cli-tools-prefix.test.ts`: reword the header comment (lines 1-7) and
  test 2's description (line 33) to drop "the provisioner's scripted-provider path"/"override" —
  this is now a generic compose-interpolation-override test, not tied to the provisioner. Test 1
  (default value) and test 2 (explicit-export override, generic) both stay — the underlying
  compose behavior they check is still real and worth covering; only the wording is stale.

## Task 2 — Add a success marker to the scripted provider

`tests/uat/fixtures/scripted-provider/claude-main.ts`:
- Add `export const SUCCESS_LOG_PATH = "/data/cli-auth/uat-scripted-provider-success.log";` next
  to the existing `FAILURE_LOG_PATH` (line 38).
- At the end of `runScriptedClaude()`, immediately after the `writeCursor(...)` call (currently
  the last statement before the function's closing brace, ~line 266), add a best-effort
  `appendFileSync(SUCCESS_LOG_PATH, ...)` write carrying only `scriptId` and `turnIndex` (never
  prompt/tool/reply content — same secrets-never-escape constraint the file's header already states
  for `FAILURE_LOG_PATH`), wrapped in the identical try/catch-and-swallow shape `main()` already
  uses for `FAILURE_LOG_PATH` (lines 285-289: best-effort, no-op outside a provisioned container).
- Fix the header comment (lines 7-15): replace the `terminal-session.ts:50` mis-citation with the
  actual mechanism — `Dockerfile:71-73`'s profile.d script prepending
  `JARVIS_UAT_SCRIPTED_PROVIDER_BIN` (set via `tests/uat/provisioner.ts`'s `writeUatEnvFile`) ahead
  of `JARVIS_CLI_TOOLS_PREFIX/bin` on `PATH`.

**Test case (unit, `tests/integration/uat-scripted-claude.test.ts` — the one place this fixture
runs on a bare host, per the fixture's own `bin/claude` comment):** *behavior:* a successful
scripted turn appends one line to `SUCCESS_LOG_PATH` containing the script id and turn index and
NOT the prompt/reply text; *would fail against a broken implementation* that never added the write,
or that leaked reply content into the log line.

## Task 3 — Regression/proof: extend the existing 1533 live-path spec

`tests/uat/specs/1533-chat-surface-live-path.uat.spec.ts`: add a step after Phase 5 (turn settles)
that execs into the `jarv1s` container and reads `SUCCESS_LOG_PATH`
(`docker`, `buildUatComposeArgs(projectName, ["exec", "-T", "jarv1s", "cat", SUCCESS_LOG_PATH])`,
same `execFileSync`/`buildUatComposeArgs` pattern already used for container logs at
line 109-113 and for SQL at `job-search-board-sql.ts:18-41`) and asserts it contains a line for
this run's script id (`"1533-surface-probe"`). This is direct evidence the FIXTURE binary — not
the real CLI reinstalled by a post-restart reconcile — executed the turn, across the two boot
cycles Phase 1 already performs (seam #12 above). No new spec file, no extra restart needed.

Also fold in "done" criterion 2: add one assertion (SQL or container-exec, same session) that a
**non-chatScript** UAT run still reconciles its real `toolsPrefix` bin dir normally — reuse
whichever existing non-scripted UAT spec already exercises a fresh boot (e.g.
`job-search-board.uat.spec.ts`, which shares the install/enable/restart recipe per this file's own
header comment) rather than adding a new spec; confirmed during build, not asserted here since the
exact existing coverage needs a fresh read at build time.

## Kill gate

**After Task 1 lands and its unit tests pass green**, before touching the fixture (Task 2) or the
live spec (Task 3): if the isolated shell-syntax verification (seam #10) or the unit test for
`writeUatEnvFile`'s new line does not hold under the real `docker compose config` resolution
(i.e. `prod-compose-cli-tools-prefix.test.ts`-style real compose invocation shows the profile.d
script doesn't actually receive `JARVIS_UAT_SCRIPTED_PROVIDER_BIN` inside the container), stop and
escalate to the coordinator before proceeding — that would mean the env-conduit assumption (seam
#8) is wrong and the fix needs a compose-file change after all, which is a bigger blast radius the
handoff didn't budget for. Call made by whoever is building (escalate, don't silently absorb).

## Verification commands (run at wrap-up, exit codes never piped)

```bash
pnpm --filter . exec vitest run tests/unit/uat-provisioner.test.ts tests/unit/prod-compose-cli-tools-prefix.test.ts tests/integration/uat-scripted-claude.test.ts > /tmp/1659-unit.log 2>&1; echo "EXIT=$?"
```
Expected: `EXIT=0`.

```bash
pnpm format:check > /tmp/1659-fmt.log 2>&1; echo "EXIT=$?"
pnpm lint > /tmp/1659-lint.log 2>&1; echo "EXIT=$?"
pnpm typecheck > /tmp/1659-tsc.log 2>&1; echo "EXIT=$?"
```
Expected: `EXIT=0` for each.

Live-path UAT run (the 1533 spec, extended) per `coordinated-wrap-up`'s isolated-gate-DB recipe —
exact invocation to be confirmed at build time from `tests/uat/run-uat.ts`'s current CLI surface;
expected `EXIT=0` and the success-log assertion passing.

Full gate: `pnpm verify:foundation` — only via the `verify-gate` skill's isolated-DB procedure, per
CLAUDE.md.

## Not touched (per handoff bans)

`packages/cli-runner/src/install-service.ts` reconcile logic, `packages/cli-runner/src/terminal-session.ts`,
`docs/coordination/handoff-1659-uat-chatscript-verify.md`, PR #1654/its branch,
`infra/docker-compose.prod.yml` (no change needed — seam #8).
