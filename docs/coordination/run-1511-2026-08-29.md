# Coordination Run — run-1511-2026-08-29

**Date:** 2026-08-29
**Coordinator lock:** registered agent name `coordinator` + visible pane label `Coordinator`, **stable anchor = Claude session id `83c24c17-eb69-4c28-935e-39ee21a01489`** (match `agent_session.value` in `herdr agent list`). Single-coordinator lock — exactly one live agent named `coordinator` whose session id matches this anchor holds authority for the life of the run. ⚠️ **Pane numbers reflow on every restart/split/reap — resolve the agent fresh by name+session at read time.** (Relay #2, 2026-08-29: new coordinator is pane `w1:p1P`, prior coordinator pane `w1:p1F` closed.)
**Merge policy:** autonomous-after-verified-QA for `routine`/`sensitive`; **`security`-tier needs Ben's explicit merge sign-off**
**Relay threshold:** security-tier merge → relay immediately after Phase 3 step 7; routine/sensitive `merges_since_relay` ≥ 2 → relay. No deferral. Compaction summary = already past safe → relay, merge nothing.
**merges_since_relay:** 0

## ⚠️ Standing rule — plan authorship and approval (Ben, 2026-08-29)

**Every implementation plan is WRITTEN by an Opus agent and APPROVED by a Fable agent.** A
Sonnet-authored plan is not valid, and coordinator approval does not substitute for the Fable gate.
Pipeline per lane is therefore three roles, not one:

1. **Opus planner** (`planners` tab) — authors the plan, no implementation.
2. **Fable approver** — approves or returns the plan. This is the approval gate.
3. **Sonnet builder** (`builders` tab) — executes the approved plan only; does not write its own.

The coordinator still holds merge authority and still reviews plans, but its opinion is advisory at
the plan gate now — Fable's approval is the gate.

**Companion rule (Ben, 2026-08-29):** a build task must fit ONE session. If it does not, it is
split into smaller issues rather than relayed forward. Planners size to this and report a proposed
split rather than padding a plan.

> Coordinator's externalized memory. GitHub is the source of truth for spec/issue/board status;
> this file holds only in-flight operational state.

## Queue

| Spec | Issue | Tier | Status | Agent name | Pane | Branch | PR | Relays |
| ---- | ----- | ---- | ------ | ----------- | ---- | ------ | -- | ------ |
| docs/superpowers/specs/2026-08-10-1137-robustness-followups.md § A | #1511 | sensitive | building — released, reconciling written code against the approved Opus plan (`~/.coord-briefs/plan-1511-opus.md`); also running the full integration suite in the background | plan-1511-shares (planner, w1:p1J — DONE, reapable) · share-target-validation (builder, w1:p1G) | tab w1:tH "planners" / w1:tG "builders" | 1511-share-target-validation | — | 0 |
| docs/superpowers/specs/2026-08-10-self-hosted-tls.md § Child 1 | #1504 | security | plan awaiting final Fable sign-off — reviewer said second pass would approve on sight once the missing `/config/caddy` ownership path was added; coordinator made that doc-only fix directly, commit `26c44b3d6` | plan-1504-tls (planner, w1:p1H — DONE, reapable) · revise-1504-tls (Sonnet, w1:p1Q — DONE, reapable, pane closed) · approve-1504-tls (Fable, w1:p1N — final check) | tab w1:tH "planners" | 1504-tls-compose-proxy | — | 0 |

**Tier note.** The issue body and spec both say `routine`. Raised to `sensitive` at Phase 0: the
change sits in the cross-user access-grant path (`SharesRepository.grant`) and the acceptance
criteria explicitly require owner RLS and the no-self-share constraint to stay green. It adds no
policy surface, no migration, and no user-visibility broadening, so it does not reach `security`
tier. Per the tiering rule "in doubt between two tiers, take the higher."

## Readiness checks (Phase 0, completed 2026-08-29)

- Approved spec: `docs/superpowers/specs/2026-08-10-1137-robustness-followups.md`, § "A — Share-target validation" (locked decisions) + § "Acceptance and live-path applicability" row A. ✅
- GitHub `task` issue #1511, open, board status Ready, priority P1. ✅
- Dependency gate: #1246 CLOSED. ✅
- `main` CI green at 6c6d21eb9 (run 33288330758, success). ✅
- Referenced symbols exist: `assertUuid` at `packages/db/src/data-context.ts:23`; `app.get_user_by_id` SECURITY DEFINER helper per `infra/postgres/migrations/0047_users_rls_tighten.sql`. ✅
- Target files exist: `packages/db/src/sharing/shares-repository.ts`, `tests/integration/shares.test.ts`. ✅
- **Live-path gate: N/A.** The spec required a re-run of the caller inventory after #1246. Re-run
  2026-08-29: no production caller of `SharesRepository` outside the repository file and its tests.
  No user-facing surface, so no live-UI proof artifact is required for merge. Recorded here as the
  standing justification.
- No front-end/module gate: not a new module, no UI.

## Dependency / merge order

- **Parallel group 1:** #1511 (single lane, no collisions — sole spec in flight).
- **Merge order:** #1511.

## CI waivers

| Check | PR | Proven red on `main` @ SHA | Proof | Ben-approved |
| ----- | -- | -------------------------- | ----- | ------------ |
| <none> | — | — | — | — |

## Outstanding escalations

- [ ] Environment note (non-blocking): `coordinator-watchdog.timer` is not installed on this box —
      `systemctl --user start` returns "Unit not found". The 15-minute idle nudge is therefore not
      armed for this run. Liveness handled by a Monitor on `herdr pane list` instead.

## Spawn record

- **2026-08-29** — `share-target-validation`, Sonnet 5, pane confirmed by bounded read (model line
  reads "Sonnet 5", bypass permissions on, branch `1511-share-target-validation`).
  Worktree `.claude/worktrees/1511-share-target-validation` off `origin/main` @ 6c6d21eb9.
  Brief: `~/.coord-briefs/boot-1511-share-target-validation.txt` (outside the worktree by design).
  Tab `w1:tG` labelled `builders`, created by splitting off the coordinator pane and relocating
  immediately — no builder pane ever shared the coordinator tab.
  Note: `herdr agent start` returned `timeout waiting for agent startup`, but the agent HAD
  started; the routing name did not register on that path and was set afterwards with
  `herdr agent rename`. Verify the name after a timed-out start.
  Note: pane `w5:p1` (idle Codex in `~/jarv1s-fleet`) carried a stale visible label `Coordinator`.
  Relabelled to `Fleet repo (idle)` so agents resolving the coordinator label cannot hit it.

## Plan approval — #1511 (2026-08-29)

Plan: `docs/superpowers/plans/2026-08-29-1511-share-target-validation.md` (on the lane branch).
**Approved with one required addition**, delivery verified by bounded agent read.

Coordinator's own check of the plan against the spec's locked decisions found one gap: the spec
locks the lifecycle contract ("an existing pending or deactivated user is still an existing
target") and the plan had no coverage for it. That is the only way this change could silently
narrow who may be granted a share. Required the lane to add a third new test for it.

Verified while reviewing, so a QA agent need not re-derive it: `app.get_user_by_id` is redefined by
`infra/postgres/migrations/0050_multi_user_accounts.sql` (superseding the 0047 definition the plan
cited); the 0050 body filters on `id` only, with no status predicate. So the lifecycle behaviour is
already correct — the added test locks it rather than fixing it.

## Lane #1504 — readiness (Phase 0, 2026-08-29)

- Approved spec `docs/superpowers/specs/2026-08-10-self-hosted-tls.md` § "Child 1 — Compose proxy". ✅
- GitHub task issue #1504, open, board Backlog, parent roll-up #901. ✅
- Dependencies #1403 and #1486 both CLOSED. ✅
- Collision with #1511: **none.** #1511 owns `packages/db/src/sharing/` + `tests/integration/shares.test.ts`;
  #1504 owns `infra/docker-compose.prod.yml`, `infra/caddy/Caddyfile`, and the Caddy assertions in
  `tests/unit/prod-deploy-config.test.ts`. No shared file, no migration in either. Safe in parallel.
- **Merge is deferred by the spec's close rule:** Child 1's PR stays OPEN until Child 4 records
  independent live-path proof from a real second LAN device. Lane is done at PR-open + green +
  reviewed. Security tier also requires Ben's explicit sign-off at merge time.

## Plan-gate rework, 2026-08-29

Ben introduced the Opus-authors / Fable-approves rule mid-run, after #1511's plan had already been
written by its Sonnet builder and approved by the coordinator. Retrofitted rather than grandfathered:

- Builder `share-target-validation` told to STOP and hold; asked to report exactly how far it got.
  It was told explicitly not to revert work already done.
- Opus planner `plan-1511-shares` spawned to re-author the plan. It works in the MAIN checkout
  read-only and writes to `~/.coord-briefs/plan-1511-opus.md`, because the builder is sitting in the
  lane worktree and two agents in one worktree is banned. The coordinator relays the plan in.
- Opus planner `plan-1504-tls` spawned in the #1504 worktree before any builder — that lane never
  got a Sonnet-authored plan.
- Both planners confirmed running on **Opus 5** by bounded pane read.
- The coordinator's own findings on the #1511 plan (stale migration citation; missing lifecycle
  test) were passed to the Opus planner as input to verify, not as conclusions to inherit.

**Note for the Fable approver briefs:** the previous coordinator approval of #1511's plan is void
under the new rule. Do not treat it as prior art.

## Lane #1511 — builder state at the hold (2026-08-29)

Builder `share-target-validation` reported before standing down. Uncommitted, nothing reverted:

- `packages/db/src/sharing/shares-repository.ts` — grantee check added before the insert (id-format
  check, safe lookup, fixed error text on no match).
- `tests/integration/shares.test.ts` — three new cases (missing grantee, malformed id, deactivated
  user still a valid target) plus a seed for a deactivated user.
- Typecheck and `check:file-size`: **passed**.
- Guarded integration run: **still in flight at the time of the hold, result not yet seen.**
- No commit.

So the implementation substantially exists, including the lifecycle test the coordinator required.
The Opus planner was deliberately NOT told this — planning from the spec unbiased is the point of
the new gate. On approval the builder reconciles its existing edits against the approved plan and
is instructed to flag disagreement rather than silently conform.

**Friction worth fixing (not a lane failure):** the guarded gate script takes a script name, not a
single test file, so proving one integration test costs a full-suite run. That is a slow, expensive
loop for any lane whose acceptance is one focused DB test. Candidate for its own issue.

**Gate-collision note:** #1504's acceptance test (`tests/unit/prod-deploy-config.test.ts`) is a unit
test with no database, so it cannot collide with #1511's guarded run over the shared scratch
database name. No serialization needed between these two lanes.

## Lane #1511 — Opus plan delivered, at the Fable gate (2026-08-29)

Plan: `~/.coord-briefs/plan-1511-opus.md`, authored by `plan-1511-shares` (Opus 5).
Approver `approve-1511-shares` (Fable 5, pane w1:p1K, `planners` tab) is reviewing now.

**The re-plan earned its cost — the Opus author found a defect no one else had:** the Sonnet draft
proved "nothing was written" by asserting the shares table was empty after a failed grant. That
assertion is worthless here, because the whole path runs inside a transaction that rolls back
either way — it would pass whether or not the validation exists. The Opus plan asserts the exact
error wording instead, which is the only signal that distinguishes an up-front check from a
database foreign-key rejection firing later. **Generalised lesson: an assertion that passes
identically with and without the change is not a test.**

Also from the Opus pass:
- Stale citation confirmed and traced: two later migrations mention `app.get_user_by_id`, neither
  redefines it, so `0050_multi_user_accounts.sql` lines 59-81 is the live definition. It filters on
  `id` only — pending/deactivated users are already valid share targets, so the lifecycle test locks
  behaviour rather than fixing it. (Matches the coordinator's independent read.)
- Blast radius the Sonnet draft omitted entirely: seven other test files plus the full gate's
  seeding step call `SharesRepository.grant`. All read as safe, but the plan makes re-running them a
  required step rather than an assumption.
- Sized for one Sonnet session; no split needed.

**Fable handoffs are minimal — Ben's ruling, 2026-08-29: "here is the issue, here is the plan,
review it."** No long brief. The first Fable approver was spawned with a 60-line brief that also
listed the coordinator's own suspicions; it was killed and respawned (pane w1:p1M) on a short inline
prompt carrying only: the issue number, the spec section, the plan path, the verdict format, where
to send the verdict, and the one hard constraint (stay out of the lane worktree — a builder is
holding in it). The brief file was deleted so it cannot be copied forward.

That is a quality rule, not only a brevity one: the long brief pre-loaded the reviewer with what the
coordinator already suspected, which is exactly the anchoring an independent gate exists to avoid.

## CONTINUATION NOTE — coordinator relay #1, 2026-08-29 (context meter 70%)

**Mid-doing:** #1511 builder just released with the approved plan and is reconciling; #1504 plan is
at the Fable gate. Nothing merged, nothing reaped. No merges this run — `merges_since_relay` 0.

### Live fleet (resolve fresh by agent name; pane numbers reflow)

| Agent name | Model | Tab | State | What it owns |
| ---------- | ----- | --- | ----- | ------------ |
| `share-target-validation` | Sonnet | builders `w1:tG` | building — reconciling against approved plan | #1511 worktree `.claude/worktrees/1511-share-target-validation` |
| `approve-1504-tls` | Fable | planners `w1:tH` | reviewing #1504 plan | verdict only |
| `plan-1504-tls` | Opus | planners `w1:tH` | DONE — plan committed `6a0e361d6` | **reapable** |
| `plan-1511-shares` | Opus | planners `w1:tH` | DONE — plan delivered, Fable-approved | **reapable** |
| `approve-1511-shares` | Fable | planners `w1:tH` | DONE — verdict APPROVED, consumed | **reapable** |

Three panes above are reapable and were NOT reaped before this relay (relay trigger allows no
deferral). None owns a worktree — the two Opus planners and the Fable approvers only read. Reap
them and re-square the planners tab grid.

### Next actions for the successor, in order

1. Reap the three finished panes above; record one line each under Reaped sessions first.
2. When `approve-1504-tls` returns its verdict: if APPROVED, spawn the **Sonnet** builder for #1504
   in the **builders** tab (`w1:tG`) — brief already written at
   `~/.coord-briefs/boot-1504-tls-compose-proxy.txt`, but STRIP its "invoke plan-build and write a
   plan" instruction: under the new rule the builder executes the approved plan at
   `.claude/worktrees/1504-tls-compose-proxy/docs/superpowers/plans/2026-08-29-1504-tls-compose-proxy.md`
   and does not author one. If CHANGES REQUIRED, send them to `plan-1504-tls` — do not fix the plan
   yourself.
3. #1511: builder owes you (a) the result of the background integration run it left in flight and
   (b) confirmation it closed the gap between its 3 tests and the plan's 4. Then PR → QA.
4. #1511 QA is **sensitive** tier → Sonnet QA + invariant walk. #1504 QA is **security** tier →
   **Opus** adversarial QA + `gh pr comment` verdict + **Ben's sign-off**.
5. **#1504 does not merge when green.** Spec close rule: its PR stays open until Child 4 proves it
   from a real second LAN device. Lane is done at PR-open + green + reviewed.

### Known issues / traps hit this run

- `herdr agent start` frequently returns `timeout waiting for agent startup` while the agent HAS
  started, and the routing name does not register on that path. Always follow with
  `herdr agent rename <pane> <name>` and verify with `herdr agent list`.
- `approve-1504-tls` appears to have landed cwd'd in the #1504 worktree despite `--cwd` pointing at
  the main checkout. Harmless here (the Opus planner there is finished), but **verify pane cwd after
  split** — two live agents in one worktree is banned.
- The guarded gate script takes a script name, not a single test file, so proving one integration
  test costs a full-suite run. Candidate for its own issue.
- The manifest is UNCOMMITTED in the main checkout (direct push to `main` is blocked; needs a PR).
  Do not lose it. Fold it into a docs PR when convenient.
- Stale pane label: `w5:p1` (idle Codex, `~/jarv1s-fleet`) was relabelled `Fleet repo (idle)` — it
  had been sitting on the label `Coordinator`.

### Standing rules introduced mid-run by Ben (both already in this manifest)

- **Opus writes plans, Fable approves them.** Sonnet builders execute only. Coordinator approval is
  advisory, not the gate.
- **Fable handoffs are minimal:** issue + plan + "review it" + where to send the verdict. No brief,
  no coordinator suspicions — anchoring the reviewer defeats the gate.
- **One session per build task.** Doesn't fit → split into smaller issues, don't relay twice.

## Merge audit (one block per merged PR)

- `plan-1511-shares` (Opus, pane w1:p1J) — authored the #1511 plan; work landed at
  `~/.coord-briefs/plan-1511-opus.md`, Fable-approved and handed to the builder. Read-only in the
  main checkout, owned no worktree. Killed 2026-08-29 at relay #1.
- `approve-1511-shares` (Fable, pane w1:p1M) — approval gate for the #1511 plan; verdict APPROVED,
  consumed and acted on (builder released). Owned no worktree. Killed 2026-08-29 at relay #1.
- `plan-1504-tls` (Opus, pane w1:p1H) — authored the #1504 plan; work landed as commit `6a0e361d6`
  on branch `1504-tls-compose-proxy`. Worktree `.claude/worktrees/1504-tls-compose-proxy` is KEPT —
  it holds the committed plan and is where the #1504 builder will work. Killed 2026-08-29 at relay #1.
- (an earlier over-briefed Fable pane w1:p1K was killed and respawned as w1:p1M before producing any
  verdict — no output, superseded by the minimal-handoff rule.)

## Reaped sessions

- (none yet)

## CONTINUATION NOTE — coordinator relay #3, 2026-08-29 (context meter 70%)

**Mid-doing:** #1504 plan is APPROVED (commit `26c44b3d6`, verdict just posted by `approve-1504-tls`). Nothing merged yet.

### Next actions for the successor, in order

1. **Spawn the #1504 builder now.** Plan is approved — go straight to build, no more planning.
   - Worktree already exists: `.claude/worktrees/1504-tls-compose-proxy`, branch `1504-tls-compose-proxy`, plan at `docs/superpowers/plans/2026-08-29-1504-tls-compose-proxy.md` (approved, commit `26c44b3d6`).
   - Spawn a **Sonnet** builder in the **builders tab** (`w1:tG`, where `share-target-validation` already lives).
   - Brief already drafted at `~/.coord-briefs/boot-1504-tls-compose-proxy.txt` but it's stale (written for the old "builder authors the plan" flow) — **strip any "invoke plan-build / write a plan" instruction**. The builder's job is to *execute* the already-approved plan file, not author one.
   - Fable reviewer's reminder, carry into the brief explicitly: the builder verifies only what the brief names, so list all six spec acceptance checks **plus test case 10 (the restart-after-first-run check)** by name in the brief.
   - Tier: **security** (auth/TLS/network-exposed surface). QA later must be Opus adversarial + `gh pr comment` verdict + **Ben's explicit merge sign-off**. Per the manifest's Child-1 close rule: **this PR does NOT merge when green** — it stays open until Child 4 proves it from a real second LAN device. Lane is "done" at PR-open + green + reviewed, not merged.
2. **#1511 (share-target-validation, pane `w1:p1G`, tab `w1:tG`):** builder was told to reconcile its code against the approved plan (`~/.coord-briefs/plan-1511-opus.md`), close the gap between its 3 written tests and the plan's 4, confirm the "hollow test" concern is resolved (told it: exact-error-string assertions already satisfy this, no change needed there), and re-run the other 7 test files + gate seeding step that call the same method. It also has a background integration run in flight — do not kill it, wait for its result (exit code + failing test names if any). Last read of its pane showed it still mid-task (message was queued, not yet acted on). Check its pane fresh; don't assume progress.
3. Tier for #1511 is **sensitive** → Sonnet QA + invariant walk, once it reports PR-ready.
4. **Unreaped panes carried forward from the prior relay, still not reaped:** `plan-1511-shares` (w1:p1J, DONE), `approve-1511-shares` (DONE, verdict consumed), `plan-1504-tls` (w1:p1H, DONE). None owns a worktree (read-only planners/approvers) — safe to reap once you've confirmed via `herdr pane list` they're still idle/done. Also re-square the planners tab (`w1:tH`) grid after reaping.
5. Coordinator lock: this session's id is `83c24c17-eb69-4c28-935e-39ee21a01489`, pane `w1:p1P`. Successor must re-confirm its own session id against this file's lock line (top of file) before any merge.
6. Watchdog (`coordinator-watchdog.timer`) is not installed on this box — liveness is Monitor-based, not watchdog-based; don't try to start it again.

No CI waivers on file. No merges yet this run (`merges_since_relay: 0`).
